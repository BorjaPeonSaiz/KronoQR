# El .gitignore de la raiz ya cubre node_modules/, dist/ y coverage/.
# Aqui solo lo que produce esta aplicacion y no cubre aquel.
dev-dist/
*.local
.vite/
test-results/
playwright-report/
# Cache incremental de tsc/vue-tsc: se regenera sola y, si queda commiteada,
# arrastra referencias a ficheros ya borrados (como paso con la migracion a
# @kronoqr/web-kit, ADR-036).
tsconfig.tsbuildinfo
