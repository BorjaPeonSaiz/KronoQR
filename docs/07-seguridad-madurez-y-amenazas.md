# 07 — Seguridad: madurez (OWASP SAMM 2.0), mapa SDL y cobertura del modelo de amenazas
## KronoQR — Sistema de Control de Presencia y Registro Horario por QR

| Campo | Valor |
|---|---|
| **Producto** | **KronoQR** — fichaje de empleados mediante QR en quiosco (tablet) |
| **Fecha** | 8 de septiembre de 2026 (revisión de la tarea 5.11, guía de endurecimiento). Revisión anterior: 30 de agosto de 2026, cierre de Fase 2 |
| **Clasificación** | Documentación técnica interna |
| **Audiencia** | Arquitectura, Desarrollo, QA, DPO, revisor de seguridad externo (RS-11) |
| **Documentos hermanos** | `01-especificaciones-proyecto.md` §7 y §8, `02-stack-tecnologico-y-plan-implementacion.md` §7, `03-agentes-y-skills-ia.md` §6.6 |
| **Rama de origen** | `feat/2.6-incident-detection` (nació en `feat/ssdlc-pipeline`) |

> Este documento no es asesoramiento jurídico. Señala requisitos, controles y riesgos; la validación legal corresponde a la asesoría del cliente y a su DPO.

## 1. Propósito y uso

El proyecto tiene desde el principio requisitos de seguridad (doc 01 §8), un modelo de amenazas (§8.1) y un diseño de seguridad (doc 02 §7). Lo que no tenía era una **medida honesta de hasta dónde llega todo eso en la práctica**: qué está verificado por una herramienta, qué está escrito y nadie ejecuta, y qué no existe. Este documento es esa medida, con dos marcos externos para que la escala no sea propia: **OWASP SAMM 2.0** para la madurez del proceso (§2) y **Microsoft SDL** para comprobar que no falta ninguna práctica canónica (§3).

Cómo se usa:

- **Se revisa en cada cierre de fase** (doc 03 §6.6, paso 1) por `seguridad-cumplimiento`. El informe de cierre incluye la tabla resumen del §2.6 actualizada.
- **Un nivel solo sube con evidencia** que exista en el repositorio: fichero, prueba, job de CI o ADR. Una intención, una tarea planificada o una frase en un documento no suben el nivel; lo que hacen es aparecer en la columna «siguiente nivel».
- **Cada nivel cita su evidencia con ruta**. Quien revise puede comprobarla con `ls`/`grep`; si la ruta desaparece, el nivel baja.
- Los riesgos que se aceptan a sabiendas se anotan en §6 con su dueño y su fecha límite. Un riesgo aceptado sin dueño es un riesgo olvidado.

La escala SAMM va de 0 (nada) a 3 (práctica completa, medida y mejorada). Cuando los dos flujos (*streams*) de una práctica están en niveles distintos se indica «A/B»; el nivel de la práctica es el menor de los dos.

## 2. Autoevaluación OWASP SAMM 2.0

### 2.1 Gobierno

| Práctica | Nivel | Evidencia verificada | Para el siguiente nivel | Tarea |
|---|---|---|---|---|
| Estrategia y métricas | **1** | RS-01..RS-12 en doc 01 §8 y objetivo ASVS 2 en doc 02 §7.6; `backend/tests/Architecture/QualityGatesTest.php` ata RS-08, RS-09, RS-10 y RS-12 a comprobaciones que fallan; matriz `docs/trazabilidad-pruebas.md` generada por `qa:traceability`. No hay métricas de seguridad seguidas en el tiempo ni hoja de ruta de seguridad distinta del plan. | **2**: la tabla del §2.6 revisada por fase con hallazgos por severidad y tiempo hasta corregir; objetivo de nivel por práctica y fecha. | Cierre de fase (doc 03 §6.6); 3.8 |
| Política y cumplimiento | **2** | 21 reglas duras en `CLAUDE.md`, cada una atada a una herramienta (Deptrac en el job ② de `.github/workflows/ci.yml`, `backend/tests/Architecture/DomainPurityTest.php`, `.semgrep/kronoqr-php.yaml`); RL-* en doc 01 §7; skill `.claude/skills/revision-cumplimiento/SKILL.md`; `docs:consistency --check` y `qa:traceability --check` en el job ③; ADR-032 fija que la Fase 1 sea legalmente defendible; **obligaciones legales del cliente escritas y entregables**: `docs/cliente/obligaciones-legales.md` (registro de actividades, base jurídica, EIPD, retención por tipo de dato, canal de correo como encargado del art. 28), `docs/cliente/operacion.md` y `docs/cliente/instalacion.md`. | **3**: la checklist ASVS del §7.6 **ejecutada y archivada por versión publicada** (hoy solo está enunciada). | 5.11; 3.8 |
| Educación y orientación | **1** | El conocimiento vive en los prompts (`.claude/agents/seguridad-cumplimiento.md`, `revisor-codigo.md`) y en doc 03; doc 02 §7 hace de guía de diseño. No hay formación para personas ni guía de codificación segura más allá de eso. | **2**: guía breve de codificación segura PHP/Vue enlazada desde la Definición de Terminado; lista de lectura obligatoria para quien se incorpora. | Sin tarea |

### 2.2 Diseño

| Práctica | Nivel | Evidencia verificada | Para el siguiente nivel | Tarea |
|---|---|---|---|---|
| Modelado de amenazas | **2** | Doc 01 §8.1: STRIDE con técnica ATT&CK por fila y nota de detección; decisiones nacidas de una amenaza: ADR-005 (firma HMAC), ADR-009 (sin biometría), ADR-014 y `docs/04` (tarjeta física), ADR-033 (tres roles de BD), ADR-038 (límite por dispositivo e IP, no por credencial); revisión por funcionalidad con la skill `revision-cumplimiento`. Sin diagrama de flujo de datos ni revisión sistemática del modelo por fase. | **3**: modelo revisado en cada cierre de fase, con la cobertura técnica → detección de §8.1 como lista de pendientes; DFD de los tres clientes y el quiosco físico. | Cierre de fase |
| Requisitos de seguridad | **2** | RS-01..RS-13 (doc 01 §8) y RL-* (§7) etiquetados en pruebas: `QualityGatesTest.php`, `backend/tests/Feature/Identity/PortalPinLockoutTest.php` (RS-12), `backend/tests/Feature/Identity/AuthenticationTrailTest.php` (RS-13), `backend/tests/Feature/Identity/CredentialStatusDisclosureTest.php` (RS-03); `qa:traceability --check` falla en CI si un requisito sin prueba pertenece a una **fase ya ejecutada** (RQ-13) — el alcance de la puerta lo fija `quality.current_phase` y `PhaseOrder::isExecutedBy()`, así que un requisito de una fase futura no la hace fallar: **es una puerta de progreso, no de cobertura total**. Requisitos hacia proveedores: no aplica (on-premise, sin subencargados de datos, ADR-016). | **3**: mapa requisito ↔ control ASVS por versión. | 2.1 (RS-06) |
| Arquitectura segura | **2** | Hexagonal con frontera verificada: Deptrac (job ②), `DomainPurityTest.php`, `backend/tests/Architecture/CoreBoundariesTest.php`, ADR-025; tres roles de BD sin `UPDATE`/`DELETE` sobre `audit_log` (`infra/docker/postgres/initdb/02-application-roles.sh`, migración `2026_08_19_099000_provision_database_privileges.php`); firma en tiempo constante (`backend/app/Modules/Identity/Infrastructure/Adapter/HmacSignatureVerifier.php`, `hash_equals`); cadena de auditoría (`backend/app/Modules/Compliance/Domain/AuditChain.php`, ADR-010); acciones de CI fijadas por SHA con `persist-credentials: false`. **Hueco**: doc 02 §7.1 promete SRI en assets y no está implementado (§6). | **3**: inventario de componentes a partir del SBOM (§5) y lista de tecnologías aprobadas con criterio de entrada y salida. | Sin tarea |

### 2.3 Implementación

| Práctica | Nivel | Evidencia verificada | Para el siguiente nivel | Tarea |
|---|---|---|---|---|
| Build seguro | **1** (A 2 / B 1) | Job `security` («Dependencias y SAST») de `ci.yml`: `composer audit` con 0 avisos, `npm audit --audit-level=high` sobre el workspace y Semgrep sobre `.semgrep/` con `--error` (`make deps-audit-php`, `deps-audit-js`, `sast`); `QualityGatesTest.php` «analiza dependencias y codigo en cada integracion» (RS-10); *lockfiles* versionados. Sin SBOM, sin escaneo de imágenes, sin firma de artefactos; `.github/workflows/release.yml` es un marcador. | **2**: SBOM CycloneDX, Trivy y Dependabot en verde (§5). **3**: artefactos firmados y verificados al instalar. | 5.4; 5.7 |
| Despliegue seguro | **1** | `infra/compose.prod.yaml`; secretos fuera del repositorio verificados por `QualityGatesTest.php` («mantiene los secretos fuera del control de versiones», «no deja ningun secreto real en el fichero de ejemplo», RS-08); ADR-029; **rotación de secretos con procedimiento escrito** (tarea 2.12): `docs/runbooks/rotacion-secretos.md` (`APP_KEY`, claves HMAC del QR, los tres roles de BD, tokens de dispositivo, clave de copia) y `docs/runbooks/rotacion-clave-qr.md` (rotación con solape y reimpresión progresiva, sin dejar a nadie sin fichar). **Los dos huecos que tenía esta fila están CERRADOS (01-09-2026, tarea 5.4)**: `infra/scripts/install.sh` genera los once secretos con `openssl` en el servidor del cliente, escribe el `.env` en `0600`, no imprime ninguno y lo comprueba el escenario C de la etapa ⑧ de `ci.yml`; `.env.example` entrega `APP_DEBUG=false` y ``AppSupportnvironmentProductionSafetyGuard`` impide arrancar en producción con `true`. Queda la firma de artefactos, que es de nivel 3. | **2 en la revisión de cierre de Fase 5**: la evidencia ya existe (instalador que genera secretos, gitleaks bloqueante en CI, `APP_DEBUG=false` de serie); **el nivel no se sube aquí porque lo sube `seguridad-cumplimiento` con su propia comprobación**, no quien implementa. | 5.4 ✔; 5.7 |
| Gestión de defectos | **1** | Escala de severidad y formato de hallazgo en `seguridad-cumplimiento.md`; auditoría de cierre de Fase 1 con correcciones en `fix/auditoria-fase-1` (inyección de fórmulas CSV, OWASP A03) y pendientes en `HANDOFF.md`. Sin registro de defectos de seguridad con plazo por severidad ni métricas. | **2**: plazo por severidad (crítico: antes de publicar; alto: en la versión siguiente) registrado donde vivan las incidencias; recuento por fase en §2.6. | Sin tarea |

### 2.4 Verificación

| Práctica | Nivel | Evidencia verificada | Para el siguiente nivel | Tarea |
|---|---|---|---|---|
| Evaluación de arquitectura | **1** | Revisión de `seguridad-cumplimiento` y `revisor-codigo` en cada cierre (doc 03 §6.6); auditoría independiente de la Fase 1 (27-08-2026, `HANDOFF.md`). Las pruebas de arquitectura comprueban que el código respeta la arquitectura, no que la arquitectura sea segura. | **2**: evaluación por fase contra el modelo de amenazas, con este documento como acta y una lista de controles ausentes por fila STRIDE. | 3.8 |
| Pruebas dirigidas por requisitos | **2** | `backend/tests/Feature/AuthorizationNegativeTest.php` (parejas rol × endpoint, regla dura 18) y `*AuthorizationTest.php` en Attendance, Compliance, Kiosk y Reporting; `PortalPinLockoutTest.php` (RS-12); `CredentialStatusDisclosureTest.php` (RS-03); `QualityGatesTest.php` sobre cabeceras (RS-09), portal en red interna (RF-ID-08) y señuelo de tiempo constante del PIN (RS-03/RS-12); **el aislamiento por departamento (RF-ID-03, RS-05) probado conjunto a conjunto** en `backend/tests/Feature/Identity/DepartmentScopeTest.php`, `Identity/ScopeDenialAuditThrottlingTest.php`, `Compliance/IncidentScopeTest.php`, `Reporting/LivePresenceScopeTest.php`, `Reporting/PeriodReportScopeTest.php` y `Reporting/SelfScopeIsolationTest.php`; mutación sobre el dominio con MSI ≥ 80 % (job ③). | **3**: pruebas de abuso derivadas de cada fila STRIDE (saturación del borde, token con ámbito manipulado) y de propiedades sobre el parser del payload QR. | 3.6; 3.7 |
| Pruebas de seguridad | **1** | SAST limitado a las dos reglas propias de `.semgrep/kronoqr-php.yaml` (reloj del sistema en el dominio, supresiones de PHPStan sin motivo) y auditoría de dependencias. Sin DAST, sin *fuzzing*, sin prueba de penetración. | **2**: Semgrep comunitario y Trivy (§5); DAST de referencia (ZAP *baseline* contra el entorno de Compose). **3**: pentest externo. | 3.8 (RS-11) |

### 2.5 Operaciones

| Práctica | Nivel | Evidencia verificada | Para el siguiente nivel | Tarea |
|---|---|---|---|---|
| Gestión de incidentes | **1** | **Detección:** enrutado «Crítica (seguridad)» en doc 01 §9.3 y `infra/observability/alertmanager/alertmanager.yml`; alertas `KronoqrAuth*` con técnica ATT&CK en `infra/observability/prometheus/rules/auth.yml` y asientos `auth.*`, `access.denied` y `personal_data.accessed` en `audit_log` (ADR-037, ADR-039); `backend/tests/Architecture/BackupAndAlertingTest.php` exige runbook por alerta. **Respuesta:** `docs/runbooks/rotura-cadena-auditoria.md`, `ataque-a-credenciales.md`, `triaje-hallazgos-seguridad.md` y **`brecha-de-seguridad.md` (30-08-2026, RL-15)** —72 h del art. 33, alcance acotado desde `audit_log`, preservación y sellado de evidencia, plantillas de notificación—. **Sigue faltando** el canal de notificación de vulnerabilidades (no hay `SECURITY.md`) y no se ha hecho ningún simulacro: **el nivel no sube todavía**, porque la propia definición de 2 de este documento exige los tres y solo hay dos. | **2**: canal de reporte de vulnerabilidades (`SECURITY.md` con contacto y plazo) y un simulacro de brecha con acta, al modo del simulacro trimestral de copia. | 3.10; 3.8 |
| Gestión del entorno | **2** | `infra/docker/nginx/snippets/security-headers.conf` (CSP sin `unsafe-inline`, `camera=(self)`) verificado por `QualityGatesTest.php` (RS-09); `ssl_protocols TLSv1.3` y cinco zonas `limit_req_zone` en `infra/docker/nginx/templates/kronoqr.conf.template`; roles de BD con `NOINHERIT` y `REVOKE CREATE ON SCHEMA`; portal restringido por CIDR; imágenes de observabilidad fijadas por versión en `compose.prod.yaml`; **etiqueta inmutable obligatoria en las tres imágenes del producto** desde la 5.4 (`${IMAGE_TAG:?…}` sin valor por defecto: ni `latest` ni ninguna etiqueta movible en producción, comprobado por `QualityGatesTest`); `APP_DEBUG=false` de serie con guarda de arranque; **guía de endurecimiento entregada al cliente (08-09-2026, tarea 5.11)**: `docs/cliente/endurecimiento.md` y `docs/cliente/en/hardening.md` —reparto fabricante/cliente control a control, exposición por ruta y por rango, TLS, anfitrión, secretos, tablets, SMTP, cuentas, observabilidad y lo que se sirve sin autenticar—, atadas por `backend/tests/Architecture/ClientDocumentationTest.php` a que existan en las dos lenguas, a que las enlacen `instalacion.md` y `operacion.md`, y a que sus comandos no diverjan entre idiomas; viajan en el paquete porque `infra/scripts/package.sh` copia `docs/cliente`. Trivy bloqueante sobre las imágenes (§5). | **El nivel se queda en 2, y esta vez con el motivo escrito.** Las dos condiciones que esta fila ponía —Trivy y guía— ya están, pero el 3 de SAMM no es «existe una guía»: es **conformidad verificada de forma continua** contra la baseline y **plazo medido** de parcheo, y no hay ninguna de las dos. La baseline la repasa el IT del cliente a mano y cada tres meses (§11 de la guía), y la revisión de la 5.11 encontró **dos filas de esa lista cuyo comando no comprueba lo que la fila afirma** —la copia de anoche y los permisos del `.env` vía `./doctor.sh`, que con `app` en marcha delega entero en `product:doctor`, y ahí no hay ni sonda de frescura de copia ni sonda del `.env`—. Una baseline con comprobaciones que no comprueban no sostiene un nivel 3. **Para el 3**: corregir esas dos filas, añadir a `product:doctor` una sonda de frescura de copia y una de conformidad de endurecimiento —lo que hoy es una lista en papel pasa a ser la salida de un comando— y fijar un plazo de parcheo por severidad. | 5.11 ✔ (la guía); las sondas de conformidad y el plazo de parcheo, sin tarea |
| Gestión operativa | **2** | Copias cifradas y verificadas: `infra/scripts/backup.sh`, `restore.sh`, `restore-drill.sh`; simulacro trimestral en `.github/workflows/backup-drill.yml`; alertas en `infra/observability/prometheus/rules/backup.yml`; `docs/runbooks/restaurar-backup.md`; clasificación y retención de datos en doc 01 §7; purga del padrón al desvincular el dispositivo; **purga de retención implementada** (tarea 2.10, RL-11, RF-PR-03): `compliance:apply-retention` con propuesta semanal, frase de confirmación que caduca, rol `fichaje_maintenance` fuera del `.env` (ADR-033), sellado de particiones de `audit_log` en lugar de `DELETE` (ADR-027), informe archivable y procedimiento en `docs/runbooks/solicitud-derechos-rgpd.md` §6. Sin exportación íntegra de la instalación. | **3**: exportación íntegra (5.10). Gestión de sistemas heredados: no aplica. | 5.10 |

### 2.6 Resumen y puntuación

| Función | Práctica | Nivel | Evidencia principal |
|---|---|---|---|
| Gobierno | Estrategia y métricas | 1 | doc 01 §8 + `QualityGatesTest.php` |
| Gobierno | Política y cumplimiento | 2 | `CLAUDE.md` reglas duras + `docs:consistency`/`qa:traceability` en CI |
| Gobierno | Educación y orientación | 1 | Prompts de agentes, doc 03 |
| Diseño | Modelado de amenazas | 2 | doc 01 §8.1 con ATT&CK; ADR-005/009/014/033/038 |
| Diseño | Requisitos de seguridad | 2 | RS-* etiquetados en pruebas; RQ-13 en CI |
| Diseño | Arquitectura segura | 2 | Deptrac + pruebas de arquitectura; roles de BD; `hash_equals` |
| Implementación | Build seguro | 1 | Job `security` de `ci.yml` |
| Implementación | Despliegue seguro | 1 | `compose.prod.yaml`; pruebas RS-08 |
| Implementación | Gestión de defectos | 1 | Escala de severidad; auditoría de Fase 1 |
| Verificación | Evaluación de arquitectura | 1 | Cierre de fase (doc 03 §6.6) |
| Verificación | Pruebas dirigidas por requisitos | 2 | `AuthorizationNegativeTest.php`; `PortalPinLockoutTest.php` |
| Verificación | Pruebas de seguridad | 1 | `.semgrep/kronoqr-php.yaml`; `composer`/`npm audit` |
| Operaciones | Gestión de incidentes | 1 | `rotura-cadena-auditoria.md` y `brecha-de-seguridad.md`; `BackupAndAlertingTest.php`. Falta canal de reporte |
| Operaciones | Gestión del entorno | 2 | `security-headers.conf`; TLS 1.3; `limit_req_zone`; `docs/cliente/endurecimiento.md` atada por `ClientDocumentationTest.php` |
| Operaciones | Gestión operativa | 2 | `backup-drill.yml`; `restore-drill.sh`; `compliance:apply-retention` |

| Función | Puntuación (media de sus 3 prácticas, sobre 3) |
|---|---|
| Gobierno | **1,33** |
| Diseño | **2,00** |
| Implementación | **1,00** |
| Verificación | **1,33** |
| Operaciones | **1,67** |
| **Total** | **1,47** |

Lectura: el diseño va por delante de la implementación. Lo que está escrito y verificado (fronteras, autorización, cabeceras, copias, cadena de auditoría) es sólido; lo que falta es lo que rodea al código: inventario de dependencias, escaneo de imágenes, secretos, detección de ataques a la autenticación y respuesta a incidentes. El §5 apunta exactamente ahí.

## 3. Mapa a Microsoft SDL

| Práctica SDL | Dónde vive en este proyecto | Estado |
|---|---|---|
| Formación en seguridad | Prompts de agentes y doc 03; nada para personas | **No cubierta** |
| Requisitos de seguridad | Doc 01 §8 (RS-*) y §7 (RL-*); trazabilidad RQ-13 en CI | Cubierta |
| Umbrales de calidad y *bug bars* | Severidad en `seguridad-cumplimiento.md`; RS-10 (0 críticas o altas en versión publicada); sin plazos de corrección | Parcial |
| Diseño y modelado de amenazas | Doc 01 §8.1 (STRIDE + ATT&CK); ADR; skill `revision-cumplimiento` | Cubierta |
| Criptografía | HMAC-SHA256 en el QR (ADR-005) verificado con `hash_equals`; bcrypt coste 12 para contraseñas y PIN (verificado por `QualityGatesTest.php`); TLS 1.3 en el borde; copias cifradas (`BackupAndAlertingTest.php`, «cifra las copias»). Sin rehash al iniciar sesión (§6) | Cubierta |
| Gestión de riesgo de terceros | `composer audit` y `npm audit` en CI; sin SBOM ni política de proveedores (no hay subencargados de datos, ADR-016) | Parcial → §5 |
| Herramientas aprobadas | Doc 02 §3.5 (una herramienta por convención, verificado por `QualityGatesTest.php`); versiones fijadas (`make tool-versions`, SHAs en `ci.yml`) | Cubierta |
| Componentes inseguros u obsoletos | Auditoría de dependencias en CI; sin actualización automática | Parcial → §5 (Dependabot) |
| SAST | Semgrep con reglas propias (`.semgrep/kronoqr-php.yaml`); PHPStan 9 | Parcial → §5 (Semgrep comunitario) |
| DAST | — | **No cubierta** |
| Pruebas de penetración | Tarea 3.8 (RS-11): ninguna versión comercial antes | Pendiente |
| Respuesta a incidentes | `rotura-cadena-auditoria.md` y `brecha-de-seguridad.md` (72 h, RL-15); falta el canal de reporte de vulnerabilidades y el simulacro | Parcial |
| Despliegue y configuración segura | Cabeceras, TLS, roles de BD, secretos fuera del repositorio; instalador y rotación pendientes (5.4) | Parcial |

## 4. Shift-left y juicio humano

El flujo de agentes (doc 03) desplaza la seguridad hacia el diseño de tres maneras concretas:

1. **Hallazgo y corrección están separados por construcción.** `seguridad-cumplimiento` y `revisor-codigo` se declaran con `tools: Read, Grep, Glob, Bash`, sin `Write` ni `Edit`: pueden leer, ejecutar comprobaciones y escribir un informe, pero no tocar código. Quien corrige es el agente dueño de la capa (`backend-laravel`, `devops-observabilidad`, `frontend-*`), y vuelve a pasar por revisión. Este documento es la excepción explícita: se ha escrito con permiso de escritura **solo sobre documentación**, y lo dice.
2. **Una convención sin herramienta es una sugerencia** (`CLAUDE.md`, doc 02 §3.5). Las reglas duras no dependen de que alguien las recuerde: Deptrac, PHPStan, Semgrep y las pruebas de arquitectura fallan la CI. El coste de una regla nueva incluye su comprobación.
3. **La revisión llega antes del código.** La skill `revision-cumplimiento` y el paso 1 del cierre de fase (doc 03 §6.6) se aplican al diseño y al contrato OpenAPI, que se modifica antes que el código (ADR-013).

Lo que **no** decide un agente, y exige a una persona con nombre:

- **Contradecir un ADR.** Solo con otro ADR (`CLAUDE.md`, orden de autoridad). Un agente que lo necesite para y pregunta.
- **Excepciones de análisis estático.** Una supresión de PHPStan sin motivo falla la CI (`.semgrep/kronoqr-php.yaml`); la misma regla debe aplicarse a cualquier `nosemgrep` o exclusión de Trivy cuando existan (§5): motivo escrito, autor y fecha.
- **Aceptar un riesgo.** Se anota en §6 con dueño y fecha límite. Ejemplos ya tomados y registrados en `HANDOFF.md`: mantener sin rotular la zona horaria en el portal contra el hallazgo del revisor; no enviar la tarjeta por correo (ADR-014).
- **Ampliar la finalidad de un tratamiento de datos personales**, exponer el portal fuera de la red interna, o conceder acceso de soporte al fabricante (RF-PD-11, ADR-020): decisión del cliente y su DPO, nunca por defecto.
- **Descartar un hallazgo de severidad ALTO o CRÍTICO.** Se puede, pero queda escrito quién y por qué.

## 5. Lo que se está añadiendo en esta rama (en curso)

Nada de esta tabla cuenta como evidencia hasta que el job correspondiente esté en verde en `ci.yml` y el fichero exista en el repositorio. Los niveles de la última columna son los que alcanzaría la práctica **al quedar en verde**, no los actuales.

| Elemento | Qué aporta | Práctica SAMM que mueve | Nivel al quedar en verde |
|---|---|---|---|
| Semgrep con reglas comunitarias (PHP, OWASP Top 10) además de `.semgrep/`, **bloqueante desde el 29-08-2026** (9 hallazgos triados: 4 corregidos, 5 justificados con `# nosemgrep`) | SAST real sobre inyección, deserialización, SSRF, criptografía débil | Pruebas de seguridad (A) | 1 → 2 |
| Trivy sobre imágenes y sistema de ficheros | Vulnerabilidades del sistema operativo base y de dependencias empaquetadas | Build seguro (B); Gestión del entorno (parcheo) | 1 → 2; base para 3 |
| Dependabot | Actualización automática de dependencias y acciones de CI, con PR | Build seguro (B); Gestión de defectos | 1 → 2 |
| gitleaks | Secretos en el historial y en cada PR; complementa las pruebas RS-08 de `QualityGatesTest.php` | Despliegue seguro (B, gestión de secretos) | 1 → 2 |
| SBOM CycloneDX (PHP y JS) | Inventario de componentes por versión publicada; entrada del ADR de componentes aprobados | Build seguro (B); Arquitectura segura (B) | 2 (3 exige firma y verificación al instalar) |
| Auditoría de autenticación (ADR-039) —inicio de sesión y cierre en `audit_log` solo en el panel, **apertura de bloqueo** en los tres canales y escrita *después de responder*; cada **fallo** en el log técnico (`auth.login_failed`) y en `kronoqr_auth_attempts_total`— y alertas `KronoqrAuth*` en `infra/observability` | Señal observable para `T1110.001`; cierra OWASP A09 y es la evidencia de RS-13 (doc 01 §8) | Gestión de incidentes (A, detección); Requisitos de seguridad | 1 → 2 |
| Emparejamiento de un quiosco por código (RF-PD-06, tarea 5.6): dos rutas **públicas** —`POST /kiosk/pair` y `/pair/claim`— con limitadores por IP y por `pairing_id`, cota de solicitudes vivas, rechazo único y de tiempo constante, y `confirm` de `admin` | Es la primera escritura pública del camino del quiosco. El rechazo del `claim` es **uno solo** para «no existe», «no es tuya» y «ya no vale»: el `hash_equals` se ejecuta siempre —también sin fila, contra un hash señuelo— y el suelo de tiempo es el **mismo objeto y el mismo umbral** que usa la resolución de credenciales (`security.rejection_floor_ms`), así que la ruta no es un comprobador de solicitudes ni un oráculo de secretos. La cota `max_live_pending` es lo que el techo por IP no puede dar: aquel frena a un origen y esta mira el conjunto. Y nada se vincula sin el `confirm` de un `admin` con `settings:*` y `KioskPairingPolicy`, que deja asiento `device.provisioned` **con actor** | Arquitectura segura (B); Modelado de amenazas (`T1078` credenciales válidas obtenidas por adivinación del secreto; `T1499` denegación por agotamiento del espacio de códigos) | 2 → 2 (evidencia que sostiene el nivel, no lo sube: la práctica ya estaba en 2 por el camino de fichaje) |
| Actualizador `update.sh` (RF-PD-10, tarea 5.7): mantenimiento **antes** de la copia previa verificada y bloqueante; la versión nueva se verifica **sin borde** (FastCGI desde dentro) antes de recibir tráfico; cadena de auditoría verificada antes y después; restricciones de RN-01/RN-02 comprobadas tras migrar; `.env` copiado en 0600 y certificado con su propietario; **ningún secreto** en la salida ni en el informe, comprobado valor a valor en la etapa ⑧b; licencia inválida no bloquea; vuelta atrás automática probada con fallo inyectado | Cierra el hueco de «actualización sin procedimiento» de Despliegue seguro y da evidencia de gestión de cambios con vuelta atrás verificada. Riesgo aceptado: el paquete descomprimido **encima** del anterior deja la vuelta atrás con el compose nuevo y las imágenes antiguas (avisado, no bloqueado) | Despliegue seguro (B); Gestión del entorno | 1 → 2 |
| Marca blanca (RF-PD-08, tarea 5.8): primera **lectura pública** de `installation_settings` —`GET /api/v1/branding` y `/branding/logo`, `security: []`, limitadas por IP— con proyección cerrada a las cinco claves de presentación; el logotipo se valida **al guardar** (ruta absoluta dentro de `BRANDING_LOGO_ROOT` por `realpath`, PNG/SVG por contenido, 512 KiB, 2048 px, sin `<script` en SVG) y se sirve por contenido con `nosniff`, el SVG con CSP `default-src 'none'; sandbox`; la escritura sigue exigiendo `admin` con `settings:*`, probada en negativo con tokens de quiosco y de portal | Cierra, antes de que exista, el camino «ruta de fichero elegida por el administrador → endpoint público sirve cualquier fichero del servidor» (`T1005`, datos del sistema local) y «SVG con script servido por el propio origen» (`T1189`); lo que la marca revela sin autenticar es lo mismo que lleva impreso cada tarjeta | Requisitos de seguridad; Arquitectura segura (B) | 2 → 2 (evidencia que sostiene el nivel) |
| Diagnóstico y soporte (RF-PD-09, RF-PD-11, RF-PD-13, tarea 5.9): paquete de diagnóstico **por lista de permitidos** —sin nombres, correos, jornadas, secretos ni razón social; una prueba con volumen lo afirma sobre el paquete completo—; incluir datos personales es acción distinta, de `admin` y auditada; acceso de soporte como **token ligado a la concesión** con alcance, caducidad efectiva y revocación en el acto, y asiento `support_grant.used` en cada uso efectivo; `product:doctor` y `doctor.sh` | Cierra la minimización del canal de soporte (ADR-020, RL-19) y da por fin **señal observable a `T1199`** (relación de confianza): conceder, usar y revocar quedan en `audit_log` visibles para el cliente, y un uso fuera del incidente que lo motivó se ve | Requisitos de seguridad (D, privacidad); Gestión del entorno (O, comprobación de configuración); Gestión de incidentes (O) | Requisitos 2; Entorno 1 → 2; Incidentes 1 → 2 |
| Histórico de errores en base de datos (RF-PD-15, tarea 5.12): `error_events` agrupado por huella, con **saneado de servidor** y lista cerrada de dieciséis claves en `context`; captación de los cuatro orígenes del servidor con un único `reportable` más un contexto de ejecución, sin que ningún módulo toque `bootstrap/app.php`; canal de cliente **sin ruta anónima** —el quiosco dentro del latido, panel y portal por `POST /client-errors` con zona de limitación propia—, y `source`, `device_id` y `employee_uuid` decididos por el **token** y nunca por el cuerpo; escritura por **conexión propia**, que sobrevive al `ROLLBACK` de la transacción que acaba de fallar y no puede entrar en la de un fichaje; purga a 90 días (RL-11) que solo toca esta tabla, verificada contra `audit_log` y contra los datos de jornada; lectura de `admin` **y de un acceso de soporte**, resolución **cerrada a los tres alcances de soporte**; `application_errors_total{source,level}` y la alerta `ErroresCriticosNuevos` al IT del cliente | Da al cliente el rastro de lo que falla sin depender de un stack de observabilidad que es opcional en su instalación, y le da a soporte lo único que ADR-020 le concede sin entrar en el servidor. El uso de la pantalla por un actor de soporte queda en `audit_log` por el middleware `RecordSupportAccess`, que ya cubre el grupo entero. La revisión encontró tres huecos en la garantía central —«en esta tabla no hay datos personales»— y los tres se cerraron **en la misma tarea, antes de integrar** (§6): el mensaje de una `QueryException` con los valores enlazados (ahora `getSql()` con sus `?`, y el sanitizador corta `, SQL: `, `Key (…)=(…)` y `Failing row contains (…)`), el `critical` de cliente decidido sin mirar el origen (ahora `ErrorLevel::forClientCode(source, code)` más el catálogo cerrado `ClientErrorCode` atado por prueba a los ficheros de los clientes) y la ausencia de techo de grupos (`PRODUCT_ERRORS_MAX_OPEN_GROUPS_PER_SOURCE`, grupo `overflow`, sonda `app.error_history` de `doctor`). La prueba de PII provoca ahora una `QueryException` real con `DETAIL: Key` | Requisitos de seguridad (D, privacidad); Gestión de incidentes (A, detección) | Sostiene Requisitos 2 con evidencia (una revisión que encontró y cerró huecos en la misma tarea es la práctica funcionando, no una intención) y aporta la señal de detección que hasta ahora solo daba `auth.yml` |

Cuando todo quede en verde, la puntuación de Implementación pasaría de 1,00 a 1,67 y la de Operaciones a 2,00; Verificación a 1,67. El total quedaría en torno a 1,73. Se recalcula con la evidencia delante, no ahora.

## 6. Riesgos aceptados y decisiones pendientes

| Riesgo o decisión | Estado | Requisito | Qué se hace | Hasta |
|---|---|---|---|---|
| **Ventana de auto-alta del segundo factor**: entre que se crea una cuenta de `admin`, `rrhh` o `auditor` y que su titular activa su TOTP, quien tenga **solo la contraseña** puede completar `POST /auth/2fa/enrol` + `/confirm` y quedarse él con el segundo factor | **Aceptado, con control compensatorio** (29-08-2026, tarea 2.1). Es el precio de que RS-06 sea aplicable sin repartir secretos por SSH: sin estos dos endpoints, una cuenta nueva de `rrhh` no tiene ninguna forma de entrar. La ventana se cierra sola en el primer acceso del titular, y el ataque exige la contraseña — que es justo lo que 2FA protege *después* de esa ventana, no durante | RS-06 | **Control compensatorio, ya implementado:** el asiento `auth.two_factor_enabled` en `audit_log` deja actor, momento e IP de cada activación, así que el alta indebida es detectable y no repudiable; el alta respeta además el bloqueo por intentos de código, y el reto dura minutos. **Palanca futura, decisión de producto pendiente:** vale de un solo uso emitido por `identity:create-user` y exigido en `/auth/2fa/enrol` — mueve el secreto de la primera activación al canal por el que hoy ya se entrega la contraseña, sin inventar un canal nuevo. **Revisado en el cierre de Fase 2 (30-08-2026): la decisión sigue abierta y es del usuario**, no de un agente —está en `HANDOFF.md` desde la 2.1—. Nada de la Fase 2 la ha cambiado: el control compensatorio sigue en pie y el vale de un solo uso sigue siendo la opción recomendada. Se prorrogó a la 5.4 suponiendo que el instalador crearía la primera cuenta de gestión. **Revisado el 01-09-2026 al implementar la 5.4: esa premisa era falsa y la prórroga se corrige, no se repite.** El instalador NO crea usuarios y no debe crearlos: hacerlo obligaría a inventar un canal por el que entregar esa credencial —correo, salida por pantalla, fichero en el servidor— y los tres son peores que el problema que resuelven, además de chocar con la regla dura 6 (el alta de la primera cuenta se audita, y un script no es un actor auditable). Quien crea la primera cuenta es el **asistente de puesta en marcha**, desde el panel, y ahí sí hay una persona identificada delante. **Decisión del usuario (01-09-2026): se implementa en la 5.5, con esa primera cuenta.** **Implementado el 02-09-2026 (tarea 5.5), y el riesgo residual cambia de forma —no desaparece—.** `POST /api/v1/setup/administrator` es la única escritura pública del producto y su única guarda es de **estado**, no de permisos: existe mientras no haya **ninguna** cuenta de gestión. Dicho sin rodeos: **entre que el instalador termina y el cliente abre el panel, quien alcance esa URL se convierte en el administrador de la instalación** —con contraseña y TOTP suyos—, y el cliente legítimo se encuentra un `409` que le remite a una pantalla de acceso cuya contraseña no tiene. Es la misma ventana de la 2.1 desplazada al primer minuto de vida de la instalación, y es el precio de no repartir credenciales por SSH ni por correo. **Controles compensatorios, los tres implementados y probados:** (1) el alta deja asiento en `audit_log` **con actor nulo** —no hay sesión detrás y atribuírselo a alguien sería falsificar el *trail*—, y la activación del TOTP deja `auth.two_factor_enabled` con momento e IP, así que la usurpación es detectable y no repudiable desde el primer segundo; (2) `throttle:setup` (10 r/m por origen, `PRODUCT_SETUP_RATE_LIMIT`) acota el sondeo de la ruta; (3) la ventana **se cierra contando también las cuentas desactivadas**, de modo que dar de baja a la única persona con acceso no la reabre —que sería una escalada de privilegios disfrazada de tarea rutinaria de RRHH—. **Instrucción operativa, escrita en `docs/cliente/instalacion.md` §1.7:** completar el paso 1 del asistente **inmediatamente** después de que `install.sh` termine, y **no publicar el panel** —ni abrir su puerto al exterior, ni darle nombre DNS— hasta que ese paso esté hecho; si al abrirlo aparece un `409` que nadie ha provocado, hay que tratarlo como incidente y no como error de la instalación. Dueño: fabricante el control, cliente la ventana | **Implementado (5.5). Riesgo residual aceptado; se revisa en el cierre de Fase 5** |
| `APP_DEBUG=true` en `.env.example` | **CERRADO (01-09-2026, tarea 5.4)** | RS-08, OWASP A05 | Tres piezas, y las tres con prueba: (1) `.env.example` entrega `APP_DEBUG=false` y lo marca `[CLIENTE]` con el porqué; (2) `App\Support\Environment\ProductionSafetyGuard` **se niega a arrancar** con `APP_ENV=production` y `APP_DEBUG=true`, con mensaje bilingüe que dice qué hacer y aclara que no se ha perdido nada —la condición es estrecha a propósito: solo esa combinación, para que la guarda no pueda dejar sin fichar a un hotel por un motivo discutible—; (3) `install.sh` lo comprueba en su **fase 1**, antes de escribir nada, de modo que el camino normal es que el error no llegue a verse. Pruebas: `tests/Unit/Support/ProductionSafetyGuardTest.php` (5) y `QualityGatesTest` («entrega el .env de ejemplo con las trazas apagadas…»), más el escenario C de la etapa ⑧ de la CI, que instala con el `.env` tal cual sale del paquete | — |
| Etiqueta de imagen movible (`latest`) en producción | **CERRADO (01-09-2026, tarea 5.4)** | RS-08, doc 02 §11.6.1 | `compose.prod.yaml` declara `${IMAGE_TAG:?…}` **sin valor por defecto** en las tres imágenes del producto y `.env.example` la entrega vacía; la fija `install.sh` desde el fichero `VERSION` del paquete. Sin ella, Compose se para antes de crear nada con un mensaje que dice qué poner. Prueba: `QualityGatesTest` («no deja ninguna imagen de produccion apuntando a una etiqueta movil»), verificada además a mano contra `docker compose config` | — |
| Los secretos de una instalación se generaban a mano, sin procedimiento | **CERRADO (01-09-2026, tarea 5.4)** | RS-08, §7.7 | `install.sh` genera los once secretos con `openssl` **en el servidor del cliente**, escribe el `.env` en `0600` y **no imprime ninguno**. El escenario C de la etapa ⑧ lo comprueba de la única forma honesta: extrae cada valor del `.env` recién escrito y falla si aparece en la salida del instalador. `DB_MAINTENANCE_PASSWORD` sigue **sin generarse**, por ADR-027, y su procedimiento manual está en `docs/cliente/operacion.md` §9 | — |
| Sin rehash de contraseñas ni PIN al iniciar sesión | **Aceptado temporalmente**: el coste (bcrypt 12) es fijo y verificado; sin rehash, subir el coste no migraría los hashes existentes | OWASP A07 | `needsRehash` en `AuthenticateUserHandler` y en el portal | 2.1 |
| Sin DAST | **Aceptado**: la superficie es una API con contrato y tres SPA del mismo origen; el SAST y las pruebas de autorización cubren la mayor parte | RS-10 | ZAP *baseline* contra el entorno de Compose, sin tarea asignada | 3.8 |
| Sin revisión de seguridad externa | **Pendiente**: ninguna versión comercial se publica sin ella | RS-11 | Tarea 3.8, con este documento y §8.1 como entrada del revisor | Primera versión comercial |
| Sin requisito para OWASP A09 (registro y monitorización de fallos de seguridad) | **Resuelto (29-08-2026)**: **RS-13** en doc 01 §8 y Anexo A (Fase 1) con el reparto real que fija **ADR-039** —acceso y cierre de gestión y apertura de bloqueo en `audit_log`; cada fallo en el log técnico y en `kronoqr_auth_attempts_total`—, no con la redacción inicial de «asiento en los tres canales». La sesión del empleado (portal y PIN) queda fuera **por decisión, no por limitación**: ADR-037 excluye de la traza el ejercicio del derecho del art. 34.9 ET, y el fichaje que sigue al PIN ya deja constancia. El tipo de actor `employee` se creará con la primera acción de autoría del empleado con relevancia legal, no para sesiones | A09; `T1110.001` | Pruebas etiquetadas `RS-13`: `AuthenticationTrailTest`, `PortalAuthenticationTrailTest`, `PinScanAuthenticationTrailTest`, `AuthenticationMetricsTest`, `BackupAndAlertingTest` | Hecho |
| SRI en assets prometido en doc 02 §7.1 y no implementado | **Decisión pendiente**: con CSP `script-src 'self'` y assets del mismo origen el valor es bajo; implementar o retirar la promesa | RS-09 | Doc 02 lo edita otro agente en esta rama: anotarlo allí, no aquí | Cierre de esta rama |
| `rotacion-secretos.md` prometido (doc 02 §7.7 y §12) y no escrito | **Resuelto (30-08-2026, tarea 2.12)**: se escribió al implementar la rotación de la clave del QR, que es la única de las cinco que dura semanas | RS-08 | [`docs/runbooks/rotacion-secretos.md`](runbooks/rotacion-secretos.md) cubre `APP_KEY`, claves HMAC del QR —con su propio runbook, [`rotacion-clave-qr.md`](runbooks/rotacion-clave-qr.md)—, los tres roles de base de datos, los tokens de dispositivo y la clave de copia. La vía operativa de revocar el token de una tablet queda marcada como pendiente de la 5.6 | Ampliación en 5.4 |
| **El solape de rotación multiplica la ventana «impresa y sin entregar»**: durante una rotación puede haber hasta N tarjetas **escaneables** —una por empleado— en una bandeja de RRHH durante semanas, en lugar de las pocas de una incorporación normal | **Aceptado, con control compensatorio** (30-08-2026, tarea 2.12). Es consecuencia directa de la decisión que evita el daño mayor: si la tarjeta se acuñara al entregar, no habría reimpresión progresiva y rotar obligaría a dejar sin fichar a quien no hubiera pasado por RRHH ese día. Una tarjeta impresa **ficha en nombre de su titular desde que sale de la impresora** (ADR-034), así que el riesgo es real y no teórico: quien se lleve una de la bandeja puede fichar por esa persona | RS-01, RF-QR-04, RF-QR-06 | **Controles, ya implementados:** (1) `credentials:print-batch --pending` no obliga a imprimir la plantilla entera, y el runbook recomienda **imprimir solo la tanda que se entrega ese mismo día** (§4.3 de `rotacion-clave-qr.md`); (2) la entrega deja `delivered_at` y responsable, así que consta si una tarjeta se perdió **antes** de darla o después (doc 02 §5.5); (3) `credentials_pending_print{site}` mide la bandeja y sube de golpe al rotar, así que la ventana es visible; (4) el uso indebido se detecta como préstamo de tarjeta (RF-PR-06, RN-16), sin distinguirse del *buddy punching* ya aceptado. **Lo que no hay** y no se pretende: custodia física del papel, que es del cliente y va en la documentación de operación | Revisión en 3.11, con la detección de patrones |
| **Nombres de la plantilla saliendo por SMTP a diario**: el resumen de incidencias de RF-PR-01 se envía cada noche al responsable de cada departamento con fecha, **nombre** y tipo de cada hallazgo. Es el único camino por el que datos personales salen del servidor del cliente sin que nadie pulse nada, y el relevo SMTP suele ser de un tercero | **Aceptado, con control compensatorio** (30-08-2026, cierre de Fase 2). El aviso es lo que hace útil la detección: una incidencia que nadie ve no la corrige nadie, y un identificador opaco en el correo obligaría al responsable a abrir el panel para saber de quién se le está hablando —lo que acabaría con el resumen sin leerse—. La alternativa de no enviar nada deja RF-PR-01 sin la mitad que dice «se notifica al responsable» | RS-05, RL-15, RF-PR-01 | **Controles, ya implementados:** (1) el correo lleva lo mínimo —fecha, nombre y tipo—, nunca el registro horario ni datos de contrato, y se corta a 20 líneas detalladas; (2) **deja asiento** `personal_data.accessed` con `dataset = incident_digest` y los `employee_uuid` afectados, que es la excepción deliberada de `PersonalDataAccessLog`: sin ella no se puede responder a RL-15 «de quién se fueron los datos»; (3) el sello `notified_at` se escribe **después** de entregar, así que un correo no enviado no se da por avisado; (4) `MAIL_SCHEME` documentado en `.env.example` con la advertencia de que vacío o `smtp` es STARTTLS oportunista y **`smtps` es lo que hay que poner en producción**. **Lo que es del cliente y no del producto:** contratar el **encargado del tratamiento** del art. 28 RGPD si el correo lo entrega un tercero, escrito en `docs/cliente/obligaciones-legales.md` §2. Dueño: fabricante para el canal, cliente para el encargo | **Revisado el 08-09-2026 (tarea 5.11): sigue aceptado, y el control que era del cliente ya está escrito donde lo lee.** `endurecimiento.md` §7 dedica un apartado entero al canal: dice sin rodeos que por ahí salen nombres a diario, exige `MAIL_SCHEME=smtps` en producción explicando que vacío o `smtp` es STARTTLS **oportunista** y que con `smtps` el envío **falla en vez de degradarse** —y que un envío fallido no pierde la incidencia, que sigue abierta y entra en el resumen de la noche siguiente—, remite al art. 28 de `obligaciones-legales.md` §2 cuando el relevo es de un tercero, y da la comprobación (`product:doctor`, sondas `mail.transport` y `mail.reachable`) **advirtiendo de que no comprueba el cifrado**, que es justo el matiz que se pierde al resumir. La fila 15 de la lista trimestral lo repasa. Sin cambio en el producto: el riesgo residual es el mismo, y ahora está en manos de quien puede cerrarlo |
| **El token de dispositivo vive en `localStorage` de la tablet, en claro** | **Aceptado por diseño** (07-09-2026, tarea 5.6). Se entrega **una sola vez**, en `POST /api/v1/kiosk/pair/claim`, y la PWA tiene que poder firmar `/scan` después de un reinicio **sin que nadie intervenga**: una tablet colgada de una pared se reinicia sola tras un corte de luz o una actualización del sistema, y guardarlo solo en memoria significaría re-emparejarla —con una persona delante y un código nuevo— cada vez. Eso es exactamente lo que la regla dura 19 prohíbe: el quiosco no puede bloquear al empleado, y menos a las 06:00 | RS-04, RS-08 | **Controles, todos implementados:** (1) el token lleva **los tres ámbitos mínimos** del §7.3 —`scan:write`, `roster:read`, `heartbeat:write`— y ninguno más, así que quien lo extraiga no alcanza la plantilla ni la gestión (RS-04); (2) `POST /api/v1/devices/{uuid}/unpair` lo invalida **en la petición siguiente**, no en 90 días, y la PWA purga entonces el padrón cacheado y vuelve a `/pair` **sin tocar la cola offline**, porque ahí hay jornadas de personas; (3) el `pairing_secret` **no se persiste** una vez recogido el token, así que el emparejamiento no deja una segunda credencial olvidada; (4) CSP sin `unsafe-inline` y assets del mismo origen, de modo que no hay camino de script inyectado que lo lea; (5) el aparato va en **modo quiosco / MDM** con la aplicación fijada, que es lo que impide abrir una consola o un navegador cualquiera en él — es responsabilidad del IT del cliente y está escrito en `docs/runbooks/alta-nuevo-quiosco.md`. **Lo que no hay y no se pretende:** cifrado del token en el cliente, que solo movería la llave un metro dentro del mismo dispositivo, ni almacenamiento en `httpOnly` —la PWA necesita leerlo para firmar cada petición—. **El riesgo real es físico**: quien se lleve la tablet se lleva el token, y la respuesta es `unpair`. Dueño: fabricante los controles del producto, cliente el modo quiosco y la custodia física | **Revisado el 08-09-2026 (tarea 5.11): sigue aceptado por diseño, sin cambio.** `endurecimiento.md` §6 se lo cuenta al cliente con la misma franqueza que esta fila —«el riesgo real de un quiosco es físico»—, explica por qué el token tiene que sobrevivir a un reinicio (regla dura 19), enumera los tres controles del producto y los dos que son suyos (modo quiosco/MDM y custodia física, con el runbook de alta), y fija el orden de la respuesta a un robo: **desvincular primero** —que es lo que invalida el token en la petición siguiente— e incidencia de seguridad después (`brecha-de-seguridad.md`). Filas 19 y 20 de la lista trimestral. No queda nada que decidir |
| **La ventana de mantenimiento deja al panel y al portal fuera de servicio, y el `503` lo dice sin autenticar** | **Aceptado por diseño** (07-09-2026, tarea 5.7). Actualizar exige una ventana en la que nada escriba, y el `503` con `Retry-After` es lo que permite al quiosco distinguir «no se decidió» de «rechazado» y conservar el fichaje en su cola (regla dura 19). El detalle —«La instalación se está actualizando»— confirma a quien alcance el borde que hay una actualización en curso, y `GET /api/v1/health`, excluida del mantenimiento, sigue publicando la versión | RF-PD-10, RS-04 | **Controles, implementados:** (1) el fichaje **no se interrumpe**: los quioscos confirman en local y encolan, y cada fichaje conserva su `occurred_at`; (2) las dos sondas quedan fuera del `503` a propósito, para que el orquestador no reinicie un contenedor sano ni suene una alerta de «aplicación caída» por una operación planificada; (3) el cuerpo **no dice** de qué versión a cuál, ni cuánto falta, ni quién es el cliente: eso vive en el informe del servidor; (4) alcanzar el borde desde fuera de la red del cliente es una decisión de Nginx, no de la aplicación. **Lo que revela** a quien esté dentro de la red: que esa instalación se está actualizando ahora. Es información operativa, no un dato personal ni una credencial. Dueño: fabricante el control, cliente la exposición de la red | **Revisado el 08-09-2026 (tarea 5.11): sigue aceptado por diseño.** `endurecimiento.md` §10 reúne en una sola tabla las **tres** cosas que el producto sirve sin autenticar —estado de licencia y versión, marca, y este `503` de mantenimiento—, con qué revela cada una y por qué es aceptable, y remata con lo que de verdad las acota: quien no debe alcanzar el borde no debería alcanzarlo (§1). Es la lectura correcta del riesgo, que siempre fue de exposición de red y no de la aplicación, y por eso ese apartado va **después** del de red y no antes. Añade además el dato que le importa al hotel: durante la ventana **el fichaje no se interrumpe** |
| **El paquete descomprimido ENCIMA de la instalación anterior deja la vuelta atrás con el compose nuevo y las imágenes antiguas** | **Aceptado, avisado y no bloqueado** (07-09-2026, tarea 5.7). Negarse a actualizar en ese caso dejaría sin correcciones de seguridad a quien ya descomprimió mal el paquete, que es peor que el riesgo que evita | RF-PD-10 | **Controles, implementados:** (1) `check_installation` lo detecta comparando rutas reales y emite un `[AVISO]` que dice qué hacer la próxima vez; (2) el `.env` anterior se conserva como `.env.kronoqr-pre-update` en `0600` solo mientras dura la actualización (se retira al terminar bien), de modo que la vuelta atrás relanza con el `IMAGE_TAG` antiguo; (3) las imágenes de la versión anterior siguen en el disco: el actualizador no borra ninguna. **Lo que no se puede garantizar en ese modo:** que el compose nuevo describa correctamente los servicios de la versión antigua si la nueva añadió o renombró alguno. Dueño: cliente el modo de descompresión, fabricante el aviso | Revisión en 5.11 |
| **El informe de actualización se guarda en el servidor del cliente y el detalle técnico puede contener datos personales** | **Aceptado, con separación de ficheros** (07-09-2026, tarea 5.7). El informe es la única evidencia de qué pasó en una actualización: el fabricante no accede al servidor (ADR-016), así que si no queda ahí no queda en ninguna parte. Pero la salida cruda de una migración fallida puede traer el `DETAIL: Failing row contains (...)` de PostgreSQL, y los últimos renglones del contenedor pueden traer el mensaje de una `QueryException` con sus parámetros interpolados | RS-05, RF-PD-09, ADR-020, regla dura 21 | **Controles, implementados:** (1) el **informe** —resumen, puntos de control, comprobaciones, códigos— es `0640` del uid de la aplicación y se puede adjuntar al paquete de diagnóstico; (2) el **detalle técnico** va a `update-<marca>.detalle.log`, `0600` **de root**, que el armador del paquete de diagnóstico no puede leer por construcción y no por disciplina; (3) el informe nombra la ruta del detalle y advierte por escrito de que puede llevar datos personales y de que hay que revisarlo antes de enviarlo; (4) `license:show` no se vuelca a ningún fichero (lleva el nombre del cliente); (5) ninguno de los dos ficheros lleva secretos, y la etapa ⑧b lo comprueba valor a valor en los dos caminos, el de éxito y el de vuelta atrás. Dueño: fabricante los controles, cliente el envío | **Revisado en 5.9 (08-09-2026)**: el paquete de diagnóstico incluye solo el informe `update-<marca>.log` más reciente y la lista de informes, nunca el `.detalle.log`; el contrato (`DiagnosticsBundle.updates`) y la prueba de integración con volumen lo afirman |
| **La actualización y la vuelta atrás automática no dejan asiento en `audit_log`** | **Pendiente** (07-09-2026, tarea 5.7). El único rastro de que una instalación cambió de versión —o de que su base entera se sustituyó por una copia— es el informe en `BACKUP_PATH/reports/`, que no está encadenado por hash ni forma parte de ninguna exportación. Tras una vuelta atrás, la cadena de auditoría es continua y coherente **porque es la de la copia restaurada**: el intervalo descartado es invisible desde el propio registro | RL-04, RS-07, regla dura 6 | Propuesta: dos asientos escritos desde el actualizador vía `artisan`, `system.updated` tras el paso 5 (`from`, `to`, cadena, migraciones, huella de la copia) y `system.restored_from_backup` **sobre la cadena ya restaurada** (fichero, huella, fecha de la copia, paso y motivo), que es lo que hace explícita la discontinuidad. Sin PII ni secretos. Es un cambio del dominio de Compliance (catálogo `AuditAction`, caso de uso) y se asigna a `arquitecto-dominio` | Antes del cierre de la Fase 5 |
| **La marca de la instalación se sirve sin autenticar** | **Aceptado por diseño** (07-09-2026, tarea 5.8). El quiosco y el portal la necesitan antes de identificar a nadie, y lo que revela —el nombre del hotel, un color y un logotipo— es lo mismo que lleva impreso cada tarjeta de credencial y el rótulo de la recepción | RF-PD-08, RS-04 | **Controles, implementados:** (1) proyección cerrada a las cinco claves de presentación (`additionalProperties: false` en el contrato; `BrandingEndpointTest` comprueba que no viaja ninguna otra clave); (2) limitador por IP propio (`PRODUCT_BRANDING_RATE_LIMIT`); (3) el logotipo solo sale del directorio de marca del contenedor y solo si pasa la inspección por contenido, con `nosniff` y, para SVG, CSP `default-src 'none'; sandbox`; (4) la escritura sigue exigiendo `admin` con `settings:*` y `SettingsPolicy`, probada en negativo con tokens de quiosco y de portal. Exponer las dos rutas fuera de la red del cliente sigue siendo decisión de Nginx. Dueño: fabricante los controles, cliente la exposición | **Revisado el 08-09-2026 (tarea 5.11): sigue aceptado por diseño.** Cubierto en la misma tabla de `endurecimiento.md` §10 —con el argumento de esta fila: lo que revela es lo que lleva impreso cada tarjeta y el rótulo de la recepción— y en la fila `/api/v1/branding` del §1.1, que marca la ruta como «sin autenticar» y deja la exposición donde estaba: en el cortafuegos del cliente |
| **El filtro del SVG del logotipo es una lista negra corta y la seguridad depende del contexto de consumo** | **Aceptado, con controles** (08-09-2026, tarea 5.8). `LogoFileInspector` rechaza `<script`, manejadores `on*=`, `<foreignObject` y `<!ENTITY`, pero un SVG sigue siendo un documento: lo que lo hace inocuo es **cómo se consume**, no el filtro | RS-04, ADR-016 | **Controles, implementados:** (1) en las tres SPA va por `<img src>`, que no ejecuta guiones ni resuelve referencias; (2) en los PDF va como `<img src="data:…">` en un Chromium **sin red** (ADR-016); (3) abierto en una pestaña manda la CSP del controlador (`default-src 'none'; sandbox`) más `nosniff`; (4) solo entra por `PATCH /settings` de un `admin` y desde el directorio de marca del propio cliente. **Lo que no hay:** una comprobación automática que impida que un cambio futuro incruste esos bytes en línea (`v-html`, `{!! !!}`), ni tope de complejidad para un SVG (512 KiB de trazados se rasterizan en cada hoja de tarjetas; el actor sería el IT del cliente sobre su propio servidor). Dueño: fabricante | Al primer cambio que toque cómo se pinta el logotipo; candidata a regla de ESLint/Pest Arch en la 5.11 |
| Préstamo de tarjeta (*buddy punching*) | **Aceptado por diseño** (`docs/04`, ADR-014): fraude autolimitado sin técnica ATT&CK; se detecta, no se impide | RF-PR-06 | Detección de patrones anómalos | 3.11 |
| **Agrupación por ventana de los asientos de divulgación de la presencia en vivo**: `GET /api/v1/attendance/live` se sondea cada 15 s (RNF-D-03), y su asiento `personal_data.accessed` se escribe una vez por cuenta y ventana de 15 min en lugar de una por lectura | **Aceptado, con control compensatorio** (29-08-2026, tarea 2.4). Un asiento por sondeo no responde mejor a RL-15 —dice lo mismo veinte mil veces al día— y mete esas escrituras bajo el `pg_advisory_xact_lock` **global** de ADR-010, el mismo por el que pasa cada fichaje: una funcionalidad accesoria (ADR-023) en el camino crítico del cambio de turno viola las reglas duras 15 y 19. Es la palanca que **ADR-037 nombra por escrito** para este puerto | RS-05, RL-15 | **Control compensatorio, ya implementado:** el primer asiento de cada ventana se escribe **siempre**, y las repeticiones se cuentan en `repeated_since_last_entry` del asiento siguiente, así que consta *que* esa cuenta tuvo la presencia delante y *cuántas veces* la refrescó. La agrupación vive detrás del puerto (`GroupedPersonalDataAccessLog`) y es **lista cerrada**: solo `live_presence`, declarada en `config/compliance.php`; el directorio de plantilla, el padrón del quiosco y la exportación legal siguen dejando un asiento por lectura. `COMPLIANCE_DISCLOSURE_WINDOW_SECONDS=0` la desactiva para un cliente que prefiera la fila a la contención. **Consecuencia asumida:** si el panel se cierra a mitad de ventana, la cola de ese contador no llega a ningún asiento. **Revisado en el cierre de Fase 2 (30-08-2026): se prorroga, con evidencia.** La lista sigue cerrada a `live_presence` y solo a él —`config/compliance.php`, `disclosure_grouping.datasets`—, pese a que la Fase 2 ha añadido cinco conjuntos nuevos (`incident_board`, `incident_digest`, `period_report`, `employee_workdays`, `credential_status`) y **ninguno ha entrado**: cada uno deja su asiento por lectura. `repeated_since_last_entry` está implementado y probado, así que el hecho no se pierde. Los 15 min siguen siendo el grano correcto para la pregunta que RL-15 hace, y el runbook de brecha lo advierte por escrito para que nadie confunda filas con lecturas ([`docs/runbooks/brecha-de-seguridad.md`](runbooks/brecha-de-seguridad.md) §2 y §4.1). Dueño: fabricante | **Prórroga: cierre de Fase 5** |
| **`allowed_origins: *` en el servidor de Reverb** | **Aceptado por diseño** (29-08-2026, tarea 2.4): el dominio del panel lo pone cada cliente y ni el fabricante ni el repositorio lo conocen (ADR-017, regla dura 13). La defensa no está en el origen | RS-04, ADR-011 | **Todos los canales son privados** y su suscripción la firma el servidor en `POST /api/v1/broadcasting/auth` con el ámbito del token y el alcance por departamento (regla dura 18): un origen cualquiera puede abrir el socket y no recibe absolutamente nada. Además, `accept_client_events_from: none` —y no el `members` de serie—, así que un panel suscrito **no puede publicar** en el canal e inventarse que alguien ha fichado. `REVERB_ALLOWED_ORIGINS` permite cerrarlo por instalación sin tocar el repositorio | Hecho |
| **Degradación por licencia de la presencia en vivo, sin punto de decisión real** | **Resuelto (01-09-2026, tarea 5.3)**: el puerto existe (`Shared\Application\Port\FeatureGate`) y la comprobación entró donde de verdad se decide. La versión inicial de esta tarea la puso **solo en la respuesta** —`meta.realtime.enabled: false`—, lo que era **cooperativo**: un cliente escrito a mano que ignorase ese campo conservaba el tiempo real íntegro, porque el canal seguía firmando la suscripción | RF-PD-05, ADR-019, ADR-023 | **Dos mitades, las dos implementadas:** (1) los dos `Broadcast::channel` de `routes/channels.php` consultan el `FeatureGate` **antes que nada** y no firman la suscripción sin licencia que conceda el tiempo real; (2) la respuesta deja de servir `key` y `channels` cuando no se concede, para no entregar lo que hace falta para conectarse justo después de decir que no hay tiempo real. `path` y `auth_endpoint` sí se sirven: son rutas estáticas del propio origen y no dan acceso a nada. Probado con Reverb **configurado de verdad** —sin eso, la suite pasaba con la comprobación borrada, porque `BROADCAST_CONNECTION=null` ya apagaba el tiempo real— y con la comprobación retirada a mano para medir que la prueba cae. **Sigue siendo degradación y no apagado:** el listado se sirve entero y el panel sondea (ADR-011). **Lo que queda, dicho explícitamente:** (a) el servidor **sigue publicando** el evento de presencia en el canal aunque la licencia no lo conceda, porque esa emisión es la estela de un fichaje ya registrado y meterle una condición la acercaría al camino crítico (regla dura 19) — nadie puede suscribirse, así que no llega a ningún navegador, pero se hace trabajo inútil; (b) **una suscripción firmada antes de que la licencia caduque sobrevive** hasta que ese socket se reconecte, porque el protocolo autoriza al suscribirse y no en cada mensaje. Ninguna de las dos es una fuga: el dato que viajaría es el que esa misma cuenta puede pedir por sondeo, que no se degrada | Hecho |
| **`GET /api/v1/health` publica el estado de la licencia sin autenticación** | **Aceptado por diseño** (01-09-2026, tarea 5.3). El §10.5 pide que `doctor` y el paquete de diagnóstico puedan informarlo, y la sonda es el único sitio que no exige sesión ni base de datos. Lo que se publica es **una palabra** —`valid`, `expired`, `absent`, `unverifiable`, `not_yet_valid`, `expiring_soon` o `unknown`— junto a la versión, que ya se publicaba | RF-PD-04, RF-PD-09, ADR-020 | **Controles:** (1) ni el nombre del cliente, ni el plan, ni los límites, ni las fechas salen por ahí: eso es información comercial del cliente y vive en `GET /api/v1/license`, que es de `admin`; (2) exponer la sonda fuera de la red del cliente es una decisión de Nginx, no de la aplicación, y así está escrito en el contrato desde la tarea 1.7; (3) el estado **no cambia el código de respuesta**: una licencia caducada devuelve `200`, porque lo contrario haría que el orquestador retirara del balanceo un contenedor que ficha perfectamente. **Lo que revela** a quien alcance la sonda: que esa instalación opera con la licencia vencida. Es un hecho comercial del cliente, no un dato personal ni una credencial, y el §8.1 ya enmarca la licencia como control comercial | Hecho |
| **La evidencia comercial del exceso de plan se purga con el registro** | **Aceptado, con control compensatorio** (01-09-2026, tarea 5.3). Los asientos `license.plan_exceeded` viven en `audit_log`, que se purga con `retention_years` del perfil de cumplimiento (RL-02, ADR-027). Pasados esos años —cuatro de serie— el cliente y el fabricante dejan de poder demostrar desde cuándo se operó por encima del plan | RF-PD-04, RL-02, ADR-028 | **Es la decisión correcta y no un descuido:** darle a un apunte comercial una retención propia y más larga que la del registro horario obligaría a conservar datos del cliente más allá del plazo que él fijó, por conveniencia del fabricante. **Control compensatorio:** el fabricante conserva `license_id`, cliente, plan y fechas en su **propio registro de emisión**, fuera de la instalación, y el asiento lleva ese `license_id` para poder cruzarlos. Cuatro años es más que cualquier ciclo comercial razonable. **Decisión de las asesorías laboral y jurídica** si alguna vez hiciera falta más | Revisión con las asesorías |
| **Manipular la fila `license` o la clave pública del producto** | **Aceptado por diseño** (01-09-2026, tarea 5.3), y el doc 01 §8.1 lo enmarca: la alteración de la clave de licencia *«es un control comercial, no de seguridad de datos: no debe protegerse a costa de bloquear el registro legal»*. Quien controla el servidor puede editar la fila o recompilar con otra clave pública | RF-PD-04, ADR-018 | **Lo que sí ocurre:** editar la fila hace que la firma deje de cuadrar y el estado pase a `unverifiable` —degradado, nunca detenido—; la activación deja asiento en `audit_log`, que es solo-apéndice y encadenado. **Lo que no se hace y no se hará:** ofuscación, comprobaciones antimanipulación ni verificación en línea. ADR-018 las descarta una por una: coste real, falsa sensación de seguridad, y el riesgo de que una comprobación mal hecha bloquee el fichaje es mucho mayor que el de que alguien altere su plan. La palanca es el contrato | Hecho |
| **Amplificación de asientos de exceso de plan en un alta masiva** | **Pendiente** (01-09-2026, tarea 5.3). Cada alta por encima del plan escribe un asiento en `audit_log`, bajo el candado global de ADR-010. La importación masiva de plantilla (RF-GP-05, tarea 5.5) da de alta cientos de personas de una vez: un hotel que importe 300 con un plan de 80 escribiría 220 asientos casi idénticos, y esas escrituras comparten candado con el camino de fichaje | RF-PD-04, RF-GP-05, ADR-010, ADR-028 | **Hoy no es explotable ni frecuente:** el alta unitaria del panel es el único camino, va en `afterCommit` y bajo `try`, y un exceso real son unidades al mes. **Lo que tiene que resolver la 5.5**, y está anotado en `HANDOFF.md`: agrupar el exceso de una importación en **un solo asiento** con el recuento, en vez de uno por fila. Hay una segunda razón para hacerlo: `first_crossing` se deduce con `excess() === 1`, así que en altas masivas o concurrentes **no es fiable** —varias pueden creerse el cruce, o ninguna—, y el primer asiento es justo el que da la fecha desde la que se opera fuera de contrato | 5.5 |
| **El asiento `license.activated` lleva el nombre del cliente** | **CERRADO (08-09-2026, tarea 5.9)**. Planteado el 01-09-2026 (tarea 5.3): El asiento guarda `customer_name` porque es lo que responde «¿qué se contrató y desde cuándo?», y el trail es de la instalación. Pero el **paquete de diagnóstico** (RF-PD-09, tarea 5.9) va anonimizado por defecto (ADR-020) y podría llevárselo si incluyera asientos de esta familia | RF-PD-09, ADR-020 | **Resuelto excluyendo, no redactando:** el paquete lleva de `audit_log` **solo recuentos** por familia y por día y el resultado de verificar la cadena —ningún payload de ninguna familia (`DiagnosticsBundle.audit`)—, y su sección `license` se construye por lista de permitidos sin `customer_name`. La prueba de integración con volumen busca la razón social en el paquete completo y falla si aparece. El resto del payload —plan, límites, fechas, huella— no identifica a nadie y es justamente lo que sirve para diagnosticar. El log técnico ya no lo lleva: `LicenseTelemetry` publica el estado y nada más, a propósito | Hecho |
| **El alcance `configuration` de un acceso de soporte cambia los umbrales operativos del fichaje** (`ATTENDANCE_*`: anti-rebote, tránsito mínimo, desfase de reloj admitido) y puede emparejar o desvincular quioscos | **Aceptado, con control compensatorio** (08-09-2026, tarea 5.9). Es la potestad que el alcance existe para conceder: la mayoría de las incidencias que exigen intervención son de configuración. Lo que **no** alcanza, y lo cierra la policy aunque el ámbito lo permita: el perfil de cumplimiento (umbrales legales y años de conservación, RL-01/RL-02, regla dura 14), la licencia, el asistente de puesta en marcha, las credenciales, las correcciones y los datos personales del paquete | RF-PD-11, RL-18, RN-15, regla dura 14 | **Controles:** (1) cada cambio deja `calculation_setting.changed` con el valor anterior y `actor_type = support_grant`, visible para el cliente; (2) la concesión lleva motivo, caduca sola y se revoca en el acto; (3) `SupportScopeRoutesTest` recorre las rutas registradas y falla si una escritura nueva queda al alcance de un ámbito de lectura o fuera de la lista cerrada; (4) desvincular un quiosco conserva su cola offline (regla dura 19). Dueño: cliente la concesión, fabricante los controles | **Revisada el 09-09-2026 (tarea 5.12): sigue aceptada y el alcance no se ha ampliado.** El histórico de errores viaja bajo `diagnostics:*`, así que los tres alcances de soporte LEEN la pantalla; la escritura -dar un fallo por resuelto- la cierra `ErrorEventPolicy::resolve()` para todo actor de soporte, con prueba por cada uno de los tres alcances y con `SupportScopeRoutesTest` declarando la ruta. Ninguna de las dos rutas nuevas toca un umbral de fichaje ni un dato de jornada. Próxima revisión: cierre de la Fase 5 |
| **El paquete anonimizado lleva rutas del servidor del cliente** (`BACKUP_PATH`, `BRANDING_LOGO_ROOT`, directorio de informes) que pueden nombrar a la organización | **Aceptado por diseño** (08-09-2026, tarea 5.9). Identificar a la organización no es identificar a una persona, y ADR-020 no lo prohíbe: el cliente que envía el paquete ya se ha identificado al abrir la incidencia. Las rutas son lo que `doctor` necesita para decir dónde falta espacio o permiso | RF-PD-09, RL-19 | **Controles:** ninguna ruta lleva nombre de persona por construcción del producto (las carpetas las fija el IT en el `.env`); `operacion.md` §12.2 pide inspeccionar el paquete antes de enviarlo. Sin acción | Hecho |
| **El informe de actualización viaja dentro del paquete de diagnóstico** (`updates.latest.content`) | **Cerrado con lista de permitidos** (08-09-2026, tarea 5.9). El recolector solo deja pasar las líneas del formato que escribe `update.sh` —cabeceras, versiones, puntos de control, comprobaciones y códigos— y sustituye cualquier otra por un recuento `omitted_lines`; una prueba siembra un `DETAIL: Failing row contains (...)` y un correo en el informe y afirma que no salen. El `.detalle.log` queda fuera por nombre y por permisos | RF-PD-09, RL-19, regla dura 21 | Hecho; enlaza con el riesgo del informe de la 5.7 de arriba | Hecho |
| **El paquete con datos personales generado por consola queda en el disco del cliente** (`storage/app/diagnostics`, `0600`) | **Cerrado con purga automática** (08-09-2026, tarea 5.9). La retención de un fichero con datos personales no puede depender de que alguien se acuerde de borrarlo: `product:diagnostics` purga al arrancar los paquetes de más de `PRODUCT_DIAGNOSTICS_RETENTION_DAYS` (7) y lo dice; el endpoint HTTP no escribe nada en disco | RL-19, RGPD art. 5.1.e | Hecho; `operacion.md` §12.2 sigue pidiendo borrarlo al enviarlo | Hecho |
| **La exportación íntegra deja en el disco del cliente un ZIP con todos los datos personales de la plantilla y el registro completo** (`storage/app/exports`) | **Aceptado, con purga automática y auditoría** (08-09-2026, tarea 5.10). Es lo que RL-20 pide: el cliente tiene que poder llevárselo por sí mismo, y un fichero en disco es la única forma de que el panel lo entregue sin cargar cuatro años de registro en memoria | RF-PD-14, RL-20, RS-05 | **Controles, implementados:** (1) directorio `0700` y fichero `0600` dentro del contenedor, fuera de `BACKUP_PATH`; (2) caducidad `PRODUCT_DATA_EXPORT_RETENTION_DAYS` (7) y purga horaria que borra el fichero y conserva la fila; (3) `data_export.requested`, `generated` y `downloaded` en `audit_log`, con quién y cuándo; (4) solo `admin` y nunca un actor de soporte (`DataExportPolicy`); (5) una sola en curso por instalación (índice único parcial) | — |
| **La telemetría envía un identificador estable de la instalación al fabricante** (`installation_id`) | **Aceptado por diseño** (08-09-2026, tarea 5.10). Sin él, cien informes de una instalación y cien instalaciones serían indistinguibles y la telemetría no serviría para lo único que ADR-016 le concede: saber qué se usa y qué no | RF-PD-12, ADR-020 | Es un UUID v4 aleatorio acuñado en el servidor del cliente y guardado en `storage/app/telemetry/state.json`: no es `license_id`, no deriva de la razón social ni de nada del `.env`, y borrar el fichero estrena identidad. Viaja solo si el cliente reúne las tres condiciones (`TELEMETRY_ENABLED`, `TELEMETRY_ENDPOINT` y `telemetry` en la licencia); el resto del informe es un subconjunto por lista de permitidos del paquete anonimizado, atado por prueba a `configuracion.md` | — |
| **El panel descarga la exportación íntegra entera en memoria del navegador** (`fetch` + `Blob`, porque la sesión es un token *Bearer* y no una cookie) | **Aceptado, documentado** (08-09-2026, tarea 5.10). Un enlace de descarga sin autenticación, o con un token de un solo uso en la URL, sería una segunda puerta a todos los datos personales de la plantilla: peor que el límite práctico | RF-PD-14 | `operacion.md` §13 indica que por encima de ~1 GB se genera desde la consola y se saca con `docker compose cp`; la cabecera `X-Kronoqr-Export-Sha256` permite comprobar que el fichero llegó entero | — |
| **Una exportación íntegra interrumpida a mitad (contenedor parado, actualización, trabajador de cola muerto) podría bloquear nuevas exportaciones** por el índice único de «una sola en curso» | **CERRADO (08-09-2026, tarea 5.10, hallazgo de `seguridad-cumplimiento`)** | RF-PD-14, RL-20 | Tres controles: `GenerateDataExportJob::failed()` marca la fila `failed`; una fila `running` más antigua que el `timeout` del trabajo (o `pending` sin empezar pasado el mismo plazo) se declara `failed` con motivo `stale` al pedir la siguiente y en la purga horaria; `operacion.md` §13 tiene la entrada «se ha quedado en Generando». Prueba de *feature* que deja una fila atascada y comprueba que el `POST` siguiente responde `202` | — |
| **El destino de la telemetría lo elige el cliente y el producto no valida quién está al otro lado ni en qué país** | **Aceptado por diseño** (08-09-2026, tarea 5.10). La telemetría es opcional, viene apagada y no lleva datos personales ni de jornada, así que el producto no tiene que afirmar nada sobre transferencias internacionales de ese canal; imponer un destino del fabricante sería justo el canal permanente que ADR-020 descarta | RF-PD-12, ADR-020 | El producto exige `https` (un destino `http://` se rechaza como si no hubiera destino), verifica el certificado y no sigue redirecciones; `configuracion.md` §3 quinquies avisa de que la responsabilidad sobre el destino es del cliente y `product:telemetry` enseña lo que se enviaría | — |
| **El producto no tiene baja de cuentas de gestión**: `users.is_active` existe y se comprueba al autenticar (`EloquentUserAccounts`), pero **nada lo pone a `false`** —ni pantalla en el panel, ni endpoint, ni comando— y tampoco existe rotación de la contraseña de una cuenta ya creada. El catálogo entero es `identity:create-user` (alta) e `identity:2fa-reset` (retirar el segundo factor) | **Aceptado a la fuerza, con control compensatorio pobre** (08-09-2026, aflorado al escribir `endurecimiento.md` §8 y su lista del §11: la guía tenía que decir cómo se da de baja una cuenta, y no había respuesta). Un jefe de recepción que deja el hotel conserva una cuenta `rrhh` plenamente válida —su contraseña, su TOTP— hasta que alguien edite la fila a mano. Eso es acceso a datos de toda la plantilla y a las correcciones de jornada, que es exactamente donde se falsea un registro horario | RS-05, RS-06, RL-16, regla dura 18 | **Lo que hay hoy, y es poco:** (1) `identity:2fa-reset` **no cierra** un acceso, lo abre: sirve para recuperar una cuenta cuyo titular perdió el móvil, no para darla de baja; (2) la fila 17 de la lista de `endurecimiento.md` obliga a listar las cuentas **cada trimestre y en cada baja** y a contrastarlas con la lista de personal, y dice por escrito que en esta versión no hay forma de darlas de baja y que hay que abrir un caso con el fabricante —el cliente al menos no se entera tarde—; (3) todo lo que haga esa cuenta deja asiento en `audit_log` con su actor, así que el uso indebido es **detectable y no repudiable**, que no es lo mismo que impedido. **Lo que falta, y es producto y no documentación:** `is_active` editable por un `admin` con asiento `user.deactivated` (actor y motivo), su comando equivalente para cuando el panel no esté disponible, rotación de contraseña, y la pantalla de cuentas de gestión que hoy no existe. Con una cautela de orden: el alta pública del primer administrador cuenta **también** las cuentas desactivadas (riesgo de la 5.5, arriba), así que la baja no puede reabrir esa ventana. **Y una corrección pendiente en la propia fila 17**: manda consultar con el rol `fichaje_migrator`, que es `LOGIN SUPERUSER CREATEDB CREATEROLE REPLICATION` (`infra/docker/postgres/initdb/02-application-roles.sh`); enseñar al IT del cliente una sesión `psql` de superusuario como rutina trimestral contradice el cierre de la propia guía —«el registro de jornada no se toca por SQL»— y es la forma en que una consulta de lectura acaba siendo un `UPDATE` rápido. Dueño: fabricante | **Antes de la primera versión comercial.** Endpoint, comando y asientos, a `backend-laravel`; la pantalla, a `frontend-admin`; el rol de la fila 17, a `documentacion-tecnica` |
| **La clave de entorno del suelo de tiempo constante de RS-03 no era la documentada**: `backend/config/security.php` leía `SECURITY_REJECTION_FLOOR_MS` mientras `.env.example` documentaba `IDENTITY_CREDENTIAL_REJECTION_FLOOR_MS` | **CERRADO (08-09-2026, tarea 5.11)** | RS-03 | Los dos valores coincidían en 25 ms —el del código por defecto y el de la plantilla—, así que el comportamiento era correcto **por casualidad**: nada fallaba, y ninguna de las dos pruebas de RS-03 podía notarlo. Lo que estaba roto era la palanca. Un cliente que subiera el suelo en la variable documentada no cambiaba nada, y los dos caminos de rechazo que comparten ese suelo —resolución de credencial del fichaje y recogida del emparejamiento— seguían con el valor de serie sin que nadie se enterara; a la inversa, poner la clave vieja a `0` lo apagaba sin dejar rastro en ningún documento. Corregido: `config/security.php` lee `IDENTITY_CREDENTIAL_REJECTION_FLOOR_MS`, no queda ninguna referencia a la clave antigua en el árbol y `configuracion.md` §6.5 documenta la buena. **Lo que deja como deber**: la comprobación cruzada `.env.example` ↔ `configuracion.md` de `ClientDocumentationTest` no mira `config/*.php`, así que una clave leída por el código y ausente de la plantilla sigue siendo posible. Candidata a ampliarla | — |
| **El mensaje de una `QueryException` entra en `error_events` con los valores enlazados en claro**. Laravel compone ese mensaje como «mensaje de PDO + `(Connection: …, SQL: ` + el SQL con los parámetros **ya sustituidos**`)`» (`QueryException::formatMessage`), y `ServerErrorReporter` persiste `getMessage()` tal cual. Ni los valores de un `values (Maria, Gonzalez Perez, …)` ni el `DETAIL: Key (col)=(valor)` de PostgreSQL van entrecomillados, y lo entrecomillado es lo único que el saneado sustituye sin mirar dentro: un nombre de persona no tiene forma reconocible | **Cerrado el 09-09-2026, en la segunda vuelta de la 5.12, antes de integrar.** Abierto como bloqueante esa misma mañana por la revisión de `seguridad-cumplimiento`, que lo verificó ejecutando `ErrorMessageSanitizer::sanitize()` sobre tres mensajes reales: el nombre y los apellidos sobreviven en un `insert into "employees"`, el `bcrypt` de `pin_hash` sobrevive en el `update` de `EloquentEmployeePinRepository::issue()` —que no atrapa `QueryException`— y el valor del `DETAIL:` sobrevive entero. El repositorio **ya conocía este riesgo**: `DeferredAuditEntry` lo describe con estas mismas palabras y por eso registra la clase de la excepción y nunca su mensaje. Esta tabla hace justo lo contrario, y sale de la instalación dentro del paquete de diagnóstico | Regla dura 21, RL-19, RF-PD-15, ADR-020, RGPD art. 5.1.c y 5.1.f | **Corregido, no aceptado.** (1) `ServerErrorReporter` compone el mensaje de una `QueryException` con `SQLSTATE`, el texto del driver y `getSql()` —que conserva los `?`—, nunca `getMessage()`, y corta en origen el `DETAIL: Failing row contains (…)` con el que PostgreSQL vuelca la fila entera ante `NOT NULL`/`CHECK` (segunda fuga, descubierta al corregir la primera); (2) `ErrorMessageSanitizer` tiene tres reglas de red: cortar lo que siga a `, SQL: `, sustituir el valor de `DETAIL: Key (…)=(…)` y `Failing row contains (…)`; (3) `ErrorEventsHaveNoPersonalDataTest` provoca una `QueryException` real con el `DETAIL:` de PostgreSQL y `ErrorMessageSanitizerTest` usa los tres mensajes literales de la revisión. Los textos de `operacion.md` §15 y del runbook describen ahora el mecanismo en vez de prometer | Hecho |
| **El nivel `critical` lo decide el código del cliente sin mirar de qué origen viene**: `ErrorLevel::forClientCode()` compara contra la lista de códigos de quiosco sin consultar el `ErrorSource` que el servidor acaba de decidir por el token, y el `code` solo se valida contra un patrón, no contra el catálogo cerrado que el contrato describe en prosa | **Cerrado el 09-09-2026, en la segunda vuelta de la 5.12.** Hasta entonces una sesión de portal —código de empleado y PIN, la credencial más repartida del producto— podía enviar `kiosk.camera.unavailable` y escribir filas `critical` con `source: portal`, subir `application_errors_total{level="critical"}` y disparar `ErroresCriticosNuevos`, que es severidad alta y va al IT del cliente. `ClientErrorEndpointTest` fijaba ese comportamiento como correcto desde una sesión de gestión | RF-PD-15, RS-04, doc 01 §9.3 | **Corregido:** `ErrorLevel::forClientCode(ErrorSource, code)` solo concede `critical` cuando el origen es `kiosk`; el `code` se valida en el latido y en `/client-errors` contra `Shared\ClientErrorCode`, catálogo cerrado por origen que `ClientErrorCatalogTest` compara en los dos sentidos con los tipos de `errorReporter.ts` y `clientErrors.ts`. `ClientErrorEndpointTest` fija ahora lo contrario: portal y gestión con `kiosk.camera.unavailable` reciben `422` y nunca producen `critical` | Hecho |
| **Una sesión autenticada escribe filas nuevas, sin techo de grupos, en la misma base de datos por la que pasa cada fichaje.** El limitador acota las peticiones (12 por minuto y sesión, 48 por IP) pero no los **grupos**: la huella de un error de cliente es `origen + código + mensaje normalizado`, y el código es texto libre dentro de un patrón, así que cada envío de 50 puede crear 50 filas distintas —600 por minuto y actor, 2.400 por IP— que viven 90 días | **Cerrado el 09-09-2026, en la segunda vuelta de la 5.12** (se propuso para el cierre de fase y se hizo en la tarea). El comentario de `config/product.php` reconocía las 600 filas por minuto y las llamaba pequeñas; lo que nadie acotaba era el acumulado | RS-04, RF-PD-15, ADR-010 | **Corregido:** (1) el catálogo cerrado de códigos corta la entropía en su origen; (2) `PRODUCT_ERRORS_MAX_OPEN_GROUPS_PER_SOURCE` (500 de serie): por encima, la ocurrencia incrementa un grupo de desbordamiento por origen (`code = overflow`, huella fija) en vez de crear fila, con prueba de integración; (3) sonda `app.error_history` de `product:doctor` (aviso al 80 % del techo, fallo al techo), con textos en las dos lenguas y documentada en `configuracion.md` y `operacion.md` §15 | Hecho |
| **`employee_uuid` y `device_id` en un histórico técnico de 90 días que sale de la instalación dentro del paquete de diagnóstico** | **Aceptado por diseño** (09-09-2026, revisión de la 5.12). No es una decisión nueva de esta tarea: ADR-020 la nombra con todas las letras —«los empleados aparecen como `employee_uuid`»— y su apartado de verificación pide justamente eso, «solo `employee_uuid` y `device_id`». Es la forma seudonimizada que hace posible el modelo de soporte sin acceso, y sin ella un error que solo le pasa a una persona con su móvil sería indistinguible de uno que le pasa a todo el mundo | RL-19, ADR-020, regla dura 21 | **Controles, implementados:** (1) los dos los pone el **token**, nunca el cuerpo, y `ErrorContextAllowlist` los excluye del `context` a propósito para que no haya dos sitios donde puedan vivir; (2) no hay clave ajena hacia `employees` ni hacia `devices`, así que no existe el `JOIN` cómodo que sacaría un nombre; (3) el `resolved_by` —lo único del histórico que lleva un nombre— queda fuera del paquete por lista de permitidos, con prueba; (4) 90 días frente a los cuatro años del registro, y purga diaria. **Lo que sigue siendo del cliente**: revisar el paquete antes de enviarlo (`operacion.md` §12.2). Dueño: fabricante | Hecho |
| **Reabrir un grupo borra quién lo dio por resuelto y cuándo, y eso no queda en ninguna parte**: el `ON CONFLICT` pone `resolved_at` y `resolved_by_user_id` a `NULL` para que un fallo que reaparece vuelva a verse, que es lo correcto; pero la migración justifica no escribir asiento diciendo que «la traza de quién y cuándo vive en la propia fila», y es exactamente esa traza la que se pierde | **Aceptado; justificación corregida el 09-09-2026** (segunda vuelta de la 5.12). No es una cesión de la regla dura 6: resolver un error técnico no tiene relevancia legal y `error_events` no es `audit_log`. La migración y `ResolveErrorEvent` dicen ahora lo que de verdad ocurre: la reapertura borra `resolved_at` y `resolved_by_user_id` a propósito porque el contrato exige que un grupo abierto los lleve nulos, y la reapertura solo se produce con una ocurrencia posterior a `resolved_at` (un reloj de tablet atrasado no reabre nada) | RF-PD-15, regla dura 6 | Sin columna adicional: el histórico es técnico y de 90 días; quien quiera saber quién lo resolvió la vez anterior lo tiene en el log del panel. Dueño: `producto-licencia` | Hecho |

