# Evidencia automática — 2026-09-22

Commit revisado: `3bbf478b0b4aabe59a73fa88909d89127f67e19a` (`main` tras la PR #72). Herramientas y umbrales, los de
doc 02 §9.2 y del job `security` de `ci.yml`; se ejecutaron con los mismos objetivos del `Makefile` que usa la CI.
Sin secretos ni datos personales: aquí solo van recuentos y veredictos.

| Control | Objetivo `make` | Umbral | Resultado local (Windows, Docker Desktop) | Resultado en la CI (Linux) |
|---|---|---|---|---|
| Dependencias PHP | `deps-audit-php` (`composer audit`) | 0 avisos | 0 avisos («No security vulnerability advisories found») | Job «Dependencias y SAST» de la ejecución 35700735466: verde |
| Dependencias JS | `deps-audit-js` (`npm audit --audit-level=high`, workspace) | 0 críticas ni altas | 0 vulnerabilidades | Verde |
| SAST propio | `sast` (Semgrep 1.175.0, reglas de `.semgrep/`) | 0 hallazgos `ERROR` | 0 hallazgos de severidad alta | Verde |
| SAST comunitario | `sast-community` (Semgrep `p/php`, `p/owasp-top-ten`, `p/javascript`, `p/typescript`) | 0 hallazgos | 0 hallazgos (salida 0) | Verde |
| Secretos en el histórico | `secrets-scan` (gitleaks v8.30.1, `git` sobre todo el histórico) | 0 hallazgos | 264 commits, 54 MB, «no leaks found» | Verde |
| Ficheros del repositorio | `trivy-fs` (Trivy 0.74.0, `vuln,misconfig,secret`, HIGH/CRITICAL, `--ignore-unfixed`) | 0 hallazgos | 0 hallazgos (`backend/composer.lock`, `package-lock.json`, cuatro Dockerfiles). En Windows el recorrido del *bind mount* tarda ~14 min y el objetivo del `Makefile` cae por el tope de 10 min y por cachés que otros procesos reescriben (`backend/storage/framework/cache`, `.claude/`): se repitió con `--timeout 40m` y esos directorios excluidos; el veredicto que vale es el de la CI | Verde (mismo job) |
| Imágenes de entrega | `trivy-image` (`kronoqr/postgres:ci`, `kronoqr/app:ci`, `kronoqr/nginx:ci`) | 0 CVE HIGH/CRITICAL | No se ejecuta en local (las imágenes `:ci` las construye la CI) | Verde (mismo job; `nginx:ci` en la etapa ⑧) |
| SBOM | `sbom` (CycloneDX) | artefacto, no puerta | — | Artefacto `seguridad-informe-y-sbom` de la ejecución |

**Notas.**

- Ninguna herramienta del job `security` está en «modo informe» (`docs/runbooks/triaje-hallazgos-seguridad.md`): las seis
  son bloqueantes.
- Lo que no hay: DAST (doc 07 §6, fila «Sin DAST», reaceptada el 22-09-2026 con dueño y fecha) y prueba de intrusión
  (la hace el tercero, RS-11).
- La misma ejecución 35700735466 cayó en «⑧b Actualización desde la versión anterior» (escenario U3 de vuelta atrás,
  asiento `system.restored_from_backup`), que no es un control de seguridad del §9.2 sino de la actualización (RF-PD-10);
  su diagnóstico está en `HANDOFF.md` → «Trampas del entorno».
