# HANDOFF

## Sesión «tarea 5.4: instalador, Compose de producción, requisitos y secretos» (01-09-2026), en `feat/fase-5-productizacion`

**Tarea 5.4 implementada y verificada, sin commitear.** Cubre `RF-PD-02`, contribuye a `RQ-11` (etapa ⑧ de la CI) y a `RS-08`/§7.7.

**Construido.**
- `infra/scripts/install.sh` — cinco fases estrictas (requisitos sin escribir → instalación previa → secretos → arranque y esquema → verificación), vuelta atrás por pila de acciones, `--check-only`, `--lang es|en`, `--env-file`, `--compose-file`.
- `infra/scripts/lib/exit-codes.sh` — **tabla única** de códigos para los cinco scripts. La cargan `install.sh` y `backup-common.sh` (y por él `backup.sh`, `restore.sh`, `restore-drill.sh`).
- `infra/scripts/lib/env-file.sh` — **el unico lector de `.env`** del arbol, con la regla de comentario de Docker Compose. `infra/scripts/lib/fs.sh` — espacio en disco, antes tres invocaciones de `df`.
- `infra/scripts/check-package-links.sh` — comprueba que la documentacion entregada no enlaza fuera del paquete. Herramienta del repositorio, no entregable.
- `infra/scripts/lib/messages.sh` — catálogo es/en (**124 claves ×2**) con `kq_msg`/`kq_format`/`kq_msg_check_catalog`. Es el **único sitio del árbol** con `printf` de formato variable: la supresión de SC2059 está escrita una vez.
- `infra/compose.prod.yaml` — `${IMAGE_TAG:?…}` **sin valor por defecto** en las tres imágenes; observabilidad en perfil `observability` **encendido de serie** vía `COMPOSE_PROFILES` del `.env`; `nginx` depende de `app` por `service_healthy`.
- `infra/docker/nginx/Dockerfile` — etapa `frontends` que construye las tres SPA con `KRONOQR_BASE=/admin/|/kiosk/|/portal/` y las copia a `/var/www/spa`. `infra/docker/nginx/extra/spa.conf` sirve las tres; el portal reusa `$kronoqr_portal_allowed`.
- `backend/app/Support/Environment/{ProductionSafetyGuard,UnsafeProductionConfiguration}` + `AppServiceProvider::boot()`: se niega a arrancar con `APP_ENV=production` y `APP_DEBUG=true`, mensaje bilingüe.
- Etapa ⑧ `clean-install` en `.github/workflows/ci.yml` (19 pasos, cuatro escenarios A/B/C/D) + `make release-gate` en etiquetas `vX.Y.Z` + `IMAGES=nginx` en `build-ci-images`.

**Tabla de códigos, idéntica en los cinco scripts** (`lib/exit-codes.sh`, publicada en `docs/cliente/operacion.md` §8): `0` correcto · `1` uso incorrecto · `2` requisitos no cumplidos, **nada escrito** · `3` estado previo incompatible, **nada escrito** · `4` fallo con vuelta atrás **completada** · `5` fallo con vuelta atrás **incompleta**, requiere intervención · `6` verificación posterior fallida, **sin deshacer nada**. `backup.sh`/`restore.sh`/`restore-drill.sh` remapeados; la equivalencia con su tabla anterior está en `lib/backup-common.sh` y en la guía. `changelog.sh` **conserva la suya** y lleva escrito por qué (no es entregable del producto).

**Decisiones que conviene conocer.**
1. **La señal de «instalación previa» es triple**: contenedores del proyecto, volúmenes `kronoqr_*` y **un `.env` con `APP_KEY` ya puesta**. La tercera salva el caso peligroso: contenedores borrados pero volúmenes con el registro dentro; reinstalar rotaría `APP_KEY` y dejaría esos datos ilegibles.
2. **El cliente edita `.env`, no `.env.example`.** Por eso la presencia del `.env` no es señal de instalación previa; lo es que ya tenga secretos.
3. **Los tres `dist/` viajan DENTRO de la imagen de Nginx**, no en un volumen: Docker copia a un volumen *vacío* y no vuelve a tocarlo, así que la primera actualización dejaría un panel viejo contra una API nueva.
4. **`KRONOQR_BASE`** (variable de *build*, no de despliegue) en los tres `vite.config.ts`, por defecto `/`: dev y E2E no cambian. En el quiosco gobierna además `start_url`, `scope` y `navigateFallback` del service worker.
5. **Observabilidad en perfil, encendida de serie.** Va en el `.env` y no en `--profile` a propósito: si el instalador pasara la bandera, el siguiente `docker compose up -d` que teclee el IT apagaría la observabilidad en silencio.
6. **`DB_MAINTENANCE_PASSWORD` NO la genera el instalador** (ADR-027). Procedimiento manual de asignación temporal en `operacion.md` §9.
7. **`--insecure` en las sondas de la fase 5**: se comprueba por loopback contra el propio contenedor; que el certificado sea el correcto se comprueba desde un quiosco, y la guía lo dice así.
8. **`pull` imagen a imagen**, no `compose pull`: una instalación sin internet con las imágenes ya cargadas (`docker load`) no puede quedarse esperando a un registro que no alcanza.
9. **Resolución DNS de `APP_URL` = aviso, no bloqueo**: el DNS partido es habitual y bloquear impediría instalaciones correctas.
10. **`port_in_use` tiene tres respuestas** (ocupado / libre / no se ha podido averiguar): sin `ss` ni `netstat`, un «libre» falso dejaría el fallo para la fase 4 con el `.env` ya escrito.
11. **El vale de un solo uso del 2FA NO va aquí.** Doc 07 §6 corregido: la prórroga a la 5.4 partía de una premisa falsa (que el instalador crearía la primera cuenta). **El instalador no crea usuarios y no debe crearlos.** Decisión del usuario (01-09-2026): se implementa en la **5.5**, con la primera cuenta del asistente.

**Configuración nueva.** `COMPOSE_PROFILES=observability` (perfil de Compose, no parámetro del producto) e `IMAGE_TAG` **vacía** en la plantilla. Ninguna variable nueva de aplicación.

**Verificado en local, tras las tres revisiones:** `make sh-lint` 0 hallazgos (16 scripts) · `make quality` verde (api-lint 0, Pint **1239 PASS**, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 0 sin cubrir**) · `make secrets-scan` **0 hallazgos en 142 commits** · `make test-unit` **1257 en 3,92 s** · `make test` **2836 / 12833** (442 s) · `qa:traceability --check` sin huecos, matriz regenerada · `docs:consistency` sin divergencias · `type-check` verde en las tres SPA · `make restore-drill` **CORRECTO** · **`make build-ci-images IMAGES=nginx` construye la imagen de entrega**, con `nginx -t` en verde dentro de ella, las tres SPA en `/var/www/spa/` y el manifiesto del quiosco con `scope=/kiosk/` · `compose config` **falla con el mensaje de `IMAGE_TAG`** y resuelve 12 servicios con el perfil / 7 sin él · `check-package-links.sh` **71 enlaces, 0 rotos** · `bash -n` en los nueve scripts.

**Fases del instalador ejercitadas DE VERDAD en local (Git Bash sobre Windows):** fase 1 completa en **los dos idiomas** (salida **0** con `--check-only`; salida **2** retirando el certificado, con el valor de plantilla en `APP_URL`, con `TLS_ALLOW_SELF_SIGNED=true` y sin privilegios de root, siempre con el `.env` **idéntico por MD5**), fase 2 (salida **3** contra el entorno de desarrollo real) y el tratamiento del `.env` de la fase 3 en aislamiento, cargando el script gracias a la guarda `BASH_SOURCE`. Códigos **1, 2, 3 y 6** de `backup.sh` comprobados en vivo. **Las fases 4 y 5 completas y los códigos 4 y 5 siguen SIN ejecutarse en local**: exigen Linux con Docker. Su evidencia son los escenarios **C, D, E y F** de la etapa ⑧, **que todavía no se ha ejecutado ni una vez**.

**Pendiente.**
- **Lanzar la etapa ⑧ a mano («Run workflow») sobre esta rama** antes de dar la tarea por cerrada: es la única evidencia de las fases 4 y 5 y del fallo seguro con puerto ocupado. Duración estimada 12–18 min sin caché, 6–9 con ella.
- **5.7:** completar `docs/runbooks/actualizacion-cliente.md` (hoy esqueleto, escrito porque `install.sh` remite ahí al salir con `3`) y añadir a la etapa ⑧ la mitad de «actualización desde la versión anterior» que RQ-11 también pide.
- **5.9:** `phase_verify` de `install.sh` tiene el punto de enganche documentado para `product:doctor`; hoy verifica con `/health`, `/ready` y `license:show`.
- **5.11:** capturas de pantalla y guía de endurecimiento en `instalacion.md`.
- **Usuario:** sigue pendiente generar el par ed25519 y pegar la pública en `config/license.php`. Ahora `make release-gate` lo exige **en cada etiqueta `vX.Y.Z`** desde la etapa ⑧: sin ello no se puede publicar versión.
- **Vigilar el `package-lock.json`**: cualquier `npm install` ejecutado en Windows con `node_modules/` presente puede volver a dejar a `@tailwindcss/oxide` con una sola plataforma y romper la imagen de Nginx. La guarda de `QualityGatesTest` lo caza en la CI; la receta de reparacion esta en la cabecera de `infra/docker/nginx/Dockerfile`.
- **Cierre de fase:** `seguridad-cumplimiento` decide si la fila «Despliegue seguro» de doc 07 §3 sube de 1 a 2. La evidencia ya está escrita; **el nivel no lo he subido yo**.


### Correcciones de las revisiones de `devops-observabilidad` y `revisor-codigo`, y del coordinador (01-09-2026, sin commitear)

**DEFECTO DEL COORDINADOR — la imagen de entrega de Nginx no se construía en Linux.** `npm ci` moría con `Cannot find module '@tailwindcss/oxide-linux-x64-musl'`. Causa: un `npm install` ejecutado **en Windows con `node_modules/` presente** reescribió `package-lock.json` y dejó a `@tailwindcss/oxide` con **una sola plataforma** (`win32-x64-msvc`), anidado además bajo los tres workspaces (npm/cli#4828). En Windows no se notaba nada. Reparado con la receta de tres pasos, **ejecutada en un contenedor Linux sobre un árbol con solo los `package.json` y el lock**: `npm install --package-lock-only ... -w frontend-{admin,kiosk,portal} @tailwindcss/oxide@4.3.3` → restaurar los cuatro manifiestos → `npm install --package-lock-only`. **Medido: 0 paquetes cambian de versión** (comparación por ruta completa), 19 entradas nuevas (oxide izado a la raíz con sus 12 plataformas y sus dependencias `wasm32-wasi`), 6 duplicados por workspace retirados. Verificado con `npm ci` + `require('@tailwindcss/oxide')` en `node:24-alpine`, y después con **`make build-ci-images IMAGES=nginx` en local: imagen construida**, las tres SPA dentro (`/var/www/spa/{admin,kiosk,portal}`), assets con su prefijo (`/admin/assets/…`) y el manifiesto del quiosco con `start_url` y `scope` = `/kiosk/`. Guarda contra la regresión: `QualityGatesTest` («mantiene en el lock los binarios nativos…»), que exige `oxide-linux-x64-musl` **y** `rolldown-…-musl` como entradas de raíz y prohíbe que oxide vuelva a anidarse bajo un workspace. La receta está escrita en la cabecera de `infra/docker/nginx/Dockerfile`, junto al `npm ci` que fallaba.

**BLOQUEANTES.**

- **B1 (devops) — un fallo después de empezar a escribir no deshacía nada.** Sin `trap ... ERR`, un `cp`, un `chmod` o uno de los catorce `set_env_value` que fallara mataba el script con `set -e` y su código crudo: sin vuelta atrás, `.env` a medias y `.env.kronoqr-pre-install` huérfano; y el reintento salía **3** («ya hay secretos») sin un solo contenedor: callejón sin salida. Ahora `set -Eeuo pipefail` y `trap 'rollback_and_die …' ERR` **armado al entrar en `phase_secrets`** —nunca antes: la fase 1 tiene que seguir saliendo 2— y `trap - ERR` tras `phase_verify`, porque a partir de ahí la instalación existe y deshacerla por un fallo al imprimir el informe sería el fallo que el manejador evita. `make sh-lint` acepta ahora `set -Eeuo pipefail` (estrictamente más estricto), con el porqué escrito en el Makefile.
- **B2 (devops) — el paquete entregaba enlaces rotos.** No copiaba `restore-drill.sh` ni `docs/runbooks/`, y `instalacion.md` invocaba `/opt/kronoqr/scripts/restore-drill.sh`, que no existe en el paquete. Ahora se copian los dos, la ruta es `./restore-drill.sh`, y **la documentación conserva el layout del repositorio** (`docs/cliente/` + `docs/runbooks/`) en vez de aplanarse: aplanarla obligaría a reescribir enlaces al empaquetar, un paso que nadie prueba. Guarda nueva: `infra/scripts/check-package-links.sh`, invocado por la etapa ⑧. **Medido: 13 enlaces rotos con el layout plano, 0 con el actual, 71 comprobados.** Dos enlaces se retiraron porque apuntaban a documentos que **no deben** viajar al cliente (doc 07 interno, ADR-007) y uno a un runbook que aún no existe (`alta-nuevo-quiosco.md`, 5.6): ahora se nombran sin enlazar.
- **B1 (revisor) — `rmdir` convertía todo fallo de la fase 4 en un código 5.** `${BACKUP_PATH}/wal` es bind mount y PostgreSQL archiva desde el primer segundo; `down -v` no lo vacía y `rmdir` falla sobre un directorio con segmentos. El propio mensaje `f_migrating` prometía lo contrario. Ahora `rm -rf`, seguro porque la acción **solo se registra dentro del `if [ ! -d … ]`**: nunca toca un destino que ya existía.
- **B2 (revisor) — los valores de ejemplo de la plantilla pasaban la fase 1.** `APP_URL=https://localhost` y las tres redes de ejemplo son cadenas no vacías: instalaban, la fase 5 los daba por buenos (sondea `127.0.0.1` con `--insecure`) y **ningún quiosco podía llegar**. Ahora `APP_URL`, `KIOSK_VLAN_CIDR`, `PORTAL_INTERNAL_CIDR` y `METRICS_ALLOW_CIDR` se **comparan con `.env.example`** y fallan si no han cambiado. `BACKUP_PATH` y `TLS_CERT_DIR` no: sus valores de plantilla son legítimos en producción. La etapa ⑧ los rellena ahora como lo haría el IT (`kronoqr.ci.local` en `/etc/hosts`, cert con ese CN).
- **B3 (revisor) — `--lang en` mezclaba idiomas.** 15 literales españoles fuera del catálogo y `kq_exit_name` solo en español (afectaba a los cinco scripts). Todo al catálogo: **124 claves × 2**, `kq_msg_check_catalog` en verde. `kq_exit_name` resuelve idioma **dentro de `exit-codes.sh`**, no vía `messages.sh`: los tres scripts de copia cargan el primero y no el segundo. Prueba nueva que barre la salida inglesa buscando diez palabras españolas.

**IMPORTANTES y menores, uno a uno.** I1 self-signed → `check_fail` con `APP_ENV=production` y en la lista mínima de `instalacion.md` §1.2 · I2 cada secreto se valida (no vacío y longitud) antes de escribirse, con `set_generated_secret`; antes un openssl roto escribía `APP_KEY=base64:` sin quejarse · I3 el `register_undo` de `metrics/` solo se registra si la creación fue bien, y si no se pudo crear se avisa · I4 rutas de producción en `operacion.md` y `rotacion-secretos.md` (`-f docker-compose.yml`, no `infra/compose.dev.yaml`) más una nota de dónde se ejecutan · I5 **escenario F nuevo**: enciende el perfil `observability` sobre la instalación de C y exige los cinco servicios en marcha, `/ready` respondiendo después y que node-exporter lea el árbol de copias · I6 `instalacion.md` §1.1 dice qué **no** viene en la 2.0.0 (`update.sh`, `doctor.sh`, asistente) y cuándo llega · I6 (revisor) **un solo lector de `.env`** en `lib/env-file.sh`, con la regla de comentario de Docker Compose, más `lib/fs.sh` para las tres invocaciones de `df` · I8 `.env.kronoqr-pre-install` a `0600` inmediato y borrado al final de `main()`, retirándolo antes de la pila con `discard_undo` · I9 `check_privileges`: la fase 1 exige root (salvo que `wal` ya exista con propietario 70) y el mensaje ya no ofrece el grupo `docker` como equivalente · I10 `toContain()` es variádico y el «mensaje» se evaluaba como segunda aguja: ahora `str_contains(...)->toBeFalse('…')` · 11 etiquetas propias para los estados negativos (`c_port_busy`, `c_not_writable`, `c_appurl_dns_no`, `c_tls_missing`, `c_env_missing`, `c_template_missing`) · 12 retirado el `chmod 0755` de `metrics/` y su comentario falso · 13 typo `App\Support\Environment\ProductionSafetyGuard` en doc 07 §3 · 14 `existing_stop` y `done_next` dicen «se entrega a partir de la 2.1» · 15 `exec 3<&-` muerto retirado.

**Prueba nueva:** `tests/Integration/Install/InstallScriptTest.php`, **10 pruebas**, que EJECUTAN el instalador (antes RF-PD-02 y RQ-11 se apoyaban solo en greps): salida 1 con opción desconocida; salida 2 con requisito roto y **`.env` idéntico por MD5**; rechazo de los valores de plantilla; rechazo del autofirmado en producción; `--check-only` sin escribir; `--lang en` sin una palabra española; catálogo completo; lectura de `.env` con los dos casos que separaban a los dos lectores (`"…"   ` y `…#1`); escritura de un secreto **sin perder ni un comentario ni duplicar la clave**; y que las acciones de deshacer usan `rm -rf`. Posible gracias a la guarda `[ "${BASH_SOURCE[0]}" = "$0" ]` al final de `install.sh`.

**Etapa ⑧, ahora seis escenarios** (A `--check-only` · B fallo seguro con el 443 ocupado · **E fallo dentro de la fase 4** → salida 4, vuelta atrás completa comprobada contenedor a contenedor y `.env` byte a byte, más reintento posible · C instalación completa · D segunda ejecución · **F el perfil por defecto encendido de verdad**). Estimación revisada: **20–30 min en frío, 10–15 con caché**; `timeout-minutes: 50`. La rotura de E es `DB_HOST` a un nombre que no resuelve: postgres y redis arrancan sanos y el fallo cae en las migraciones, que es donde ya hay contenedores, volúmenes y directorios que deshacer.

**Desviación consciente, dicha por escrito.** El escenario E prueba que el código es **4** y que la máquina queda limpia; **no fuerza que `${BACKUP_PATH}/wal` tenga segmentos dentro** en el momento del fallo, porque el archivado de PostgreSQL depende de `archive_timeout` (900 s) y no es determinista en una ventana de CI. El mecanismo exacto —`rm -rf` y no `rmdir`— lo fija la prueba de integración sobre el texto del script. Las dos juntas cubren el bloqueante; ninguna de las dos sola.

### Corrección del fallo de la primera ejecución de la etapa ⑧ (run 33547769303, salida 5) — 01-09-2026, sin commitear

**EL PATRÓN QUE EL PROYECTO TIENE QUE RECORDAR: `set -E` + `trap ERR` + `pipefail` + sustitución de orden.** Es la causa raíz, y no se parece en nada al síntoma.

**Cadena completa, reproducida con `set -x`.** `random_password` era `tr -dc … </dev/urandom | head -c 32`. `head` cierra el descriptor al llegar a 32 bytes, `tr` —que lee un flujo infinito— muere por **SIGPIPE**, y con `pipefail` la tubería «falla». Con `set -E`, el `trap ERR` que arma `phase_secrets` **se hereda dentro de la subshell** de `"$(random_password)"`, así que la **vuelta atrás se ejecutaba ahí**: restauraba el `.env` —borrando `APP_KEY` y las claves QR ya escritas—, consumía `.env.kronoqr-pre-install` y hacía `exit 4` **de la subshell**. El padre no se entera, porque el estado de una sustitución en posición de argumento se descarta: seguía escribiendo secretos sobre un fichero ya restaurado. Tres invocaciones de `rollback_and_die` en una sola ejecución. El `.env` final **no tenía `APP_KEY`** (el escenario C habría fallado al arrancar la aplicación aunque E hubiera pasado), y cuando la fase 4 falló de verdad, la copia previa ya no existía: **salida 5**.

**Las dos capas del arreglo, las dos necesarias.**

1. **`rollback_and_die` solo actúa en el proceso principal.** Primera línea: `if [ "${BASHPID}" != "$$" ]; then return 1; fi`. Deshacer tiene efectos sobre disco y solo tiene sentido **una vez y desde `main`**; en una subshell se vuelve sin tocar nada y decide el padre, con lo que de verdad sabe: la longitud del secreto recibido (`set_generated_secret`). Esto convierte el SIGPIPE benigno en inocuo.
2. **`random_password` sin SIGPIPE.** Entrada finita (`openssl rand -base64 48`, que ya es requisito de la fase 1), `tr` la consume entera y **el recorte a 32 lo hace la expansión de parámetros de bash**, que no es un proceso. Bucle para el caso remoto de que el filtrado deje menos de 32.

**La clase entera, barrida, no solo la instancia.** Otras dos tuberías cortaban a su productor: `manifest_field` en `lib/backup-common.sh` (`sed … | head -n 1` → ahora `sed …;T;q`, sin tubería: bajo `pipefail` devolvía 141 aunque encontrara el campo) y la comprobación de cabecera de `changelog.sh` (`head -n 1 | grep -q`). Las dos corregidas con su porqué escrito.

**Y que un fallo de deshacer diga por qué.** `rollback_and_die` capturaba el stderr de cada acción en `/dev/null`: el mensaje decía «NO se ha podido deshacer: X» y obligaba a reproducir a ciegas. Ahora se captura y se imprime bajo la línea. Son órdenes sobre rutas y contenedores, sin un solo secreto.

**Prueba nueva: `tests/Integration/Install/InstallSecretsPhaseTest.php`, 6 pruebas, sin Docker.** Es la reproducción exacta con la que se diagnosticó: carga `install.sh` por la guarda `BASH_SOURCE` y ejecuta `phase_secrets` sobre un `.env` temporal. Afirma que la copia previa **sobrevive**, que las 13 variables quedan escritas con su longitud, que ningún secreto sale por pantalla (valor a valor, contra el `.env` recién escrito), que la vuelta atrás legítima restaura **byte a byte** (MD5) y sale 4, que un trap disparado en una subshell **no deshace nada**, y que `random_password` da 32 caracteres veinte veces seguidas sin disparar un trap centinela. Más una que prohíbe que vuelva a aparecer una tubería sobre `/dev/urandom` en los cinco scripts.

**Medido que la prueba caza el fallo, que es lo único que hace válida una prueba de regresión:** con el generador antiguo restaurado, **2 de 6 fallan**; retirando además la guarda `BASHPID`, **fallan las 6**. Con el árbol arreglado, **16/16** en los dos ficheros de `tests/Integration/Install/`.

**Etapa ⑧:** el artefacto ahora recoge las salidas de **B, C, D, E y el reintento de E** (antes solo `salida-b.log`, y por eso hubo que diagnosticar a ciegas), y el paso de diagnóstico mira los **dos** paquetes (`paquete/` y `paquete-e/`) más el estado global de contenedores y volúmenes.

**Sigue pendiente:** relanzar la etapa ⑧. Los escenarios C, D, E y F y los códigos 4 y 5 **siguen sin ejecutarse nunca de extremo a extremo**; lo que sí está ejercitado en local, y con la prueba que lo demuestra, es la fase 3 completa y las dos mitades de la vuelta atrás.

## Sesión «tarea 5.3: licencia firmada, verificación local y degradación honesta» (01-09-2026), en `feat/fase-5-productizacion`

**Tarea 5.3 implementada y verificada, sin commitear** (lo commitea el usuario tras las revisiones de `seguridad-cumplimiento` y `revisor-codigo`). Cubre `RF-PD-04` y `RF-PD-05`.

**Las tres respuestas del paso 11, con la prueba que las sostiene:**
1. **¿Algún camino en el que la licencia impida fichar o consultar el registro?** No. `tests/Feature/Product/LicenseDoesNotBlockTest.php` (11 pruebas) lo comprueba con una licencia **caducada de verdad** —clave firmada con un par generado al vuelo y reloj adelantado—: `POST /scan` 200 y crea tramo, padrón y latido del quiosco, `reports/legal-export` 200, `employees/{uuid}/workdays` 200, `me/workdays` y `me/export` 200, corrección trazada 200, cadena de auditoría íntegra, `/health` y `/ready` 200. Más los casos «sin ninguna licencia» y «licencia ilegible». Y por construcción: el `FeatureGate` **solo acepta un `Feature`**, y en ese enum el conjunto legal no tiene caso, así que la pregunta no se puede ni formular.
2. **¿Alguno en el que impida dar de alta o emparejar?** No. `tests/Feature/Product/PlanLimitsDoNotBlockTest.php`: con `max_employees` superado, `POST /employees` 2xx y la persona ficha; con `max_devices` superado, la emisión de token vincula y el quiosco ficha. El conteo es un **observador** en `afterCommit` y bajo `try`: un fallo suyo pierde el asiento, nunca el alta.
3. **¿La verificación es realmente local?** Sí. `tests/Feature/Product/LicenseVerificationIsLocalTest.php` con `Http::preventStrayRequests()` + `Http::assertNothingSent()` sobre los cinco caminos (activar, consultar, decidir, endpoint, consola) y también con la licencia caducada.

**Formato de la clave y dónde vive cada pieza.** `KQL1.<carga base64url>.<firma base64url>`, firma ed25519 **sobre el texto codificado tal y como viaja**. **Verificación** en el producto (`Product/Infrastructure/Adapter/Ed25519LicenseVerifier`, sodium nativo) con la clave **pública** de `config/license.php`. **Emisión fuera del producto**, en `tools/license-issuer/` de la raíz —que no entra en ninguna imagen: el `Dockerfile` hace `COPY backend/ ./`—, con la privada por variable de entorno. `LicenseIssuerRoundTripTest` ejecuta ese CLI **como subproceso** y verifica su salida con el verificador del producto.

**La frontera de ADR-023, materializada.** `Shared/Domain/ValueObject/Feature` (7 casos accesorios) **es** la lista; el conjunto legal no tiene caso, así que no existe forma de expresar su desactivación. `tests/Architecture/LicenseBoundaryTest.php` **lee ADR-023** y falla si el enum y la tabla divergen, comprueba que solo `Product/Infrastructure/Persistence` nombra la tabla `license`, que el estado de licencia no sale de `Product`, que nadie escribe su propia condición sobre la caducidad, y que el camino de fichaje no menciona la licencia por ninguna vía.

**Retrofit (paso 10).** 2.8: `GET /reports/period` y su exportación responden **`402` con `problem+json` y tipo propio** (`urn:kronoqr:problem:feature-not-licensed`), con `feature`, `restriction`, `since` y un `detail` que **nombra lo que sigue disponible**. 2.4: la presencia **degrada a sondeo** —`meta.realtime.enabled: false` con `unavailable_reason`/`unavailable_since`— y el listado se sirve entero. Los dos con prueba en `LicenseDegradesAccessoriesTest`.

**Decisiones que conviene conocer:**
1. **`max_sites` no se modela.** ADR-040 lo retiró; una clave que lo traiga verifica igual y el campo se ignora, porque la lectura tolera lo desconocido (compatibilidad hacia delante: una instalación en la 1.2 debe poder activar una clave emitida con la 1.6). Modelarlo «por compatibilidad» lo dejaría a la vista de quien quisiera comprobarlo.
2. **`LICENSE_KEY` no se lee en ejecución.** Solo la mira `license:activate` sin argumento, que es como la llamará el instalador de la 5.4. Manda la fila (decisión de la 5.1).
3. **`config/license.public_key` va vacía de serie**, y eso significa «esta compilación no puede verificar ninguna clave»: motivo `no_public_key`, producto entero funcionando y mensaje que manda a revisar el **despliegue**, no a pedir otra clave. **El fabricante tiene que generar el par una vez** (`tools/license-issuer/generate-keypair.php`) y pegar la pública antes de la primera imagen de release. **Pendiente del usuario.**
4. **Novena familia del bloque D: `LicenseLifecycle`**, con dos acciones (`license.activated`, `license.plan_exceeded`). Es la única de relevancia **comercial** y su caso lo explica: no cabe en `AuthorityOrCalculationChange`, que responde «¿quién movió las reglas del cálculo?» y es una pregunta de inspección.
5. **`/health` gana `license`**, una palabra, leída **solo de caché** y `unknown` si no está: la sonda de vida no puede tocar la base de datos. La escribe el punto único de resolución (no el `FeatureGate`), para que tras activar una clave la sonda no siga diciendo `unknown`.
6. **Se puede activar una clave ya caducada** y consta el estado resultante en el asiento. Rechazarla obligaría a un cliente que renueva tarde a pedir otra.
7. **Aviso de caducidad a 30 días** (decisión del usuario), umbral en `config/license.php`. Banner persistente y no descartable, que cambia de tono al caducar. Sin correos.

**Configuración nueva** (`config/license.php`): `LICENSE_PUBLIC_KEY` (vacía), `LICENSE_EXPIRY_WARNING_DAYS=30`, `LICENSE_HEALTH_PROBE_TTL_SECONDS=600` y `LICENSE_KEY` (solo arranque). Métrica nueva: `license_limit_exceeded_total{limit}`.

**Verificado:** `make quality` en verde (api-lint 0, Pint **1233 PASS**, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 0 sin cubrir**; Rector informativo) · `make secrets-scan` **0 hallazgos en 140 commits** · `make sast` **0 hallazgos** · `make test-unit` **1240 en 4,04 s** · `--group=RF-PD-04` **146** · `--group=RF-PD-05` **78** · `make test` **2784 / 12628** (447 s) · `qa:traceability --check` sin huecos y matriz regenerada · `docs:consistency` sin divergencias · `type-check` verde en las **tres** SPA con `schema.d.ts` regenerado (cambio **aditivo salvo una línea de descripción**) · panel **293 unitarias**, `lint` y `prettier` limpios · migración aplicada con `fichaje_migrator`, con prueba de ida y vuelta propia · **verificación manual completa sobre la BD de desarrollo**: `license:show` sin clave (degradado, salida 1, nunca error fatal), clave **caducada emitida con el emisor real** y un par generado al vuelo, activada (salida 1, aviso «YA ESTA CADUCADA»), `license:show` mostrando caducidad, exceso de plan 236/200, degradación con fecha y la sección «Lo que NUNCA depende de la licencia»; asiento `license.activated` con huella y sin la clave; cadena de auditoría íntegra; sonda devolviendo `expired`.

**Cambio de comportamiento que conviene conocer.** El informe por periodo y la presencia en vivo **exigen ahora una licencia que los conceda**. Siete ficheros de prueba existentes llaman a `LicenseKeys::grantAll()` en su `beforeEach`; **no se activa licencia en la suite entera a propósito**, porque entonces la degradación no la vería nadie hasta casa del cliente. En una instalación recién puesta en marcha y sin clave, RRHH no tiene informes por periodo hasta activarla: es lo que dice ADR-023 y lo que documenta la guía de cliente.

**Presupuesto de la suite unitaria subido de 4 a 5 s** (`Makefile`), con el porqué escrito allí: la suite pasó de 1136 a 1240 pruebas y midió 4,2 s. El coste marginal de las 104 nuevas fue de 0,3 s, medido fichero a fichero. **Es una decisión revisable**; si molesta, lo que hay que discutir es si esas pruebas deben ser unitarias.

**Documentación:** `docs/cliente/configuracion.md` §3 bis completa (qué es la clave, cómo se activa, qué pasa al caducar con las dos tablas de «deja de funcionar / sigue funcionando», los límites del plan y `curl` de las dos rutas) y cuatro «qué hacer si…» nuevos · `docs/cliente/operacion.md` §7 (los dos comandos con sus códigos de salida y el campo de la sonda) · `docs/cliente/obligaciones-legales.md` §7 bis («tu obligación de registrar no depende de la licencia») y dos filas nuevas en §8 · enmiendas fechadas en **ADR-018, ADR-019, ADR-023 y ADR-028** · doc 01 §5 (tabla `license`) y Anexo B (tres filas nuevas de notas de contrato) · doc 02 §8.2 · doc 05 §10.5 (qué se pierde exactamente y que los límites no bloquean) · `.env.example`.

**Pendiente / diferido:**
- **El fabricante debe generar el par ed25519 una vez** y pegar la pública en `config/license.php` antes de la primera imagen de release. Hasta entonces ninguna clave verifica y el producto funciona degradado, diciéndolo con esas palabras.
- **La 5.6 debe extender `PlanLimitsDoNotBlockTest`** al emparejamiento por código cuando exista `/kiosk/pair/confirm`.
- **La 5.9 (`doctor`)** puede leer el estado con `license:show` (código de salida 0/1) o con el campo `license` de `/health`.
- **La 5.5 (asistente)** activa la clave llamando a `POST /license/activate` o a `license:activate`.
- **Sin E2E de la pantalla de licencia**: la sesión de las pruebas E2E del panel no lleva `license:*`. Encaja mejor con la 5.5.
- Las cuatro funcionalidades accesorias que aún no existen (cuadro de impacto, exportación de nómina, resumen semanal, marca blanca) están en el catálogo y marcadas como no implementadas: ni el panel ni `license:show` las presentan como pérdida.

### Correcciones de las revisiones de `seguridad-cumplimiento` y `revisor-codigo` (01-09-2026, sin commitear)

**BLOQUEANTES del panel, los dos con la prueba que los habría cazado.**

**B1 — todas las fechas de licencia salían vacías.** `formatDay()` usaba el `d()` de vue-i18n y `createAppI18n()` no declara `datetimeFormats`: devolvía cadena vacía. El banner decía «La licencia caducó el , hace 12 días» y la pantalla «Vigencia: – ». Ahora hay un solo sitio que formatea, `frontend-admin/src/features/settings/license.dates.ts`, sobre `formatCivilDate` de `web-kit` y **en UTC a propósito**: la vigencia la fija el fabricante al emitir y es la que aparece en la factura; convertirla a la zona del centro haría que un hotel de Canarias viera un día distinto del de su contrato. Pruebas nuevas en `LicenseView.spec.ts` («enseña el día concreto de la vigencia, no un hueco») y en `LicenseNotice.spec.ts`, que afirman `1 ene 2026`, `31 dic 2026` y que el texto no contiene la fecha vacía.

**B2 — el banner imprimía la clave de traducción en crudo.** Con licencia **vigente** y plan superado, `needs_notice` es `true`, el estado es `valid` y `license.notice.valid` no existía —la había quitado la prueba de i18n que prohíbe textos vacíos—. Añadida en `es.json` y `en.json` con texto real. `LicenseNotice.spec.ts` afirma ahora el **texto principal** (`data-test="license-notice-text"`), no solo el sufijo del exceso, y que el banner no contiene ninguna clave sin traducir.

**IMPORTANTES.**

**I1 — escritura desnuda en el camino de una lectura.** `DatabaseLicenseRepository::markVerified()` es una escritura en el camino de `GET /license` y `license:show`, que son las dos superficies desde las que alguien diagnostica un problema de licencia: con la base en solo lectura o el disco lleno daba `500` con traza, contra la invariante «nunca lanza» escrita tres veces. Ahora va bajo `try/catch (Throwable)` con `warning` `product.license_touch_failed`, sin clave ni cliente. `tests/Feature/Product/LicenseTouchFailureTest.php` lo fija con `ReadOnlyLicenseConnection`, un doble de **conexión** (no de repositorio: un repositorio postizo probaría lo contrario) que lanza antes de tocar la base — un `UPDATE` que falla de verdad dejaría abortada la transacción de la prueba, cosa que en producción no pasa. **Medido**: las dos pruebas caen al retirar el `try`.

**I2 — la degradación del tiempo real era cooperativa.** Un cliente que ignorase `meta.realtime.enabled` conservaba el tiempo real íntegro. Tres cambios:

- **(a)** los dos `Broadcast::channel` de `routes/channels.php` consultan el `FeatureGate` **antes que nada** y no firman la suscripción; y la respuesta deja de servir `key` y `channels` cuando no hay tiempo real. `path` y `auth_endpoint` **sí** se sirven: son rutas estáticas del propio origen, no dan acceso a nada, y quitarlas obligaría a hacer nulable en el contrato un campo que nunca lo fue. `BroadcastPresenceChange` **no se toca** (estela del fichaje, regla dura 19).
- **(b)** `isAvailable()` pasa a ser `blocker()` y devuelve **qué** condición falla (`installation`, `deployment`, `license`). Antes, una instalación sin Reverb y con la licencia caducada anunciaba `license_expired` y mandaba a quien lo leyera a hablar con el comercial cuando lo que hay que arreglar es el despliegue.
- **(c)** las pruebas anteriores pasaban sin mirar la licencia, porque en la suite `BROADCAST_CONNECTION=null` ya apagaba el tiempo real. Cuatro pruebas nuevas con **Reverb configurado de verdad**: `enabled:false` + `license_expired` + `key` nula + `channels` vacío, suscripción **rechazada con 403**, control positivo que la firma, y Reverb apagado + licencia caducada → motivo nulo. **Medido**: retirando el último `return` de `blocker()` cae una; retirando las dos guardas de `channels.php` cae la de la suscripción.

**I3 — puerta de release.** `make release-gate` falla si `backend/config/license.php` sigue con la clave pública vacía: una imagen de entrega construida así rechaza la licencia recién pagada del cliente con motivo `no_public_key`, y el síntoma apunta al sitio equivocado. **No entra en `build-ci-images`** a propósito: la CI construye imágenes de prueba, que deben poder llevarla vacía. **La 5.4 tiene que invocarlo desde el paso que construye la imagen de entrega.** Corregida además la redacción: no hay «constante» que pegar, es el segundo argumento de `env('LICENSE_PUBLIC_KEY', '')` — dicho así en el config y en el README del emisor.

**I4 — la huella estaba duplicada.** Un solo punto, `StoredLicense::fingerprintOf()`, usado por la activación (antes de que exista la fila) y por el recurso. Es el número por el que el fabricante y el hotel se entienden por teléfono. Prueba en `LicenseActivationTest` de que asiento y recurso coinciden, y en `LicenseIssuerRoundTripTest` de que **la que imprime el emisor es la misma**. Verificado a mano de punta a punta: emisor `ba7c6e41fe70` → `license:show` `ba7c6e41fe70`.

**I5 — el emisor no validaba lo que firmaba.** Un `--max-employees=ochenta` daba `0` y producía una clave **firmada** que el cliente activa y su producto rechaza como `invalid_payload`, descubierta en casa del hotel con la factura ya mandada. Ahora valida antes de firmar: enteros positivos (`ctype_digit`), rango de vigencia, y nombres de `--features` contra el catálogo de ADR-023 —una errata como `advanced_report` produce una clave que verifica y **no concede nada**—. `--force` emite una funcionalidad que la herramienta aún no conoce, para cuando el producto vaya por delante. Seis casos en el round-trip, más el de `--force`.

**MENORES.** **M1** `/scan/batch` (207, lote entero, dos `scan_events`) y `/scan/pin` (200, origen `pin_kiosk`) con licencia caducada. · **M2** la rama `catch` de `ObservePlanLimits` ejercitada de verdad con un `PlanUsageCounter` que lanza —la prueba anterior no la tocaba, porque sin licencia verificada el caso de uso se da la vuelta antes de contar—. · **M3** seis filas nuevas en doc 07 §6 (abajo). · **M4** `tools/` en `.dockerignore`, con la nota de que el empaquetado de la 5.4 debe excluirlo. · **M5** `generate-keypair.php`: pública por **stdout**, privada por **stderr** o a fichero `0600` con `--secret-out`, y `sodium_memzero` al salir. · **M6** regla propia de gitleaks `kronoqr-license-signing-key` para una privada ed25519 en hexadecimal —ninguna regla por defecto la reconoce—; **comprobada plantando una clave falsa: 1 hallazgo; retirada: 0**. · **M7** las tres variables en doc 02 Anexo B y `LICENSE_HEALTH_PROBE_TTL_SECONDS` en `.env.example`. · **M8** comentario de `deptrac.yaml` corregido: publica `GetLicenseStatusHandler` vía `CachedLicenseStatePublisher`, no el gate. · **M9** la fecha del `402` se formatea en el **borde** y en el idioma negociado (`license.since_format`), con una sola conversión a UTC: antes el mensaje inglés salía con fecha española. Prueba que compara `es` y `en` y que el instante del cuerpo es el mismo. · **M11** `LicenseVerifierTest` movido a `tests/Unit/Product/Infrastructure/`. · **M12** `severity` y `state` del store tipados con las uniones de `schema.d.ts`.

**Lo que queda cooperativo tras I2, dicho por escrito** (y anotado en doc 07 §6): el servidor **sigue publicando** el evento de presencia en el canal aunque la licencia no lo conceda —esa emisión es la estela de un fichaje ya registrado y meterle una condición la acercaría al camino crítico, regla dura 19—, y **una suscripción firmada antes de que la licencia caduque sobrevive** hasta que ese socket se reconecte, porque el protocolo autoriza al suscribirse y no en cada mensaje. Ninguna de las dos es una fuga: lo que viajaría es lo que esa misma cuenta puede pedir por sondeo, que no se degrada.

**Consecuencia de I2 que conviene conocer.** `meta.realtime.channels` va **vacío** siempre que `enabled` es `false`, sea cual sea la causa. Tres pruebas de presencia (`LivePresenceScopeTest` ×2, `LivePresenceTest` ×1) miran el **alcance** y no la degradación: ahora piden Reverb y licencia para ver la respuesta en su forma de producción.

**Verificación tras las correcciones:** `make quality` en verde (sh-lint 0, api-lint 0, Pint **1235 PASS**, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 0 sin cubrir**; Rector informativo) · `make secrets-scan` **0 hallazgos en 140 commits** · `make sast` **0 hallazgos** · `make test-unit` **1252 en 4,07 s** (presupuesto 5) · `--group=RF-PD-04` **169** · `--group=RF-PD-05` **90** · `make test` **2815 / 12719** (467 s) · `qa:traceability --check` sin huecos y matriz regenerada · `docs:consistency` sin divergencias · `type-check` verde en las **tres** SPA · panel **294 unitarias**, `lint` y `prettier` limpios · `license:show` sin clave (salida 0, «SIN LICENCIA (el sistema sigue funcionando)») y con clave **caducada emitida con el emisor real** y un par generado al vuelo (activación con salida 1 y aviso de clave caducada; `license:show` con caducidad, exceso 236/200 y degradación fechada) · `make mutate` **MSI 83,10 %** (293 sin probar, 1 timeout, 1440 probados; umbral 80), medido tras las correcciones y **idéntico** al de antes de ellas: las pruebas nuevas cubren caminos que la mutación del dominio no toca —HTTP, canales, consola y el emisor— y los 11 supervivientes del dominio de licencia siguen siendo los mismos. **Los 11 son equivalentes**, comprobados uno a uno: cuatro en `License` (dos banderas de comparación estricta de `in_array` sobre *enums*, que comparan igual en modo laxo; un `=== false` defensivo tras un `preg_match` que ya garantiza el formato; y un `instanceof` cuyo `null` el filtro posterior descarta igual) y siete en `LicenseStatus` (un `>` frente a `>=` que en el instante exacto de la caducidad da el mismo cero; cuatro guardas sobre estados que solo produce `of()`, donde la licencia no puede ser nula; y dos enteros dentro de un `max(0, …)` que `intdiv` entre 86 400 devuelve a cero de todos modos). Matarlos exigiría afirmar estados inalcanzables o la prosa literal de un mensaje. `LicenseLimits`, `LicenseState` y `LicenseVerification`: **cero supervivientes** · BD dejada **sin clave activada** y con la semilla intacta (257 empleados, 19 680 tramos).

**Pendiente del usuario (lo más importante):** **generar el par ed25519 una vez** con `php tools/license-issuer/generate-keypair.php`, guardar la privada en el gestor de secretos y **pegar la pública** como valor por defecto de `env('LICENSE_PUBLIC_KEY', '')` en `backend/config/license.php` **antes de la primera imagen de release**. `make release-gate` lo comprueba. Hasta entonces ninguna clave verifica y el producto funciona degradado, diciéndolo con esas palabras.

**Pendiente anotado para otras tareas:**

- **5.4:** invocar `make release-gate` en el paso que construya la imagen de entrega, y excluir `tools/` del empaquetado.
- **5.5:** la importación masiva debe agrupar el exceso de plan en **un solo asiento** con el recuento, no uno por fila —bajo el candado global de ADR-010—; y `first_crossing` se deduce con un exceso de exactamente uno en `afterCommit`, así que **no es fiable en altas masivas ni concurrentes**. Además, extender `PlanLimitsDoNotBlockTest` al emparejamiento por código cuando exista `/kiosk/pair/confirm` (5.6).
- **5.9:** el asiento `license.activated` lleva `customer_name`; si el paquete de diagnóstico incluye asientos de `audit_log`, debe **redactar** ese campo en la familia `license_lifecycle` o excluirla (ADR-020).
- **Deuda anotada, fuera de alcance (M10):** cuarta implementación de base64url en el árbol (`Ed25519LicenseVerifier::decode()`, el emisor, `QrSigningKey`, `CredentialSecret`). Candidata a `Shared\Domain\ValueObject\Base64Url`; **no se unifica ahora** porque toca material criptográfico de tres tareas distintas y el riesgo supera al beneficio dentro de esta.

## Sesión «tarea 5.2: perfiles de cumplimiento y umbrales legales parametrizados» (31-08-2026), en `feat/fase-5-productizacion`

**Tarea 5.2 implementada y verificada, sin commitear** (lo commitea el usuario tras la revisión). Cubre `RF-PD-07` y toca `RN-10`, `RN-11`, `RN-12` y `RL-02`.

**Criterio de aceptación demostrado sobre la BD de desarrollo:** con `min_rest_hours = 12`, `attendance:detect-incidents` da `open_shift_expired: 1`; tras `UPDATE compliance_profiles SET min_rest_hours = 20`, la misma orden sobre las mismas 983 jornadas da `insufficient_rest: 786` + `open_shift_expired: 1`. **Ni una línea de código tocada.**

**Lo que ya estaba hecho y no había que rehacer** (verificado antes de escribir nada):
- `compliance_profiles` **ya tenía los once campos** del doc 01 §5 desde la 1.3, y la semilla `ES-hosteleria` ya traía `max_weekly_hours = 40`, `week_starts_on = 1` y `holiday_calendar = []` — exactamente los valores de la decisión del usuario. **No hacía falta migración de ampliación.**
- RN-10/11/12 **ya recibían su umbral por el puerto** desde la 2.6 (`AnomalyDetectionPolicy`); no quedaba ninguna constante que extraer en `Attendance/Domain`.
- La retención **ya salía de `compliance_profiles.retention_years`** desde la 2.10 (`ConfiguredRetentionPolicyProvider`). `compliance:apply-retention --dry-run` antes y después es **literalmente idéntico** (corte 2022-08-31, 4 años, perfil del centro 1, 0 filas en los cinco almacenes).

**Construido.** `Product/Domain/ValueObject/{ComplianceProfileField,ComplianceProfileFieldType,ComplianceProfileSnapshot,ComplianceProfileSource}`; `Product/Domain/Exception/InvalidComplianceProfileValue`; evento `Product/Domain/Event/ComplianceThresholdChanged` y listener `Compliance/Infrastructure/Listener/RecordComplianceProfileChange` (acción `calculation_setting.changed`, `subject_type = compliance_profile` **con `subject_id`**); `Product/Application/{Port/{ComplianceProfileRepository,ComplianceProfileMetrics},Command/UpdateComplianceProfileCommand,UseCase/{GetComplianceProfileHandler,UpdateComplianceProfileHandler}}`; `Product/Infrastructure/{Persistence/DatabaseComplianceProfileRepository,Metrics/RedisComplianceProfileMetrics}`; HTTP completo en `Product/Http/` (controlador, `UpdateComplianceProfileRequest` con reglas **derivadas del catálogo de campos**, resource, policy solo `admin`, telemetría); rutas `GET|PATCH /api/v1/compliance-profile` con `ability:settings:*` + `throttle:management`; i18n `lang/{es,en}/compliance-profile.php`; pantalla `frontend-admin/src/features/settings/ComplianceProfileView.vue` con su cliente tipado, su ruta (`SETTINGS_MANAGE`), su entrada de navegación y 9 pruebas de componente.

**Decisiones que conviene conocer:**
1. **Recurso propio y singular, `GET|PATCH /api/v1/compliance-profile`**, no una clave más de `PATCH /settings`. Un umbral legal lo fija la jurisdicción y uno operativo lo fija el hotel: en un mapa clave-valor serían indistinguibles «he bajado el anti-rebote» y «he bajado los años de conservación», y el asiento perdería el matiz. Singular como `/site` (ADR-040). Comparte `settings:*` porque el §7.3 no declara ámbito propio y ningún rol lo usaría por separado.
2. **Sin retroactividad** (decisión del usuario): el valor nuevo rige desde el cambio, la revisión diaria lo aplica en su siguiente pasada sobre su ventana de 7 días, no se recalcula el histórico y no se cierra ni se reabre ninguna incidencia. Va escrita en doc 01 §4, en `docs/cliente/configuracion.md` §2.4 y **dentro del propio asiento** (`applies_from: change_forward_only`). **No se ha escrito ADR:** es la decisión por defecto que ya exige la skill y la ventana de la 2.6 ya la sostiene. Elevarla a ADR es decisión del usuario.
3. **Migración nueva, la única del árbol:** `2026_09_06_100000_bound_compliance_profile_thresholds`. **Paso expand puro** (dos `CHECK` con `NOT VALID` y su `VALIDATE`). No amplía columnas —no faltaba ninguna— sino que pone **topes superiores** y la coherencia `max_weekly_hours >= max_daily_hours`. Existe porque esta tarea abre por primera vez un camino de escritura **humano** a estos umbrales, y el error peligroso es el silencioso: `max_daily_hours = 90` no rompe nada y apaga RN-11 hasta que alguien compara una nómina con el convenio. `down()` probado en la BD de desarrollo y con prueba de integración propia.
4. **Dos booleanos en el asiento**, no el `affects_worked_hours` de la 5.1: ningún campo del perfil mueve los **minutos** —las reglas de cumplimiento clasifican, no corrigen (doc 01 §4, regla dura 19)—, así que se separan `affects_incident_detection` y `affects_retention`, que son las dos preguntas que trae quien lee el trail.
5. **Campos no editables:** `jurisdiction` (ninguna regla la lee: un campo cuyo cambio no tiene efecto es peor que no ofrecerlo), `is_default` (apagarlo dejaría la instalación sin umbrales resolubles) e `id`. Enviarlos es `422`, no se ignoran en silencio.
6. **`name` sí es editable:** un cliente que ajusta los umbrales a su convenio provincial necesita que el perfil se llame como ese convenio, porque es lo que sale en el informe de retención y en el diagnóstico.
7. `maximumWeeklyMinutes`, `weekStartsOn` y `holidayCalendar` **amplían `CompliancePolicy`** aunque no los lea ninguna regla (los estrena la 3.4). Se declara en el docblock, en el contrato y **en la propia pantalla**, para no prometer un efecto que hoy no existe.
8. **Un `PATCH` que no cambia nada no escribe fila ni asiento**, y el panel manda solo los campos modificados. Las dos mitades se refuerzan: sin ellas, abrir la pantalla y pulsar «guardar» llenaría el trail de asientos que solo dicen «alguien miró el perfil».

**Enmiendas documentales:** doc 01 §4 (RN-10/11/12 enunciados en términos de parámetro, conservando las notas de RN-10 y de RN-12; nota nueva sobre la no retroactividad), doc 01 Anexo B (dos rutas nuevas y fila en la tabla de notas de contrato), doc 02 §7.3 (precisión 4) y §8.2 (`compliance_profile_changes_total{effect}`), doc 02 Anexo B y `.env.example` (`COMPLIANCE_PROFILE` **no se lee en ejecución**: es el nombre con el que el instalador de la 5.4 marcará el perfil por defecto; manda la fila, decisión de la 5.1).

**Documentación de cliente:** `docs/cliente/configuracion.md` §2.4 nueva y completa (los ocho campos con su origen normativo, la no retroactividad y sus tres consecuencias, el peligro de `retention_years`, y `curl` de lectura y de escritura) más dos «qué hacer si…» («cambio un umbral legal y la bandeja no cambia», «he bajado los años de conservación por error»). `docs/cliente/obligaciones-legales.md` §7 nueva: **ajustar el perfil al convenio aplicable es responsabilidad del cliente** (RL-16, RL-21), con la tabla de qué hace el sistema y qué tiene que hacer el hotel, y lo que el fabricante explícitamente no puede hacer.


**Verificado (todo con instrumento, no supuesto):** `make quality` en verde (Pint **1157 ficheros PASS**, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 7148 permitidas / 0 sin cubrir**, api-lint 0, sh-lint 0; Rector informativo) · `make test-unit` **1107 pruebas en 3,69 s** · `php artisan test --group=RF-PD-07` **143** · `make test` **2520 / 11862** (375 s) · `qa:traceability --check` sin huecos y matriz regenerada · `docs:consistency` sin divergencias · `type-check` verde en las **tres** SPA con `schema.d.ts` regenerado (cambio **solo aditivo**, 222 líneas) · `frontend-admin`: **271 unitarias**, `lint` y `prettier` limpios · migración aplicada, revertida y reaplicada con el rol `fichaje_migrator` y con prueba de integración propia · `compliance:apply-retention --dry-run` **idéntico** antes y después · `make mutate` global **MSI 82,76 %** (276 sin probar, 1324 probados; umbral 80). Sobre el dominio nuevo, medido aparte: `ComplianceProfileField` **100 %** y `ComplianceProfileSnapshot` + `CompliancePolicy` **97,75 %** — los dos mutantes que sobreviven ahí son equivalentes (un `positive()` que la invariante semanal/diaria ya cubre y un `array_values()` que `sort()` reindexa igual).

**Estado de la BD de desarrollo:** recreada y sembrada al terminar (`make migrate-fresh` + `make seed`): perfil `ES-hosteleria` con sus valores de serie, 14 729 jornadas, 0 incidencias.

**Defecto propio encontrado y corregido durante la verificación:** `ComplianceProfileField::maximum()` devolvía 24 para `week_starts_on`, así que un `8` pasaba la validación y moría en el `CHECK` del esquema — un error del cliente presentado como `500`. Ahora devuelve 7 y hay prueba (feature y unitaria).

**Trampa que conviene recordar:** `ContractInstallationSettingsScopeMigrationTest` hacía `migrate:rollback --step=1` y la migración nueva lo desplazó. Ahora cuenta los pasos hasta la migración concreta (`migrationTestStepsBackTo`), así que la siguiente tarea que traiga una migración no lo rompe.

**Pendiente / diferido:**
- **El panel no ofrece «volver a revisar los últimos N días»** después de cambiar un umbral. Es coherente con la no retroactividad, pero hoy la guía manda ejecutar `attendance:detect-incidents` a mano por consola. Candidato para la 5.5 o la 3.5.
- **`jurisdiction` no la lee ninguna regla** y por eso es de solo lectura. Cuando exista un segundo perfil de país habrá que decidir si se vuelve editable o si la fija el instalador (5.4).
- **Editar la fila con `psql` no deja asiento**, por diseño: solo audita el camino de la API. Está dicho en la guía de cliente; si algún día importa, sería un *trigger*, no código de aplicación.
- **Un cliente con dos convenios** exigiría más de un centro, que ADR-040 cierra. El esquema y el adaptador ya resuelven por centro y hay prueba; la decisión es del ADR, no del código.
- **`make e2e` no se ha ejecutado.** La sesión de las pruebas E2E del panel no lleva `settings:*`, así que la pantalla nueva no aparece en su navegación y ninguna de las 10 pruebas la toca. Un E2E propio del perfil encaja mejor con la 5.5.

### Correcciones de la revisión de `revisor-codigo` (31-08-2026, sin commitear)

**IMPORTANTE 1 — el asiento de auditoría afirmaba algo falso.** `break_required_after_hours` gobierna RN-12, y RN-12 se evalúa pero **no abre incidencias** (suspensión de la 2.6). Aun así el asiento escribía `affects_incident_detection: true`, la métrica contaba `effect=incident_detection` y el panel prometía «se marcarán jornadas distintas». Las tres eran mentira, y la primera dentro de un registro con valor legal.

**Cómo se resolvió sin romper capas.** La lista de suspensión era una constante **privada** de `DetectAttendanceAnomalies`, y `Product` no puede importar `Attendance` (doc 02 §1.6). Se invirtió: el hecho sube a `Shared/Domain/ValueObject/ComplianceRuleSuspension` (con el enum `ComplianceRule`, que nombra RN-10/RN-11/RN-12), que es la única capa que los dos alcanzan —Deptrac ya concedía `AttendanceDomain → SharedDomain` y `ProductDomain → SharedDomain`—. `AnomalyType::complianceRule()` y `ComplianceProfileField::complianceRule()` son los dos puentes; el caso de uso filtra por `AnomalyType::openingIsSuspended()` y el perfil deriva `affectsIncidentDetection()`. **La 3.5 sigue reactivando la regla vaciando una sola lista**, y ahora el asiento, la métrica y la pantalla se enteran solos: hay una prueba que lo ata comparando las dos fuentes.

**Desviación consciente sobre la letra del hallazgo.** Se pidió meter `BreakRequiredAfterHours` en `hasNoConsumerYet()`. No se hizo: ese método significa «no lo lee **ninguna** regla» —jornada semanal, inicio de semana y festivos— y RN-12 sí se evalúa y tiene sus pruebas en 5:59/6:00/6:01. Colapsarlos mentiría en la otra dirección. En su lugar hay `governsSuspendedRule()`, que es lo que distingue «no mueve alertas porque no gobierna nada» de «gobierna RN-12 y RN-12 está suspendida», y el asiento lleva **las dos cosas**: `affects_incident_detection: false` (la verdad de hoy) y `detection_suspended: true` (lo que explica ese `false` a quien lo lea dentro de dos años). El panel tiene un aviso propio junto al campo en vez del genérico.

**IMPORTANTE 2 — un festivo mal escrito apagaba dos reglas legales.** Reproducido: `holiday_calendar = '["navidad"]'` pasaba el filtro del adaptador y hacía estallar el objeto de valor **dentro de `DetectAttendanceAnomalies::handle()`**, que resuelve la política una vez, antes del bucle y sin `try`. La pasada nocturna moría entera —ni RN-10 ni RN-11 en toda la instalación— y `apply-retention` caía por lo mismo. Ahora la lectura descarta lo que no tiene forma de fecha, deja `warning` `product.compliance_profile_calendar_discarded` con `profile_id` y **cuántas** entradas se cayeron (ni fechas ni nombres: ese log sale en el paquete de diagnóstico), y hay prueba de integración que ejecuta **la pasada completa** con el calendario roto y exige que las incidencias RN-10/RN-11 se abran igual.

**IMPORTANTE 3 — concurrencia probada, y da señal.** `ComplianceProfileConcurrencyTest` con `ParallelRequests` + `CommittedDatabase`. **Medido retirando el candado**: la primera falla con «se esperaba 8 y hay 9» —el último escritor devuelve la jornada diaria al valor que leyó, borrando el cambio de otro— y la segunda con «cinco asientos en vez de seis». El riesgo era mayor que en `/settings` porque aquí se escribe la **fila entera**.

**IMPORTANTE 4 — cuatro copias del parseo, una sola ahora.** `Shared/Domain/ValueObject/HolidayCalendar` sustituye a los dos `decodeCalendar()` y a los dos `isRealDate()`. Política elegida y escrita: **al leer se normaliza sin lanzar nunca** (descarta y deduplica, y dice qué descartó) y **al escribir se rechaza con 422** (fecha imposible o repetida). Es la que pidió la revisión y es coherente con «lectura tolerante» de la 5.1.

**Menores.** **5**: `actorUserId` era campo muerto; en vez de borrarlo se hizo real con la migración expand `2026_09_07_100000_track_compliance_profile_updates` (`updated_at` + `updated_by_user_id`, **nullable y sin valor por defecto**: `NULL` significa «tal como se instaló», y rellenarlas diría que alguien revisó el convenio el día de la actualización). `updated_at` se expone en el contrato y en la pantalla; el «quién» se queda en `audit_log`. · **6**: `observe()` sale de la transacción —un contador incrementado dentro de una transacción que se revierte no se deshace— y el docblock ya coincide con el código. · **7**: `409` retirado del `PATCH`. · **8**: `min`/`max` copiados fuera de la plantilla. · **9**: el campo numérico vacío o con decimal se **marca con error visible** y bloquea el guardado en vez de descartarse en silencio (los dos casos que un `<input type="number">` deja llegar de verdad; con letras el navegador ni las acepta). · **10**: `detectionWarning` (es/en) dice ya que la revisión mira los últimos 7 días y que endurecer un umbral puede abrir incidencias de jornadas pasadas dentro de esa ventana. · **11**: renombrar a un nombre existente es `422` traducido, comprobado **bajo el candado** para que no haya ventana entre comprobar y escribir.

**Defecto propio que encontró la verificación por grupos.** `ComplianceProfileConcurrencyTest` usa `CommittedDatabase`, y `compliance_profiles` está en su lista de catálogos de producto: sobrevive al vaciado. Al mutar esa fila y no restaurarla, dejaba un perfil de 11 h/8 h para todo lo que viniera después **en otros ficheros**. Corregido con un `afterEach` que devuelve la fila a los valores de la migración. Es exactamente el fallo contra el que advierte el docblock de `CommittedDatabase`.

**Cambio de comportamiento que conviene conocer:** `CompliancePolicy` ya **no lanza** ante un festivo mal formado —descarta—, y su prueba unitaria lo dice ahora al revés que antes, con el porqué escrito. La comprobación estricta no desapareció: vive en el camino de escritura.

**Ficheros nuevos de esta revisión:** `Shared/Domain/ValueObject/{HolidayCalendar,ComplianceRule,ComplianceRuleSuspension}`, migración `2026_09_07_100000_track_compliance_profile_updates`, `tests/Unit/Shared/Domain/HolidayCalendarTest`, `tests/Feature/Product/ComplianceProfileConcurrencyTest`, `tests/Integration/Product/{CorruptHolidayCalendarDoesNotBlockDetectionTest,TrackComplianceProfileUpdatesMigrationTest}`.

**Verificación tras las correcciones:** `make quality` en verde (Pint **1165 ficheros PASS**, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 0 sin cubrir**, api-lint 0, sh-lint 0) · `make test-unit` **1136 en 3,67 s** · `--group=RF-PD-07` **190** · `make test` **2568 / 11997** (403 s) · `qa:traceability --check` sin huecos y matriz regenerada · `docs:consistency` sin divergencias · `type-check` verde en las tres SPA · panel **277 unitarias**, `lint` y `prettier` limpios · las dos migraciones aplicadas, revertidas y reaplicadas con `fichaje_migrator`, cada una con su prueba · `compliance:apply-retention --dry-run` **idéntico** al de antes de la tarea · criterio de aceptación revalidado sobre el árbol final (0 → 786 `insufficient_rest`, y **sin** `missing_break`: la suspensión sigue en pie tras moverla a `Shared`). · `make mutate` global **MSI 83,08 %** (273 sin probar, 1339 probados; umbral 80, sube desde el 82,76 % previo a la revisión); sobre los tres ficheros de dominio nuevos o reescritos —`HolidayCalendar`, `ComplianceProfileSnapshot` y `ComplianceProfileField`— medido aparte: **100 % (94/94)**. Sobrevive un mutante en `CompliancePolicy` (`RemoveMethodCall` sobre el `positive()` de la jornada semanal) que es **equivalente**: la invariante «semanal ≥ diaria» ya lo cubre y ninguna prueba puede distinguirlo sin afirmar el texto del mensaje. Los otros 12 supervivientes de código nuevo están todos en `InvalidComplianceProfileValue`, sobre los **arrays de parámetros de los mensajes traducidos** — la misma familia que el proyecto ya acepta en `InvalidSettingValue` (18) y por el mismo motivo: matarlos exigiría afirmar la prosa literal de cada mensaje.

## Sesión «tarea 5.1: módulo `Product`, configuración y auditoría» (31-08-2026), en `feat/fase-5-productizacion`

**Tarea 5.1 implementada y verificada, sin commitear** (lo commitea el usuario tras la revisión). Cubre `RF-PD-01` y deja enlazado `BrandingProvider` para la 5.8.

**Decisión ejecutada:** el ámbito `site` de `installation_settings` **se ha retirado** con la migración de contracción `2026_09_05_100000_contract_installation_settings_scope` (quita `scope`, `scope_id`, el `CHECK`, la FK a `sites` y los dos índices parciales; deja `one_setting_per_key`). `down()` reconstruye el esquema de la 1.3 y **está probado**. La cascada queda `fila de instalación → valor por defecto del catálogo en código`; la variable de entorno es solo el valor de arranque del instalador.

**Construido.** `Product/Infrastructure/Persistence/{InstallationSetting,EloquentSettingsRepository}`; `Product/Infrastructure/Adapter/{CachedSettingsRepository,DbBrandingProvider,LaravelProductEventPublisher}` y reescritura de `DbOperationalSettingsProvider` sobre `ResolvedSettings` (ya **no** lanza si falta una fila: sin filas rige el valor de serie); `Product/Application/{Command/UpdateSettingsCommand,Port/ProductEventPublisher,UseCase/{GetSettingsHandler,UpdateSettingsHandler}}`; evento `Product/Domain/Event/InstallationSettingChanged` y listener `Compliance/Infrastructure/Listener/RecordInstallationSettingChange` (acción `calculation_setting.changed`, ya existente); HTTP completo en `Product/Http/` (controlador, `UpdateSettingsRequest` con reglas **derivadas del catálogo**, `SettingResource`/`SettingCollectionResource`, `SettingsPolicy` solo `admin`, `SettingsTelemetry`); rutas `GET|PATCH /api/v1/settings` con `ability:settings:*` + `throttle:management`; i18n `lang/{es,en}/settings.php`.

**Decisiones que conviene conocer:**
1. **Caché** en Redis del mapa de filas guardadas (no del objeto resuelto: un VO serializado sobrevive al despliegue que cambia su clase, y eso rompería el camino de fichaje en casa del cliente). Clave versionada `kronoqr:product:installation_settings:v1`, TTL de seguridad 300 s, invalidación **dos veces**: al escribir y en `afterCommit`.
2. **Un asiento por clave cambiada**, no uno por petición: un `affects_worked_hours` único para un conjunto mixto perdería el matiz para el que existe. Un `PATCH` idempotente no escribe fila ni asiento.
3. `MigrationSafetyTest` gana una segunda lista con trinquete propio, `declaredContractions()`: `DROP COLUMN` solo es legítimo en la fase *contract* y el detector no lo puede deducir del código. Entrar en la lista exige que la versión anterior ya no use la columna, plan de despliegue en el docblock y `down()` probado.
4. `SettingsPolicy` deja fuera a `rrhh` y al `auditor`. El auditor obtiene lo que necesita —qué umbral regía y quién lo cambió— de `audit_log` con `audit:read`.

**Enmiendas documentales hechas** (nota «Enmienda 31-08-2026 (tarea 5.1)», sin reescribir la historia): ADR-040 «Decisión» 1 y 6; ADR-017 «Decisión» 1 y «Verificación»; doc 01 §5 (fila `installation_settings` sin `scope`, y **las funcionalidades activas son de la licencia, no de esta tabla**); doc 02 §4 (filas ADR-017 y ADR-040) y Anexo B (`BRANDING_ACCENT_COLOR` añadido); docblocks de la migración 1.3, del puerto `OperationalSettingsProvider` y de `CommittedDatabase`.

**Documentación de cliente nueva:** `docs/cliente/configuracion.md` (los tres sitios donde vive la configuración, las nueve claves con su valor de serie y su consecuencia, cómo se cambia, y «qué hacer si…» para cada fallo previsible). Enlazada desde `operacion.md`.

**Verificado:** `make quality` en verde (Pint, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 6937 permitidas**, api-lint 0, sh-lint 0; Rector informativo 180 ficheros, preexistente). `make test` **2370 / 11446** (394 s). `make test-unit` 1030 en 3,42 s. `--group=RF-PD-01` 103 pruebas. `qa:traceability --check` sin huecos, matriz regenerada. `docs:consistency` sin divergencias. `type-check` en verde en las tres SPA con `schema.d.ts` regenerado (cambio **solo aditivo**). Migración aplicada, revertida y reaplicada sobre la BD de desarrollo; `migrate:fresh --seed` deja la instalación arrancando con las cuatro filas `ATTENDANCE_*` y el resto en `product_default`; `compliance:verify-audit-chain` en verde tras cuatro `PATCH` (4 asientos, el idempotente no escribió).

### Correcciones de la revisión de `revisor-codigo` (31-08-2026, sin commitear)

**Bloqueante — una fila corrupta podía impedir fichar.** La lectura de configuración pasa a ser **tolerante** y la escritura sigue siendo **estricta**. `ResolvedSettings::resolve()` ya no lanza: un valor guardado que su clave no admite se descarta, rige el valor de serie del catálogo y la clave queda anotada en `ResolvedSettings::$invalidKeys` (VO nuevo `InvalidSetting`, con clave de traducción, motivo técnico y `affectsWorkedHours`); la incoherencia de idiomas se resuelve aplicando el **primer idioma disponible**. `with()` / `UpdateSettingsHandler` siguen lanzando. El descarte no es silencioso: viaja en `meta.invalid_keys` de `GET /settings` y deja un `warning` `product.settings_anomaly` agrupado por ventana (puerto `SettingsAnomalyReporter`). `GetSettingsHandler` es ahora el **único punto de resolución** y lo usan también los dos adaptadores.

**Importante — carrera entre dos `PATCH`.** `UpdateSettingsHandler` lee dentro de la transacción, tras `pg_advisory_xact_lock(5010001)`, y por `storedValuesForWrite()` (método nuevo del puerto, sin caché). Medido: sin candado, seis escritores concurrentes dejan **cinco** asientos en vez de seis.

**Importante — `source` mentía en cuatro claves.** La migración de contracción retira la siembra de la 1.3 **que nadie ha tocado** (`updated_by_user_id IS NULL` y valor idéntico al de serie); `down()` la repone, porque el adaptador anterior exige que exista. Una instalación limpia tiene la tabla vacía y las nueve claves en `product_default`.

**Importante — mensajes de dominio en castellano dentro de `Domain/`.** Las excepciones llevan mensaje técnico en inglés más `translationKey` + `parameters`; el borde resuelve con `ProblemDetails::translated()` en el idioma negociado. Claves nuevas en `lang/{es,en}/settings.php` → `errors.*`.

**Importante — documentación que prometía lo que llega en 5.8/5.9.** `docs/cliente/configuracion.md`, el catálogo `SettingKey` y el contrato dicen ahora que las claves `BRANDING_*` y `LOCALE_*` **se guardan y se auditan y todavía no se pintan**, y que el aviso de `doctor` llega después. `.env.example` queda con **un solo bloque** de marca, el que el código lee hoy (`BRANDING_NAME`, `BRANDING_LOGO_PATH`, `BRANDING_ACCENT_COLOR`); se retiró el bloque duplicado y muerto de `BRANDING_APP_NAME`. Sin renombrar nada.

**Menores.** `scoped()` en lugar de `singleton()` para los cuatro servicios que memoizan por petición · prueba de contrato que ata `SettingKey::cases()` con el `enum` del esquema y con `propertyNames` · `Request` sin uso fuera de `SettingsController::show()` · «exactamente» suavizado en el `down()` (las columnas vuelven al final de la tabla) · contador `installation_setting_changes_total{affects_worked_hours}` (puerto `SettingsMetrics` + `RedisSettingsMetrics`), añadido al catálogo del doc 02 §8.2 · la etiqueta `RF-PD-08` retirada de la prueba del puerto de marca (ese requisito lo cumple la 5.8).

**Configuración nueva:** `PRODUCT_SETTINGS_ANOMALY_WINDOW_SECONDS=300` (`config/product.php`) — cada cuánto se repite el aviso de configuración inaplicable. Se lee en cada fichaje: sin agrupar serían 50 avisos/s.

**Verificación tras las correcciones:** `make quality` en verde (PHPStan 9 **0 errores**, Deptrac **0 / 6981**, api-lint 0, sh-lint 0) · `make test-unit` **1036 en 3,84 s** · `--group=RF-PD-01` **124** · `make test` **2391 / 11563** (402 s) · `qa:traceability --check` sin huecos · `docs:consistency` sin divergencias · `type-check` verde en las tres SPA con `schema.d.ts` regenerado (aditivo) · `migrate:fresh --seed` deja la tabla **vacía** y las nueve claves en `product_default` · `compliance:verify-audit-chain` íntegra tras cuatro `PATCH` · `migrate:rollback` repone la siembra y `migrate` la vuelve a retirar.

**Pendiente / diferido:**
- **5.8:** migrar `BrowsershotCardRenderer` y `CsvLegalExportWriter` de `config('branding.*')` al puerto `BrandingProvider` (hoy siguen leyendo el `.env`), decidir el renombrado `BRANDING_NAME` → `BRANDING_APP_NAME`, y llevar `APP_SUPPORTED_LOCALES` a `LOCALE_AVAILABLE`. Hasta entonces, la marca y los idiomas **se guardan y se auditan pero no se aplican**, y así lo dicen el contrato, el catálogo, `.env.example` y la guía de cliente.
- **5.9 (`doctor`):** enseñar `meta.invalid_keys`, avisar si `.env` y base de datos difieren, y comprobar que `BRANDING_LOGO_PATH` existe. Los tres son promesas retiradas de la documentación hasta que existan.
- **No hay pantalla de configuración en el panel**: la tarea 5.1 es solo backend y contrato. La UI llega con 5.8 (marca) y 5.5 (asistente).
- Una fila editada con `psql` tarda hasta el TTL de la caché (300 s) en verse. Es el comportamiento documentado; la invalidación explícita solo cubre lo que pasa por la API.

## Sesión «arranque de la Fase 5» (31-08-2026), en `feat/fase-5-productizacion`

**PR #35 fusionada en `main` (`2918b4a`). Fase 5 iniciada** con el prompt del doc 03 §6.5 y el plan `plan implementacion/05-fase-5-productizacion.md`. **Método (decisión del usuario):** una rama para toda la fase, un commit por tarea con CI en cada push, PR al cierre (como la #34).

**Decisiones del usuario para la 5.1 (31-08-2026):**
1. **Precedencia entorno ↔ `installation_settings` (punto no cubierto n.º 1):** manda la base de datos. La variable de entorno (`BRANDING_*`, `COMPLIANCE_PROFILE`) es solo el valor de arranque con el que el instalador (5.4) siembra la primera fila; un `PATCH /settings` siempre surte efecto y queda auditado. `doctor` (5.9) avisa si entorno y base de datos difieren. Aplica también a la 5.8.
2. **Ámbito `site` de `installation_settings`:** se **retira** con migración de contracción (expand/migrate/contract). La cascada queda `fila installation → valor por defecto del catálogo en código`. **Obliga a enmendar ADR-040** (§ «Decisión» punto 6 y la lista de tablas donde `site_id` «se conserva»): la enmienda forma parte de la 5.1.
3. Pendientes de decisión que llegarán tarea a tarea (plan, «Puntos no cubiertos»): 5.2 (valores de serie de `max_weekly_hours`/`week_starts_on`/`holiday_calendar`; retroactividad al cambiar umbral; plazo de purga de `employment_contracts`), 5.3 (avisos de caducidad), 5.4 (códigos de salida; vale de un solo uso 2FA; `backup.sh`/`restore.sh`), 5.6 (formato/caducidad del código), 5.7 (punto de control), 5.9 (`scope` de `support_grants`; formato del paquete), 5.10 (telemetría), portal e incidencias (qué ve cada perfil), 5.8 (`supported_locales` → `installation_settings`).

## Sesión «arranque local y corrección del informe por periodo» (31-08-2026), en `main`

**Entorno local levantado** con el procedimiento ya documentado (sesión del 27-08): `make up` para backend y servicios; las tres SPA desde el host con `npm run dev` (kiosk 5173, admin 5174, portal 5175), porque los contenedores `node-*` siguen estructuralmente rotos (ADR-036) y se dejan parados. La BD local se había resembrado: el dispositivo `quiosco-pruebas` ya no existe; se emitió llave (`IssueDeviceToken` por tinker) para «Entrada de personal» (id 1) y se pegó en `localStorage['kronoqr.kiosk.device_token']` del navegador del quiosco. Sin llave, el quiosco muestra «Sin conexión» permanente (401 → `reachability=false`, por diseño).

**PR #35 abierta contra `main`** (<https://github.com/BorjaPeonSaiz/KronoQR/pull/35>) con los 9 jobs en verde (run 33377053542). `gh` tiene ya sesión en esta máquina (`gh auth login` hecho el 31-08-2026). **Siguiente acción: merge de la #35**; después, Fase 5 (orden real 0→1→2→5→3→4).

**Rama `fix/api-locale-and-boolean-query`, dos commits.** El primero (`dd13b8f`) es el booleano de abajo. El segundo es la **negociación de idioma de la API**: antes no existía (`App::setLocale` solo en pruebas) y todo salía en `APP_LOCALE`, con lo que el panel mostraba los mensajes de validación en inglés. Piezas: `App\Http\Middleware\NegotiateLocale` (grupo `api`, `Accept-Language` acotado a `app.supported_locales` ← `APP_SUPPORTED_LOCALES=es,en`, respaldo `app.locale`); alias de ruta `locale.installation` (`UseInstallationLocale`) en las **cinco rutas que generan documentos** (`/reports/period/export`, `/reports/legal-export`, `/me/export`, `credentials/print` y `print-batch`), que restaura el idioma de la instalación capturado antes de negociar — los ficheros no cambian de idioma, la decisión de `CsvDialect` se mantiene; `lang/{en,es}/validation.php` publicados con `attributes` para todos los campos de la API y la clave `unknown_field` que ahora usa `RejectsUnknownInput`; `web-kit` manda `Accept-Language` (`setLocaleProvider`, atado en `main.ts` de panel y portal al *locale* de vue-i18n); `ErrorNotice` acepta `fieldLabels` y el informe los pasa («Hasta: …» en vez de «to: …»); párrafo «Idioma» en `info.description` del contrato. **Trampa descubierta:** `Request::create()` de Symfony (el *helper* `Api` de pruebas) añade `Accept-Language: en-us,en;q=0.5` por defecto; el *helper* lo anula (`''`) para que el caso neutro sea «sin cabecera». Pruebas: `tests/Feature/Http/LocaleNegotiationTest.php` (7 casos, `RF-PD-01`), `RegisterScanTest` manda `es` para el campo desconocido, `http.spec.ts` y `PeriodReportView.spec.ts`. **Pendiente de la 5.8:** llevar `supported_locales` a `installation_settings`. Un fallo aislado y no reproducible de `deja constancia en audit_log` (`orderByDesc('id')->first()` sobre `audit_log`) apareció en una tanda de seis ficheros y no en las repeticiones: si vuelve, mirar si los asientos escapan a la transacción de `RefreshDatabase`.

**Bug corregido (commit `dd13b8f`):** marcar «Contar los días con turno abierto» en el informe de horas por periodo devolvía `422` («include_open_shifts must be true or false»). Causa: el contrato declara el parámetro `type: boolean` en `query`, `web-kit` lo serializa como `include_open_shifts=true` y la regla `boolean` de Laravel no acepta esa cadena; la prueba existente mandaba `'1'`. Es el **mismo fallo que ya se arregló a medida para `pending`** en `IndexCredentialStatusRequest`. Arreglo: trait compartido `App\Http\Requests\NormalisesBooleanQuery` (deduce los campos `boolean` de `rules()` y normaliza con `filter_var`, como `RejectsUnknownInput`), usado por `DescribesPeriodReport` (consulta + exportación) y por `IndexCredentialStatusRequest` (que pierde su `prepareForValidation` propio). Pruebas nuevas en `PeriodReportTest` (`true/1/yes/on` aplican el criterio; `false/0/no` no; `maybe/2/''` → 422) y en `PeriodReportExportTest` (`true` descarga). Pint, PHPStan, Deptrac, trazabilidad y las tres suites afectadas (76) en verde. **Siguiente acción:** commit `fix(reporting)` y PR.

**Pendiente detectado (no tocado):** los mensajes de validación llegan al panel en inglés (no existe `backend/lang/es/validation.php`; solo hay `es/{incidents,legal-export,personal-record,reports}.php`). El panel los muestra tal cual en «Hay datos que revisar». Decidir si se publican y traducen los mensajes de Laravel o si el panel traduce por clave de campo.

## Sesión «cierre de la Fase 2» (31-08-2026), en `feat/2.6-incident-detection`

**CIERRE DE FASE 2 EJECUTADO** (procedimiento del plan 04, l. 692-731). Puertas deterministas: cadena de auditoría íntegra (1338 asientos), reconciliación 14 729 jornadas / 0 divergencias, trazabilidad **Fase 2: 30/30** con `current_phase=2`, mutación global **MSI 84,21 %** (umbral 80). Cuatro revisores: `devops-observabilidad` (sin bloqueantes; correcciones en `61b0c78`), `revisor-codigo` (sin errores de corrección; refactors en `8d34133`), `qa-testing` (huecos de cobertura cerrados en `c90e33b`), `seguridad-cumplimiento` (2 bloqueantes resueltos en `d5f1f2e`: runbook de brecha 72 h y retención de `employment_contracts`). El commit de cierre sube `current_phase` 1→2 y regenera matriz y CHANGELOG.

**Pendiente del usuario (decisiones abiertas):**
- Vale de un solo uso para el alta del 2FA — prorrogado a la 5.4 en doc 07 §6, la decisión sigue abierta.
- Prueba de campo de una tarjeta reimpresa (2.12).
- Techo del formato PDF (`docs/verificacion-manual.md`; el aviso de trazabilidad sobre `PeriodReportPdfSealTest` saltada es este mismo asunto).
- Plazo de purga de `employment_contracts` con la asesoría laboral (5.2); hasta entonces se conservan.
- Versión para la etiqueta: propuesta **2.1.0** al integrar en `main`.

**Deuda anotada en el cierre, no silenciada:**
- **RNF-D-03, mitad de colas:** el fallback Redis→base de datos que prometía `.env.example:119` no está implementado; el comentario ya dice la verdad. El trabajo real (driver `failover` y quién consume la cola de respaldo conviviendo con Horizon) es diseño de la Fase 3.
- **La puerta de cobertura (`make coverage`) no corre en CI**; solo mutación y trazabilidad.
- Paso 9 de la tarea 3.2 (alerta de fallos de los comandos nocturnos), ya descrito más abajo.

**Siguiente acción: PR de `feat/2.6-incident-detection` contra `main`.** Después, Fase 5 (orden real 0→1→2→5→3→4).

## Sesión «Fase 2 en cadena: 2.5 a 2.12» (30-08-2026), en `feat/2.6-incident-detection`

**Orquestación de la fase completa sobre una sola rama, una tarea = un commit.** Estado al escribir esto: **2.6, 2.5, 2.7 y 2.8 commiteadas y con CI en verde** sobre `e3ed906` (los 9 jobs). Commits: `ae255bf` 2.6 · `c709a28` 2.5 backend · `4df701c` 2.5 panel · `a560e2d` correcciones de la revisión de la 2.6 (4 importantes + 7 menores de `revisor-codigo`) · `804316d` clientes TS · `1f02dc6` 2.7 · `b08e6c2` 2.8 · `4f23819` asiento `projection.reconciled` · `e3ed906` `AuditLogRow`. **2.10 terminada en el árbol sin commitear** (informe del agente verificado); **2.9 y 2.12 en verificación final** (las cortó el límite de sesión de la API a media tarde; reanudadas).

**Decisiones del usuario (30-08-2026):**
1. **RN-12 suspendida hasta la 3.5** (opción C): la regla queda enunciada, implementada y con unitaria, pero `DetectAttendanceAnomalies::SUSPENDED_UNTIL_DECLARED_BREAK` filtra `missing_break` y la pasada no abre esa incidencia — sobre la semilla pasaba de 546 hallazgos a 1 (`open_shift_expired`). Nota en doc 01 §4 («Sobre RN-12»), prueba de integración que exige cero incidencias y cero avisos, y **la 3.5 la reactiva vaciando la lista**. En el árbol, sin commitear.
2. **El portal del empleado no muestra incidencias** (`GET /me/workdays` → `incidents: []` siempre): la incidencia es revisión interna, no registro. **Revisar con el cliente en la Fase 5** al decidir qué ve cada perfil; si se activa, solo resueltas.

**FASE 2 INTEGRADA Y CON CI EN VERDE.** Head `94cafe5`: los 9 jobs en verde (run 33334128330). Commits de la segunda mitad: `c9f60fe` suspensión RN-12 (decisión del usuario) · `9255788` 2.10 retención · `bdaa057` 2.9 exportaciones · `6b85bc3` 2.12 rotación de clave · `94cafe5` correcciones de la revisión de seguridad de la 2.12 (sin bloqueantes: clave huérfana visible, invariante real en doc 01 §5.2, riesgo de bandeja anotado en doc 07, `.env.example` coherente). **Siguiente acción: el procedimiento de cierre de la Fase 2** (plan 04 l. 692-731): cuatro revisores (`seguridad-cumplimiento` global, `revisor-codigo`, `qa-testing` con `current_phase` → 2, `devops-observabilidad`), `make mutate` global, comprobaciones del cierre y runbooks verificados. Después, PR contra `main` y la Fase 5. Pendiente del usuario: prueba de campo de una tarjeta reimpresa (2.12) y las decisiones de PDF (techo propio del formato, anotado en `docs/verificacion-manual.md`).

*(Párrafo histórico de cuando 2.9/2.12 estaban en curso:)* cuando 2.9 y 2.12 entreguen, pasada completa de calidad y pruebas sin agentes concurrentes (la base `fichaje_test` compartida provoca fallos falsos «relation … does not exist» si dos suites corren a la vez), commits en orden (suspensión RN-12 → 2.10 → 2.9 → 2.12; los ficheros compartidos —`docs/02` Anexo B/C, README de runbooks, `.env.example`, `AuditAction.php`— van con la última tarea que los tocó), **revisión obligatoria de `seguridad-cumplimiento` sobre la 2.12** (bloque C), push único y CI. Después, el procedimiento de **cierre de la Fase 2** del plan 04 (líneas 692-731): cuatro agentes revisores, `current_phase` → 2, y los runbooks nuevos verificados (`divergencia-proyeccion` ✅ 2.7, `turno-abierto-prolongado` ✅ 2.6, `rotacion-clave-qr` y `rotacion-secretos` ← 2.12, `solicitud-derechos-rgpd` ✅ 2.10).

**PHPStan local, resuelto (30-08-2026):** los 11 errores `PersonalAccessToken::$…` de `Identity/*` que la CI nunca reproducía se corrigieron con dos stubs en `backend/tools/phpstan/stubs/` registrados en `phpstan.neon` (`stubFiles`): Sanctum no declara `@property` y Larastan no infiere columnas de un modelo de vendor; el stub de `Carbon` existe porque el validador de stubs no dispara el autoloader y necesita la clase de las fechas declarada en otro stub. Diagnóstico descartado por experimento: migraciones nuevas, caché, `.env`, variables de compose, BD alcanzable, extensión turbo (musl) y `databaseMigrationsPath`. El porqué exacto de que el runner resolviera sin stub queda sin explicar; con el stub, local y CI analizan igual. Si un uso nuevo de Carbon aparece en código analizado, PHPStan pedirá añadir el método a `Carbon.stub`.

**Deuda de la Fase 2 → tarea 3.2 (revisión final, 30-08-2026):** los comandos programados de madrugada devuelven código de salida distinto de cero cuando algo falla (`attendance:reconcile` ante cualquier divergencia, `attendance:detect-incidents` cuando un hallazgo no se pudo abrir), pero **ese código no llega hoy más allá del log del planificador**: no hay `onFailure()`, ni regla de Loki, ni serie de fallos con alerta. Los tres docblocks que afirmaban lo contrario (`DetectAttendanceAnomalies`, `AnomalyScanResult`, `ReconcileProjectionsCommand`) ya dicen la verdad. El trabajo está anotado como **paso 9 de la tarea 3.2** en `plan implementacion/06-fase-3-operacion-y-refuerzo.md` (series `projection_reconciliation_last_failures` e `incident_detection_last_failures` con regla `> 0`, más `onFailure()` y reglas de Loki).

**Notas de la 2.10 (sin commitear, del informe del agente):** purga de `audit_log` vía función `SECURITY DEFINER` `audit_log_drop_sealed_partition()` propiedad del migrador con `EXECUTE` solo para `fichaje_maintenance` (conexión nueva `pgsql_maintenance`, sin credencial en dev/CI a propósito); confirmación `--confirm=PURGAR-<corte>-<hash>` derivada del plan (un informe caducado no se ejecuta); corte estricto (el día exacto del plazo NO se purga); el planificador solo programa el `--dry-run` (lunes 05:10). Deuda que dejó para `qa-testing`: purga con volumen realista, dos particiones consecutivas, partición retrodatada, mutación de `RetentionPolicy`. `DETACH PARTITION` exige ventana de mantenimiento (en `docs/cliente/operacion.md`).

## Sesión «tarea 2.6: detección automática de incidencias» (30-08-2026), en `feat/2.6-incident-detection`

**Tarea 2.6 implementada** (commiteada después como `ae255bf` + `a560e2d`; ver la sesión de arriba). Cubre RF-PR-01 y estrena en dominio RN-10, RN-11 y RN-12; reutiliza RN-07/RN-08 (`ClockingPolicy`) y RN-15 (`ReviewPolicy`).

**Construido.** `Attendance/Domain`: `AnomalyDetectionPolicy` (las seis reglas con el umbral recibido), `AnomalyType`, `DetectedAnomaly`, eventos `AttendanceAnomalyDetected` y `AttendanceReviewCompleted`, y `ReviewPolicy::exceedsSkewTolerance()` (extraído para no duplicar la comparación de RN-15). `Attendance/Application`: caso de uso `DetectAttendanceAnomalies` con puertos **de solo lectura** `WorkDayLedger` y `FlaggedScans` — que no expongan `save()` es la garantía estructural de RN-08. `Compliance`: modelo `Incident`, enums `IncidentType|Severity|Status`, casos de uso `OpenIncident` (transacción + asiento), `NotifyPendingIncidents` y `PublishIncidentMetrics`, adaptadores sobre `incidents`, notificación encolada por correo y métrica textfile. `Product/Infrastructure/Adapter/DbCompliancePolicyProvider` **nuevo** y enlazado (el plan lo daba por hecho y no existía). Comandos `attendance:detect-incidents` (04:30) y `compliance:incident-metrics` (cada 5 min).

**Decisiones que conviene conocer:** tipo nuevo `missing_break` para RN-12 (colapsarlo en `long_shift` haría indistinguible «9 h y media hoy» de «6 h y media sin parar»); severidad por tipo, decidida en `Compliance`; **retroactividad acotada** a `COMPLIANCE_INCIDENT_LOOKBACK_DAYS=7` con los tramos abiertos siempre revisados; idempotencia por índice único parcial `one_open_incident_per_finding` con `NULLS NOT DISTINCT`, no por `SELECT` previo; `AuditableEvent::IncidentLifecycle` es la **octava** familia del bloque D; el aviso al responsable es un resumen por persona y por pasada, sellado con `incidents.notified_at`, y un fallo de correo nunca rompe la detección.

**Pendiente para la 3.5 (RF-AT-12, pausa declarada):** RN-10 solo compara **entre jornadas**. El hueco entre dos tramos de la misma `work_date` —salir a las 15:00 y volver a las 23:00 del mismo día, 8 h de descanso— no se evalúa, porque sin la intención declarada del fichaje (ADR-024) no se distingue una pausa para comer de un descanso entre turnos y alertar de todas convertiría cada jornada partida en un incumplimiento del art. 34.3 ET. Documentado en doc 01 §4 (nota «Sobre RN-10»), en el docblock de `AnomalyDetectionPolicy::insufficientRest()` y fijado con una prueba unitaria que la 3.5 tendrá que cambiar a propósito.

**Pendiente / deuda:** sobre la semilla de desarrollo, RN-12 produce **545 incidencias `missing_break`** (el `VolumeSeeder` genera 90 días de turnos de 8 h continuas sin pausa). Es un verdadero positivo según la regla, pero conviene decidir con el cliente si el umbral de RN-12 debe evaluarse solo cuando exista la pausa declarada de RF-AT-12 (tarea 3.5). La bandeja de la 2.5 necesitará filtro por tipo desde el primer día. `missing_clock_out` y `anomalous_pattern` siguen en el catálogo sin detector, a propósito.

## Sesión «tarea 2.4, backend y cierre» (30-08-2026), en `feat/2.4-live-presence`

**Tarea 2.4 completa en la rama.** Esta sesión hizo el backend, el contrato y los docs (agente `backend-laravel`); la sección de abajo es la del panel, hecha en paralelo por otra sesión. Verificado que encajan: `schema.d.ts` regenerado desde el contrato final **sin ningún cambio**, `type-check` 0 y 219/219 unitarias del panel, y 310 pruebas backend nuevas + contrato + autorización negativa en verde. `make quality` limpio (PhpStan 9 0 errores, Deptrac 0 violaciones) y `make test` **1881 / 9736** según el agente; trazabilidad 1224 Pest + 58 Playwright sin huecos.

**Construido** (`backend/app/Modules/Reporting/`): `GET /api/v1/attendance/live` (ámbito `attendance:read`, rol `manager+`, filtros `department_id`/`status`/`q`, sin paginación, `meta.realtime` con lo que el panel necesita — **cero variables `VITE_*`**, ADR-017); `DatabaseLivePresenceReader` sobre el índice parcial `one_open_shift_per_employee` con el alcance **en la consulta**; difusión por Reverb en canales privados `presence.all` / `presence.department.{id}`, evento `presence.updated`, autorización en `routes/channels.php` + `POST /api/v1/broadcasting/auth` (Bearer); listener `ShouldQueue` con `afterCommit` que **atrapa `Throwable`** para que un Reverb caído no rompa `POST /scan`; `GroupedPersonalDataAccessLog` (asiento de divulgación agrupado por ventana de 15 min, solo para `live_presence`, lista cerrada en `config/compliance.php`); `reporting:presence-metrics` cada minuto → `open_shifts_current{site,department}` y `websocket_connections_active` (API HTTP de Reverb; serie omitida si no contesta); migración `index_clock_in_scans_by_shift_entry` (CONCURRENTLY, revertida y reaplicada); `config/broadcasting|reverb|realtime.php` (Reverb con `accept_client_events_from: none`).

**Decisiones a conocer:** módulo `Reporting` y no `Attendance` (cruza cuatro tablas; 0 cambios en Deptrac); toda corrección/anulación difunde, no solo la de un tramo abierto; **degradación por licencia pendiente de la 5.3** — punto único `RealtimeSubscription::isAvailable()`, riesgo anotado en doc 07; `allowed_origins: *` en Reverb aceptado por diseño (doc 07); `REVERB_APP_SECRET` sin valor en `.env.example`, lo genera el instalador de la Fase 5 (plan 08 paso 5, README E-2).

**Deuda (no bloqueante):** plan de consulta no medido con volumen de producción (BD de desarrollo casi vacía); `open_shifts_current` hereda `service="backup"` del job textfile (comentado en `prometheus.yml`, para `devops-observabilidad` en la 3.2); alertas Prometheus de las dos métricas (3.2); LCP medido con `PerformanceObserver` en el E2E y no con Lighthouse CI; `.env` local lleva un `REVERB_APP_SECRET` de desarrollo (recrear `app` con `docker compose up -d app` si no lo lee).

**CI (corregido el 30-08-2026, sin commit todavía):** las tres ejecuciones de la rama (`86d8e65`, `a1549d0`, `957b1b4`) cayeron en `php-toolchain` y **no era transitorio**: `composer install` lanza `package:discover`, que carga `routes/channels.php`, y `Broadcast::channel()` construye el cliente de Pusher con `REVERB_APP_KEY` = `null` (el runner no tiene `.env`; `broadcasting.default` es `reverb`). Peor que la CI: con `BROADCAST_CONNECTION=reverb` y la clave sin declarar, **cualquier arranque de la aplicación muere** — `POST /scan` incluido — lo que incumple las reglas duras 15 y 19. Arreglo: `(string) env(...)` en `key`/`secret`/`app_id` de `config/broadcasting.php` (cadena vacía → `RealtimeSubscription` anuncia `enabled: false` y el panel sondea, que es la degradación de diseño) y prueba `tests/Feature/Reporting/BootWithoutReverbCredentialsTest.php` (subproceso `artisan about` sin credenciales; falla sin el casting). Verificado: prueba en rojo antes y en verde después, `Feature/Reporting` 126/126, Pint, PHPStan 9 completo 0 errores, trazabilidad sin huecos (1225 Pest).

**Siguiente acción:** commitear el arreglo de la CI (`fix(reporting): el arranque no depende de las credenciales de Reverb`), comprobar que la CI queda en verde y abrir la **PR de la 2.4 contra `main`** (#28/#29/#30 ya están mergeadas; la base `2fb66f5` está en `main`, no hace falta rebase) y mergear con *merge commit*. Después, camino crítico de la Fase 2: **2.6** (detección automática de incidencias) o **2.8** (informes). `current_phase` sigue en `1` hasta cerrar la fase.

## Sesión «tarea 2.4, frontend» (29-08-2026), en `feat/2.4-live-presence`

**Reparto acordado con el usuario:** otra sesión hacía el backend de la 2.4 en este mismo árbol; esta sesión hizo **solo el panel** (`frontend-admin/`), sobre el contrato de `/api/v1/attendance/live` y el webhook `presence.updated` que aquella dejó en `docs/api/openapi.yaml`. Commit `feat(admin): presencia en vivo…` con ficheros disjuntos del backend; el backend, el contrato y los docs los commitea la otra sesión.

**Construido** (`frontend-admin/src/features/live/`): `live.api.ts`; `realtime/pusherClient.ts` — **cliente propio del protocolo Pusher 7** (saludo, firma de canales privados en `auth_endpoint` con el Bearer, `pusher:ping/pong`, reconexión con espera creciente hasta 30 s) en vez de `laravel-echo` + `pusher-js`, porque cabe en 200 líneas y el E2E lo simula fielmente con `routeWebSocket`; `presence.store.ts` — foto + `presence.updated` aplicados fila a fila con descarte de mensajes reordenados por `occurred_at`, recuentos ajustados, **sondeo cada `poll_interval_seconds` cuando el canal no está `live`** y parada del sondeo al recuperarlo (con foto nueva), **reloj del servidor** (`meta.generated_at` + delta local, nunca hacia atrás); `LivePresenceView.vue` — filtros al servidor (departamento, `present`/`absent`, búsqueda con debounce), recuentos, indicador `data-test="transport"` con `role="status"` y `data-degraded` (en vivo / no disponible: cada N s / desactivado en la instalación); `PresenceTable.vue` — virtualizada con `@tanstack/vue-virtual` (dependencia nueva, 3.13) a partir de 80 filas, horas en la zona del centro, tiempo transcurrido. Ruta `/live` (`attendance:read`), entrada «Presencia» en la navegación, y **`live` como última sección de respaldo de la guarda**: es la primera que alcanza un `responsable_departamento` (que no tiene `employees:*`) y el `auditor` sigue entrando por Inspección. i18n ES/EN (36 claves).

**Verificado:** `type-check` 0, `lint` limpio, **219 unitarias** (+15: cliente Pusher 8, store 7… y la guarda), `build` (bundle 114 KiB gzip, `renderQrPath` sigue aparte), **23/23 E2E** (+6: dos pestañas con Reverb simulado → cambio sin recargar y sin consultar la API; Reverb caído → aviso y ≥ 3 sondeos con intervalo 1 s; tiempo real desactivado; filtros al servidor y 05:00 UTC → 06:00 Madrid con el navegador en Canarias; **500 filas con LCP < 1,5 s (RNF-P-04) y < 120 filas en el DOM**; axe). `qa:traceability --check` lee las etiquetas nuevas (58 Playwright).

**Decisiones / deuda:** el LCP se mide con `PerformanceObserver` dentro del E2E, no con Lighthouse CI como sugiere el plan (`@lhci/cli` sería otra herramienta y otra etapa de CI; si se quiere, es una tarea de `devops-observabilidad`). Los `schema.d.ts` de las tres SPA se regeneraron desde el contrato en disco y van en este commit; el `openapi.yaml` va en el commit del backend. `router.spec` cambió el ejemplo de «sin sección alcanzable» de `attendance:read` a `incidents:*`. Pendiente de la otra sesión: commit del backend, `make quality`/`make test`, CHANGELOG, PR de la 2.4 contra `main` (ya no está apilada) y merge con *merge commit*.

## Sesión «tarea 2.1: 2FA obligatorio y ámbito por departamento» (29-08-2026), en `feat/2.1-auth-2fa-rbac` → PR #30

**Tarea 2.1 de la Fase 2 implementada** (el bloque de abajo es el informe del agente de backend; el estado final está al final de esta sección). Cubre RF-ID-01 completo, RF-ID-02, RF-ID-03, RS-04, RS-05, RS-06 y RS-13.

**Verificación real, no supuesta:** `make quality` en verde (sh-lint 0, api-lint 0, PHPStan 9 **0 errores**, Deptrac **0 violaciones / 4581 permitidas**; Rector 129 ficheros en dry-run, umbral informativo). `make test` **1786 pruebas / 9396 aserciones** (216 s). `make traceability-check` y `make docs-consistency` sin divergencias. `type-check` en verde en las tres SPA con `schema.d.ts` regenerado; unitarias del panel 191/191. Las dos migraciones aplicadas y revertidas con el rol `fichaje_migrator`.

**Decisiones tomadas que conviene conocer antes de revisar:**

1. **`POST /auth/login` gana un `202`** con `TwoFactorChallenge` (`challenge_token`), en vez de un `oneOf` sobre el `200`: con una sola forma, un cliente que leyera `token` guardaría como sesión un token que no autoriza nada.
2. **Dos endpoints que el Anexo B no listaba**, `POST /auth/2fa/enrol` y `/2fa/confirm`. Sin ellos RS-06 es inaplicable: una cuenta nueva de `rrhh` no tiene forma de obtener su TOTP. Documentado en la tabla de notas de contrato del Anexo B.
3. **Ámbito nuevo `employees:read`** para reconciliar el Anexo B («`GET /employees` es manager+», incluye al responsable) con el §7.3 del doc 02, que no le daba ningún ámbito de plantilla. `GET /employees` y `GET /employees/{uuid}` pasan a exigirlo; escribir sigue siendo `employees:*`.
4. **RS-06 obliga a tres roles y no a cuatro.** El doc 02 §7.3 escribía «Sesión + 2FA» también para el responsable; manda el doc 01. Resuelto como configuración (`IDENTITY_2FA_REQUIRED_ROLES`) y anotado en el §7.3.
5. **Retirar un segundo factor es `identity:2fa-reset`** (consola, auditado) y no un endpoint. **Sin códigos de recuperación**: deuda anotada.
6. **`current_phase` sigue en `1`** en `backend/config/quality.php`. Ponerlo a `2` ahora exigiría prueba a los 20 requisitos de la Fase 2 y dejaría la CI en rojo durante las 8 tareas restantes; se sube al **cerrar** la fase, como dice el propio comentario del fichero.

**Anomalía reproducible que conviene no tocar:** el nombre del fichero `backend/database/migrations/2026_08_30_100100_grant_read_ability.php` es corto a propósito. Con el nombre largo original, PHPStan pasaba de 0 a 13 errores en ficheros que la rama no toca (Larastan dejaba de reconocer las propiedades de `PersonalAccessToken`). Aislado cambiando solo el nombre, con la caché borrada; causa no encontrada. Está anotado en el docblock de la migración.

**Pendiente de esta tarea:**
- **Pantalla del segundo factor en `frontend-admin`** (`frontend-panel`) y su **E2E** `tag: ['@RF-ID-01', '@RS-06']`, que el plan exige y no está.
- **Revisión de `seguridad-cumplimiento`** sobre los bloques C y D de `/revision-cumplimiento` (paso 9 de la tarea, obligatorio).
- Códigos de recuperación de 2FA (hash y un solo uso) — hoy la única salida ante un teléfono perdido es la consola.
- `GET /credentials/status` **no** lleva filtro por departamento: el responsable no tiene `credentials:*` y no llega, así que el filtro sería una rama inalcanzable. Si algún día se le concede, hay que añadirlo.

**Nginx:** la zona `auth` ya estaba a 5 r/m y `location ^~ /api/v1/auth/` cubre los tres endpoints nuevos. **No se ha tocado** (paso 6 de la tarea, ya satisfecho por la 1.7).

**Estado al cerrar la sesión (29-08-2026): tarea 2.1 completa y en PR #30** (`feat/2.1-auth-2fa-rbac`, apilada sobre #29 → #28; mergear en ese orden, GitHub reapunta la base). Cuatro commits: `feat(identity)!` (backend, `45c1bb1`, CI en verde), `feat(admin)` (pantalla del segundo factor en tres pasos + 17/17 E2E; `renderQrPath` movido a `web-kit` y opción `token` del cliente HTTP), `security(identity)` (los **10 hallazgos** de la revisión obligatoria de `seguridad-cumplimiento` aplicados, cada uno con prueba que falla sin el arreglo), y `docs(changelog)`. **Verificado por mí, no solo por los agentes:** `make quality` limpio, `make test` **1816 / 9511**, trazabilidad 1165 Pest + 52 Playwright sin huecos, `docs:consistency` sin divergencias; admin 200 unitarias + 17/17 E2E, web-kit 142, portal 69, quiosco 309 + 37/37 E2E y bundle 97,2/250 KiB.

**Hallazgos de seguridad que conviene recordar** (los dos altos eran defectos reales del limitador, no de diseño): `throttle:auth` en `/auth/2fa/*` degeneraba en un cubo global de 5 r/m para toda la instalación (la clave por cuenta leía un `email` que esos cuerpos no llevan) → zona `2fa` por dueño del reto; el bloqueo por códigos fallidos era por cuenta+IP y se reiniciaba rotando origen → por cuenta. Además: zona `management` (120 r/m por cuenta+origen) en las rutas que llegan a `ScopeGuard`; `GroupedAuthorizationJournal` agrupa denegaciones repetidas (60 s) detrás del puerto sin quitar el asiento; `#[SensitiveParameter]` + `zend.exception_ignore_args=On`; el alta respeta el bloqueo; `identity:2fa-reset` cierra todas las sesiones; `config/sanctum.php` con `guard=[]`; señuelo con prueba de simetría. Doc 07 registra el riesgo residual de auto-alta con solo la contraseña.

**Decisiones del usuario pendientes (anotadas también en la PR):**
- ¿El `responsable_departamento` debe ver el tablero de credenciales de su gente? Hoy no llega (sin `credentials:*` por Anexo B ni §7.3); si sí, conceder el ámbito y filtrar por departamento.
- Códigos de recuperación de 2FA (hoy la única salida ante un teléfono perdido es `identity:2fa-reset` en consola).
- Vale de un solo uso en `identity:create-user` para cerrar la ventana de auto-alta (doc 07).

**Deuda anotada por los agentes (no bloqueante):** `revokeAllFor()` no cuenta sesiones cerradas en el asiento (`arquitecto-dominio`, campo en `TwoFactorReset`); `qa-testing`: concurrencia de `/auth/2fa/verify` y de la agrupación (Redis real), `make mutate` sobre `VerifyTwoFactorHandler`, integración de `GroupedAuthorizationJournal` con Redis; `permission.changed` no se deja en la migración `grant_read_ability` (docblock); `clearAnnouncement()` al desmontar `LoginView`; `.env` local de desarrollo no tiene las variables nuevas (todas con valor por omisión).

**Corrección a mi propio plan:** `current_phase` **se queda en `1`** hasta cerrar la Fase 2 — el comentario de `config/quality.php` dice que se sube «al cerrar cada fase» y que el literal es el registro del cierre; ponerlo a `2` ahora dejaría la CI en rojo durante las 8 tareas restantes.

**Siguiente acción:** mergear #28 → #29 → #30 (CI en verde en #28/#29; la de #30 corre sobre `490852d`). Después, Fase 2 según el camino crítico `2.1 → 2.4 / 2.6 / 2.8` (`plan implementacion/04-fase-2-gestion-y-cumplimiento.md`): 2.4 exige presencia en vivo con Reverb y ahora tiene E2E del panel donde apoyarse. Las tres puertas de hardware de la Fase 1 siguen abiertas y son del usuario.

## Sesión «cierre de la Fase 1 con reservas» (29-08-2026), en `main`

**Pregunta del usuario:** ¿puede darse por cerrada la Fase 1 y pasar a la Fase 2? **Veredicto, verificado con instrumento y no con este fichero:** sí, cerrada en todo lo que el software puede demostrar, con tres reservas de hardware/estimación que no bloquean la Fase 2.

**Evidencia comprobada hoy:** `docs/trazabilidad-pruebas.md` con **75/75 requisitos de Fase 1 con prueba** (`current_phase=1`, 1132 pruebas etiquetadas); CI de `main` **en verde en `db978b4`** (9 jobs + simulacro de restauración, leído en la API de Actions con la credencial de git); suite 1725/9183, MSI 84,04 %, cobertura 91,5/85,1 % y >90 % en las tres SPA, bundle 101/250 KiB, axe limpio; auditoría independiente del 27-08 con sus 7 correcciones mergeadas; `v1.0.0` → `v2.0.0`. Las 18 tareas y los cuatro pasos del cierre constan más abajo («Objetivo actual», «Completado»).

**Reservas — las tres puertas que ninguna herramienta cierra** (plan 03 «Cierre de fase», doc 02 Anexo A). Dueño: el usuario (tablet e impresora). Resultado a este fichero cuando se haga:
1. Prueba de resistencia de **12 h en la tablet real** (memoria ≤ 250 MB sin crecimiento, wake lock, IndexedDB bajo presión — la vía del bug de reenvío que 1.9 corrigió).
2. **Tarjeta impresa y plastificada, escaneada** en el quiosco real (QR 34,4 mm; medir `HELD_GAP_MS`/visor de paso).
3. **Recalibración R16**: 166–214 h estimadas contra lo real.

**Contradicción documental corregida en el plan 03:** listaba «p95 < 150 ms» como umbral de cierre de la Fase 1, pero `RNF-P-02`/`RNF-P-06`/`RQ-08` son de **Fase 3** por el Anexo A y `docs/requisitos.yaml` (la propia tarea 1.7 lo decía). Ahora figura como medición informativa; la cifra válida sigue pendiente de una pasada k6 en Linux (en Docker Desktop/Windows ~450 ms, no evaluable). Añadido también un bloque «Estado del cierre» al final del plan 03.

**Orden acordado para arrancar la Fase 2:**
1. **Subida de toolchain del frontend** (ya planificada abajo, «Siguiente acción» de la sesión Trivy) — mejor antes de que 2.4/2.5 añadan pantallas.
2. **Infraestructura Playwright para `frontend-admin`** (copiar la del quiosco; primer recorrido login → ficha → detalle de jornada con `@axe-core/playwright`): cierra el hueco de §9.5 de la Fase 1 (`tests/e2e` solo existe en el quiosco) y deja el carril para 2.4/2.5. Un PR propio.
3. **Abrir la Fase 2 por 2.1** (2FA + RBAC por departamento; `backend-laravel` con revisión de `seguridad-cumplimiento`), raíz del camino crítico `2.1 → 2.4/2.6/2.8`. **Al empezar: `current_phase` → `2` en `backend/config/quality.php`** (lección del ALTO-1) — hoy la trazabilidad da 20 requisitos de Fase 2 sin prueba, que es lo esperado.
4. Decisiones que caben en la Fase 2 sin tarea nueva: revocar la credencial en la baja (`EmployeeOffboarded`, junto a 2.1 o 2.12), `POST /me/logout`, la mitad de aplicación de RF-ID-08 (encaja en 2.1).

**Ejecutado en la misma sesión (tras aprobar el plan):**

1. **Toolchain del frontend → PR #28** (`chore/frontend-toolchain`): Vite 8.2, Vitest 4.1 + coverage-v8, ESLint 10.9, Pinia 4.0 (salta la 3), vue-router 5.3, vue-i18n 11.4 en los cuatro workspaces; `spatie/laravel-permission` 6→8 (nuestras migraciones literales coinciden con los nombres por defecto de la v8). Verificado: `type-check`/`lint`/unitarias/`build` en las cuatro SPA, **37/37 E2E del quiosco** con axe, bundle crítico 97,2 KiB (baja), `make quality` y `make test` **1725/9183**, `npm audit` y `composer audit` a 0, `docs:consistency` sin divergencias. Doc 02 §3.3 y plan 01 con las versiones nuevas. Sustituye a las PR #15/#17/#18/#19/#20/#22/#23/#25 de Dependabot. **Defecto propio encontrado después y corregido en la misma rama** (`fix(deps)`): la importación `./vite.config.ts` con extensión (aviso de Vite 8) no compilaba bajo `tsconfig.node.json` sin `allowImportingTsExtensions` — el `type-check` de las tres SPA estaba en rojo y yo lo había verificado antes de hacer ese cambio.
2. **E2E del panel → PR #29** (`feat/admin-e2e`, apilada sobre #28): Playwright + `@axe-core/playwright` en `frontend-admin` con la disposición del quiosco (build por `vite preview`, API interceptada con formas del contrato, sin backend, navegador en `Atlantic/Canary` a propósito). **10/10 escenarios**: acceso/redirect/rechazo/cierre (RF-ID-01, RF-ID-02), plantilla → ficha → registro horario con corrección y cabecera `Authorization` en cada petición (RF-GP-01, RF-PA-03, RN-13), axe en cuatro pantallas. `make e2e` corre quiosco y panel; `playwright.config.ts` usa `testIdAttribute: 'data-test'` (el panel no usa `data-testid`). Trazabilidad: 45 pruebas Playwright (antes 35). La etapa E2E de la CI sigue siendo el marcador de 3.7.
3. **Fase 2, tarea 2.1 → PR #30**: ver la sección de arriba (backend, panel y revisión de seguridad hechos y verificados).

**Ficheros tocados:** ver las PR #28 y #29; `HANDOFF.md`.

**Siguiente acción:** mergear #28 → #29 → #30; luego 2.4/2.6/2.8 (ver la sección de la tarea 2.1).

## Sesión «Trivy a bloqueante» (29-08-2026), en `fix/postgres-nonroot-image`

**Rama `fix/postgres-nonroot-image`** sobre `main` (`f4f31d4`, merge de la PR #9 con la 2.0.0). Cierra el triaje de Trivy que quedaba pendiente con vencimiento 2026-11-30, sin ampliar excepciones.

**Hecho:**

1. **Imagen de PostgreSQL** (`infra/docker/postgres/Dockerfile`): `USER postgres` como última instrucción y `rm /usr/local/bin/gosu` en el `RUN` de paquetes. Verificado con volumen limpio: `initdb` y los tres scripts de `initdb.d` corren como uid 70, roles `fichaje_app`/`fichaje_migrator`/`fichaje_maintenance` y extensiones creados, `/var/lib/postgresql/data` y `/var/backups/fichaje/wal` de 70:70. Comprobado antes lo que pedía el triaje: `postgres-data` es volumen con nombre en dev y prod (Docker copia la propiedad de la imagen); el bind mount de WAL en prod ya exigía uid 70 antes (el entrypoint solo ajustaba `PGDATA`), solo cambia que ahora el fallo es explícito; `restore-drill.sh` levanta la imagen oficial y los `docker exec … psql -U postgres` son el rol de BD, no el usuario del SO. `infra/docker/.trivyignore.yaml` queda con `vulnerabilities: []` (la entrada `CVE-2025-68121` era del `gosu` eliminado).
2. **Trivy bloqueante** en `ci.yml`: los dos pasos sin `TRIVY_EXIT_CODE=0`, capturando el código antes de escribir el resumen (el desglose por severidad llega al resumen también cuando falla); **paso `PLAZOS` retirado** porque ningún control queda en informe (comentario en su lugar explica cómo recuperarlo del historial). Verificado en local en modo bloqueante: `make trivy-image` **0 hallazgos** en `postgres:ci` y `app:ci`; `trivy fs` **0 hallazgos** (los 4 Dockerfiles, `composer.lock` y `package-lock.json`) sobre una copia del árbol en disco Linux nativo (`git archive` del índice dentro del contenedor de Trivy, como corre en la CI). **`make trivy-fs` sobre el bind mount de Windows no termina** ni en 15 min pese a `--skip-dirs` y `--timeout 10m`: en esta estación se verifica con la copia nativa, no con el objetivo.
3. **Simulacro de restauración en Windows**: `restore-drill.sh` fallaba en Git Bash porque MSYS reescribía `/tmp/copia.dump` a una ruta de Windows antes de `docker exec` → `export MSYS_NO_PATHCONV=1` en el script (inocuo en Linux). `make backup`, `make backup-verify` y `make restore-drill` en verde (24 claves ajenas, 74 375 filas en 23 tablas).
4. **Simulacro en CI (`backup-drill.yml`) fallaba en todas las ramas, `main` incluida**, en el paso de datos: el bucle de espera usaba `pg_isready` por socket Unix, que contesta «listo» durante el servidor temporal de `initdb`, y el `psql` por TCP llegaba al apagarse ese servidor (en `main` ni siquiera llegó a listo en 60 s y nadie volcó los logs). Arreglo: `pg_isready -h 127.0.0.1` (por TCP solo responde el servidor definitivo), 90 s y `docker logs` + fallo si no arranca; el mismo `-h` en el bucle gemelo del job `unit` de `ci.yml`. **El volcado destapó la causa real desde la tarea 1.14:** el workflow arrancaba el clúster con `POSTGRES_USER=fichaje_app` y `02-application-roles.sh` se niega (el superusuario no puede ser el rol de la aplicación), así que el contenedor moría en `initdb`. Ahora el rol de arranque del simulacro es `fichaje_migrator`.
5. **Entorno local**: `make clean` + `make up` + `make seed` hechos; la base tiene **un** centro (`Hotel Marina`). Ojo: `make up` no reconstruye imágenes (`make build` o `compose build postgres`), y `make seed` justo tras `make clean` llega antes de que el entrypoint de `app` termine el `composer install` en el volumen `backend-vendor` vacío (~5 min): esperar a que exista `vendor/autoload.php`.
6. **Docs**: Makefile (comentarios y `##` de `trivy-fs`/`trivy-image`), `docs/runbooks/triaje-hallazgos-seguridad.md` (árbol de decisión con la lección de `gosu`, §2.2, §3 y tabla de estado), doc 02 §9.2 (tres filas: Semgrep comunitario seguía diciendo «informe» aunque ya era bloqueante, Trivy fs, Trivy image). `docs:consistency`, `sh-lint` y suite completa (**1725 pruebas / 9183 aserciones**) en verde.

**Pendiente de esta sesión:**
- **Subida de toolchain del frontend, como tarea propia** (una rama, un PR, `CHANGELOG` y versión; agente `frontend-quiosco` + `qa-testing`): Vite 8 (#23) + Vitest 4 (#22) + `@vitest/coverage-v8` 4 (#17) van juntas; ESLint 10 (#18); y las de runtime de las tres SPA — vue-router 5 (#25), Pinia 2→4 (#19, se salta la 3: leer las dos guías), vue-i18n 11 (#20). Sus checks están en verde, pero eso es lint, tipos y unitarias: exigir `make e2e`, el presupuesto de bundle del quiosco (250 KiB) y axe. Aparte, backend: `spatie/laravel-permission` 6→8 (#15), revisar la guía de actualización por si trae migraciones. Mientras tanto Dependabot seguirá rebasando esas PR; no mergearlas desde la lista.
- **`install.sh` (Fase 5, tarea 5.4)**: crear `${BACKUP_PATH}/wal` en el anfitrión con `install -d -o 70 -g 70 -m 0750` antes del primer arranque; anotado en `compose.prod.yaml` junto al bind mount.
- `make trivy-fs` en Windows: valorar una variante del objetivo que analice `git archive $(git write-tree)` desenpaquetado dentro del contenedor de Trivy (segundos) en vez del bind mount (no termina). Tres líneas de Makefile; no se hizo para no ampliar esta PR.
- `kronoqr-node-admin` y `kronoqr-node-portal` estaban en bucle de reinicio (código 127) antes de esta sesión, con la rama anterior; no se ha investigado.
- Ver la primera ejecución del job `security` con Trivy bloqueante en Actions (los cuatro puntos del bloque «Decisiones pendientes» de la sesión SSDLC siguen vigentes, salvo el (4), que ya no existe).

**Estado en GitHub:** **PR #26 mergeada** en `main` (`a066d39`). Para `6b3540e`: CI **en verde en los nueve jobs** (run 33249214623, con `security` ya bloqueante) y el workflow «Simulacro de restauracion de copia» **en verde por primera vez** (run 33249214676, los 13 pasos ejecutados). `gh` no está autenticado en esta estación: las PR se abren, mergean y leen con la credencial de git (`git credential fill`) contra la API. No cambia `VERSION` (queda en `[Unreleased]`).

**Triaje de las 16 PR de Dependabot** (abiertas al activarse `dependabot.yml` con el merge de la #9):
- **Mergeadas** (menores/parche, CI verde): #16 grupo npm (la mergeó el usuario), #14 grupo composer, #11 `nginx-unprivileged` 1.27→1.31, #10 `actions/upload-artifact` 4→7 (tras `@dependabot rebase` para que corriera con el simulacro arreglado).
- **Cerradas con `@dependabot ignore this major version`** y motivo en el comentario: #13 PostgreSQL 18 (la 17 está fijada por digest; subir la mayor del servidor exige plan de `pg_upgrade` y ADR), #12 Node 26 y #21 `@types/node` 26 (24 es la LTS hasta octubre de 2026), #24 TypeScript 7 (rompe `type-check`; compilador reescrito). Un `ignore` puesto por comentario no aparece en `dependabot.yml`; para revertirlo, la doc de GitHub («Managing pull requests for dependency updates» → *ignore conditions*) es la referencia.
- **Abiertas a propósito, no mergear desde la lista** (ver Pendiente): #23 Vite 8, #22 Vitest 4, #17 `@vitest/coverage-v8` 4, #18 ESLint 10, #25 vue-router 5, #19 Pinia 2→4, #20 vue-i18n 11, #15 `spatie/laravel-permission` 6→8.
- Creadas en el repo las etiquetas que `dependabot.yml` declara y no existían (`dependencies`, `backend`, `frontend`, `ci`, `docker`): Dependabot lo avisaba en cada PR.

**Siguiente acción:** la subida de toolchain del frontend (Pendiente, primer punto).

## Sesión «un centro por licencia» (29-08-2026), en `feat/ssdlc-pipeline`

**Decisión del usuario:** el producto no es multi-centro. **Un centro de trabajo por instalación y por licencia**; una cadena compra una licencia —y opera una instalación— por hotel. Elegido el enfoque B (el centro sigue existiendo como entidad, pero hay exactamente uno) frente a eliminar el concepto. Registrado en **ADR-040** (matiza ADR-012, 017, 018 y 028; se aplica en `/api/v1` sin abrir v2 porque no hay instalación desplegada y la superficie del quiosco no cambia). Doc 05 deja de prometer «multi-centro» y «consolidación entre centros».

**Hecho (commit `feat(workforce)!`, cambio incompatible del contrato de gestión):**

1. **Contrato:** `/api/v1/sites` y `/sites/{id}` → `GET` y `PATCH /api/v1/site` (singular, sin alta ni lista). Ningún `site_id` en filtros, cuerpos ni respuestas (plantilla, departamentos, credenciales, lote de impresión, tramos del registro). `summary` del tablero de credenciales pasa de lista por centro a **un objeto** `CredentialCoverage`. `api-lint` 0 problemas; `schema.d.ts` regenerado en las tres SPA.
2. **Backend:** migración `2026_08_29_120000_enforce_single_site` (índice único `sites_single_row_uidx` sobre `(true)`; **se niega a migrar una base con más de un centro**: en desarrollo hay que hacer `make clean` y resembrar, la semilla ya crea uno solo, `Hotel Marina`). `SiteRepository::installationSite()`, `CreateSiteHandler` lanza `SiteAlreadyConfigured` (409), `UpdateSiteHandler` sin id, `SitePolicy` sin `create`/`viewAny`. Puerto nuevo `Shared/Application/Port/InstallationSiteProvider` + VO `InstallationSite` + excepción `InstallationSiteMissing` (409, «completa la puesta en marcha»), adaptador en `Workforce/Infrastructure/Adapter`. Alta de empleado y de departamento resuelven el centro en el handler; `DepartmentNotInSite` y `StoreSiteRequest` eliminados; `Employee::updateProfile` sin `siteId`. `EmployeeCardDirectory::activeProfiles(?string $employeeUuid)`; `CredentialStatusBoard` calcula **una** cobertura (con el centro del proveedor, también con cero filas) y `CredentialMetrics::recordCoverage(SiteCredentialCoverage)`; las métricas conservan las etiquetas `site`/`site_name`. `credentials:status` y `credentials:print-batch` sin `--site`. `IndexCredentialStatusRequest` ahora rechaza campos desconocidos.
3. **Pruebas backend:** `WorkforceFixtures::site()` es idempotente (devuelve el centro existente). Reescrita `SiteAndDepartmentCrudTest`; nuevas `SingleSiteSchemaTest` (integración, índice) y `SiteConfigurationTest` (unitaria, handlers con repositorio en memoria); retiradas las que afirmaban una frontera entre centros (padrón de otro hotel, departamento de otro centro, `employee_uuid`+`site_id`). `make quality` en verde (Pint, PHPStan 9, Deptrac); 495 pruebas de los ficheros afectados en verde y suite completa **1725 pruebas / 9183 aserciones** (280 s). Migración verificada en ambos sentidos sobre `fichaje_test` (`migrate:rollback --step=1` + `migrate`).
4. **Panel:** sin selector ni filtro ni columna de centro (alta, plantilla, ficha, tablero de credenciales); la zona horaria sale de `GET /site`; el tablero muestra un único recuento con el nombre del centro; `credentials.batch.explanation` sin `{scope}`; claves i18n `site`/`siteAll`/`allSites` eliminadas en `es`/`en`. `type-check`, `lint` y 80 specs afectadas en verde; portal (fixture sin `site_id`) type-check + 69 specs en verde; quiosco type-check y lint en verde.
5. **Documentos:** doc 01 (§1, §2.3, RF-PA-02, RF-IN-02, RF-GP-01, RF-ID-03, RF-PD-03, RF-PD-04, §5.5 `sites`/`license`, Fase 4, Anexo B), `docs/requisitos.yaml` (títulos), doc 02 (fila ADR-040, §1.6, §7.3, semilla, Anexo C), doc 05 (gestión, informes, filtros, agregados, responsable, asistente, licencia, evolución, tablets), plan 04 (2.1, 2.2), plan 05 (5.1 cascada, 5.3 límites, 5.5, 5.6), plan 07 (4.e retirada). `docs:consistency` y `qa:traceability --check` en verde; `docs/trazabilidad-pruebas.md` regenerado.

**Cerrado después (mismo día):** **`VERSION` = 2.0.0** y `CHANGELOG.md` con la entrada `[2.0.0] - 2026-08-29` (`changelog-check VERSION=2.0.0` en verde). La etiqueta `v2.0.0` se pone sobre `main` tras el merge, como con la 1.2.0 (`git tag -a v2.0.0 -m 2.0.0 && git push origin v2.0.0`). **CI:** los dos jobs en rojo tenían causa propia y están corregidos (commit `fix(ci)`): (1) tres pruebas del rastro de autenticación llamaban a `VerifyAuditChain`, que escribe métricas *textfile* en `/var/backups/fichaje/metrics`, no escribible en el runner → `phpunit.xml` fuerza `METRICS_TEXTFILE_PATH=storage/framework/testing/metrics`; (2) `make sast-community` hacía eco de la orden dentro del JSON que la CI parsea y `make` convertía el 1 de Semgrep en 2 → receta con `@` que absorbe el 1, la CI cuenta hallazgos en el JSON. **Triaje de Semgrep hecho** (9 hallazgos: 4 `dependabot-missing-cooldown` corregidos con `cooldown: 7`, 5 de Nginx con `# nosemgrep` justificado) y **Semgrep comunitario ya es bloqueante** (fuera de `PLAZOS`). Los logs de la CI se leyeron con la credencial de git almacenada (`git credential fill`), no con `gh`.

**Pendiente de esta decisión:**
- ~~**Entorno de desarrollo:** la base local tiene 3 centros de la semilla anterior; `make clean` + resembrar antes de `migrate` (la migración lo avisa).~~ Hecho en la sesión «Trivy a bloqueante».
- Tarea 5.1: decidir si retira el ámbito `site` de `installation_settings` (columna y `CHECK` se conservan). Tarea 5.5: el asistente crea **el** centro con `CreateSiteHandler`. Tarea 5.6: `kiosk:pairing-code` sin argumento de centro.
- Métricas `{site,department}` del §8.2 de doc 02 (Fase 2/3): la etiqueta `site` será constante; decidir al instrumentar si se conserva por compatibilidad de paneles.
- E2E del panel (`frontend-admin/tests/e2e` no existe) sigue pendiente; el quiosco no cambió.
- ~~**CI en GitHub:** el run 25 (commit `3eae102`, antes de este cambio) **falló** en dos jobs: «Dependencias y SAST» en el paso *Semgrep comunitario* (el que estaba en modo informe: un código ≥ 2 significa que la herramienta no pudo terminar, probablemente al resolver `p/php` y compañía en el runner) y «③ Unitarias + Mutación» en el paso *Pest — contrato OpenAPI y feature*. Los logs de los jobs exigen `gh auth login` (la API pública devuelve 403); el run 26 (`dc1cca9`) quedó en cola. **Siguiente acción:** autenticar `gh`, leer esos dos logs y corregir antes de abrir la PR.~~ Corregido (ver arriba); el run 33244318610 del commit `c7f5c1f` esta **en verde en los nueve jobs**. **Siguiente accion:** abrir la PR de `feat/ssdlc-pipeline` a `main` y, tras el merge, etiquetar `v2.0.0`.

## Sesión SSDLC (28-08-2026): seguridad en el pipeline, rastro de autenticación, ATT&CK y SAMM

**Rama `feat/ssdlc-pipeline`** sobre `main` (`d8722e1`, merge de la PR #7), 4 commits convencionales, sin push. Sin cambio de versión (queda en `[Unreleased]`). Revisión de `revisor-codigo`: 2 bloqueantes, 8 importantes y 6 menores, **todos corregidos** (uno con matiz, abajo). Verificación: backend `make quality` limpio y **1723 pruebas / 9185 aserciones**; `docs:consistency` sin divergencias; `actionlint` solo con los 3 avisos preexistentes de `backup-drill.yml`; gitleaks 0 sobre los 75 commits del histórico; `promtool` en verde.

**Hecho:**

1. **Pipeline** (`devops-observabilidad`, commit `ci(security)`): job `security` de `ci.yml` con **gitleaks bloqueante** (`.gitleaks.toml` con allowlist por valor exacto, 17 falsos positivos analizados), **Semgrep comunitario** (`p/php`, `p/owasp-top-ten`, `p/javascript`, `p/typescript`; hoy 5 WARNING en `infra/docker/nginx`), **Trivy fs** (1 HIGH: `DS-0002` root en el Dockerfile de postgres, falso positivo probable) y **Trivy image** (`kronoqr/app:ci` 0; `kronoqr/postgres:ci` 21 HIGH heredados de `gosu`/Go stdlib) — los tres **en modo informe con vencimiento 2026-11-30**: un paso final del job falla pasada la fecha si siguen en informe; `--exit-code 0` para que una salida ≠ 0 signifique «la herramienta falló». **SBOM** CycloneDX (`make sbom`, Trivy sin BD de CVE) como artefacto; paso preparado y comentado en `release.yml`. **Dependabot** (composer, npm raíz, actions, docker). Imágenes base fijadas por **digest**; `apk upgrade` acotado a `libssl3 libcrypto3`; un único `make build-ci-images` para los tres sitios que construían. **Hallazgo colateral corregido**: la imagen de la app nunca había construido (`bootstrap/cache` en `.dockerignore`). Alertas `KronoqrAuthFailureBurst/Lockouts/FailureSpike` (T1110) con `docs/runbooks/ataque-a-credenciales.md` y `triaje-hallazgos-seguridad.md`; `BackupAndAlertingTest` cubre ahora `auth.yml`.
2. **Rastro de autenticación** (`backend-laravel`, commit `feat(compliance)`, **ADR-039**): `auth.login_succeeded`, `auth.logout` (gestión) y `auth.lockout_started` (tres canales) en `audit_log`; los **fallos nunca** van a `audit_log` (cerrojo global de ADR-010) sino al log estructurado `auth.login_failed` (con `ip_hash` HMAC de `APP_KEY`) y al contador `kronoqr_auth_attempts_total{channel,outcome}` (`lockout` = uno por bloqueo abierto; `failure` = todo lo demás). Las cinco ramas de rechazo del PIN cuestan lo mismo (sujeto señuelo, prueba `PinRejectionSymmetryTest`); el asiento de bloqueo se escribe **tras responder** (`DeferredAuditEntry`, `app()->terminating()`) y un fallo de auditoría nunca da 500. `PortalAccessTelemetry` conserva solo el span. Los asientos usan la columna `ip` como el resto de la tabla. **Hallazgo colateral corregido**: `tests/Support/Http/Api.php` nunca llamaba a `$kernel->terminate()`.
3. **Documentación** (`docs(seguridad)`): columna **ATT&CK** en doc 01 §8.1 (11 filas; dos «—» justificados) y **`docs/07-seguridad-madurez-y-amenazas.md`**: SAMM 2.0 práctica a práctica con evidencia (Gobierno 1,33 · Diseño 2,00 · Implementación 1,00 · Verificación 1,33 · Operaciones 1,67 · **total 1,47/3**), mapa SDL, shift-left y juicio humano, riesgos aceptados.

**Decidido el 29-08-2026 (usuario, a propuesta de la sesión):** **no** se crea `AuditActorType::Employee` para sesiones. El acceso del empleado al portal es el ejercicio del derecho que ADR-037 ya excluye de la traza, y el fichaje por PIN deja el `shift_entry.created`. El tipo se creará con la **primera acción de autoría del empleado con relevancia legal** (solicitud de corrección desde el portal, conformidad sobre el registro). En consecuencia, **RS-13 es requisito**: fila en doc 01 §8 con el reparto real de ADR-039 (acceso y cierre de gestión + apertura de bloqueo en `audit_log`; fallos al log técnico y a `kronoqr_auth_attempts_total`) y nota bajo la tabla; Anexo A Fase 1 (con nota); `docs/requisitos.yaml` (60 en Fase 1); citado en las tareas 1.6, 1.11 y 1.12 (doc 02 §11 y `plan implementacion/03`, exigido por `docs:consistency`); etiqueta `RS-13` en `AuthenticationTrailTest`, `PortalAuthenticationTrailTest`, `PinScanAuthenticationTrailTest`, `AuthenticationMetricsTest`, `BackupAndAlertingTest:227` y `AuthChannelTest:49`; doc 07 §3/§5/§6 y ADR-037/039 actualizados; `docs/trazabilidad-pruebas.md` regenerado. Verificado: `qa:traceability --check`, `docs:consistency --check` y 55 pruebas de los ficheros afectados en verde.

**Decisiones pendientes del usuario:**
- ~~Triaje de **Trivy**~~ **Hecho en la sesión «Trivy a bloqueante»** con la propuesta de abajo, tal cual. (1 DS-0002 en el Dockerfile de postgres, 21 HIGH heredados de `gosu`; Semgrep ya está triado y bloqueante), con vencimiento 2026-11-30. Propuesta que borra 22 de 27 de golpe: `USER postgres` + `rm /usr/local/bin/gosu` en `infra/docker/postgres/Dockerfile` (el entrypoint oficial no usa `gosu` si no arranca como root; retira también la entrada `CVE-2025-68121` de `.trivyignore.yaml`). Comprobar antes: propiedad del volumen de datos (bind mount vs volumen con nombre), y que los scripts de backup/drill y el rol de mantenimiento (ADR-027) no dependan de `docker exec` como root. Los 5 de Semgrep: corregir los reales y `# nosemgrep: <rule-id>` con motivo en los falsos. Después: los tres pasos a bloqueante.
- La primera ejecución real del job `security` en un runner Linux es la única verificación que no se pudo hacer en local. Qué mirar: (1) la caché `type=gha` se comprueba en la **segunda** ejecución (el `build-ci-images` debe bajar de minutos a segundos); (2) la descarga de la BD de Trivy desde `ghcr.io` es el fallo intermitente clásico — si aparece, cachear `~/.cache/trivy` o `TRIVY_DB_REPOSITORY`; (3) que un código 2+ de Semgrep (registro caído) haga fallar el job y no se silencie; (4) que el paso de vencimiento compare fechas en UTC.

**Matiz sobre un hallazgo (16)**: `isLocked()` y `secondsUntilUnlock() > 0` **no** son equivalentes en `LoginAttempts` (el segundo es la ventana del contador), así que el panel se queda con `isLocked()` y el verificador del PIN con la lectura única; documentado en el docblock de `LoginAttempts`.

**Pendiente menor**: `trace_id` sale `null` en gestión y PIN (falta un span de servidor por petición en middleware); reenvío idempotente del PIN cuenta dos veces en la métrica (anotado como causa benigna en el runbook); `docs/runbooks/rotacion-secretos.md` prometido en doc 02 y aún inexistente; SRI de assets prometido en doc 02 §7.1 y no implementado; `APP_DEBUG=false` de serie y rehash de contraseñas (mejoras del informe de seguridad, no ejecutadas).

**Siguiente acción:** abrir PR de `feat/ssdlc-pipeline` a `main` y mirar la primera ejecución del job `security` en Actions.


## Sesión 1.2.0: quiosco, tarjeta, portal y panel (28-08-2026)

**Rama `feat/quiosco-tarjeta-portal-panel-v1.2`** sobre `main` (`fa91e42`, merge de la PR #6), 9 commits convencionales, sin push. **`VERSION` = 1.2.0** y `CHANGELOG.md` con su entrada. **Etiquetas:** `v1.1.0` creada en local sobre `082d23f` (faltaba; sin ella el generador arrastraba la 1.1.0 a la 1.2.0) y **pendiente de `git push origin v1.1.0`**; `v1.2.0` se etiqueta sobre `main` tras el merge (`git tag -a v1.2.0 -m 1.2.0 && git push origin v1.2.0`; la CI valida el CHANGELOG al etiquetar).

Verificación final en verde: `web-kit` 137 · panel 172 · portal 69 · quiosco 309 unitarias + 37 E2E (axe limpio, bundle 101,1/250 KiB) · backend `make quality` y `make test` **1664 pruebas / 8859 aserciones** · trazabilidad y consistencia de docs. Revisión de `revisor-codigo`: 1 bloqueante y 3 importantes, corregidos salvo uno por decisión del usuario (abajo).

**Hecho:**

1. **Quiosco** (`frontend-quiosco`): estado **«Comprobando…»** para el PIN (`pinPipeline.ts`: gracia 300 ms → desenlace directo; hasta 2,5 s → «Comprobando…»; vencido o sin red → «pendiente», sobre en cola). Ya no aparece el panel índigo de éxito antes del rechazo rojo. Alcance anotado en **ADR-008** (su decisión es de la vía QR). Guarda `alive` en `PinView.vue` contra navegaciones tardías sobre el router. **Visor** centrado con esquinas terracota, banda con título/subtítulo/botón PIN encima, sin velo (`tests/e2e/layout.spec.ts` en 3 viewports con fixture de vídeo en blanco). Glifo «i» del anti-rebote eliminado. Decodificador: ya estaba al óptimo (100 ms, enfoque continuo, 1280×720); recorte ROI descartado por riesgo de fugas de cámara — **la fluidez real solo se mide en la tablet** (tamaño del QR impreso, luz).
2. **Tarjeta PDF** (`backend-laravel`): ID-1 apaisada partida por el ancho, QR 34,4 mm (zona tranquila ≥ 4 módulos), nombre y **código en negrita más pequeño debajo**; `qr_size_mm` pasa a mínimo garantizado, con `Log::warning` si no cabe. **Bug corregido de paso**: un `padding` con `border-box` imprimía el QR a 23 mm en vez de 26. `CredentialCardLayoutTest` (15). **Pendiente físico: imprimir y escanear con la tablet.**
3. **Portal**: código en negrita bajo el nombre; inputs de fecha `w-full sm:w-96` (medido con Inter real: la ayuda más larga 304 px) alineados por arriba, botón a la altura de los campos; Duración +10 % (`table-fixed` + `colgroup`); sin nombre de zona, sin «(WEST)» en cabeceras y sin línea UTC (**se conserva** CEST/CET en el historial de correcciones y la zona de otro centro en tramos trasladados); login sin `intro`/`pinHint`.
4. **Panel**: `pin_status` al servidor (contrato + backend, misma regla que el recurso); 30 por página (`EMPLOYEE_LIST_PER_PAGE`, `CLIENT_PER_PAGE`); login sin `intro`/`employeeNotice`.
5. **Ficha de empleado → sección «Tarjeta QR»** (`backend-laravel` + `frontend-panel`): muestra la fila de estado de la tarjeta de esa persona con sus acciones, igual que en el tablero. Contrato: `GET /credentials/status?employee_uuid=` (0/1 filas, `summary` del centro, asiento de auditoría con alcance `employee_uuid` y `record_count` 0/1). Acciones extraídas a `CredentialRowActions.vue` (única fuente para tablero y ficha) y píldora en `credentialStatusPill.ts`.
6. **Seguridad** (informe de `seguridad-cumplimiento`, solo lectura): contraseñas y PIN con bcrypt coste 12 (verificado por `QualityGatesTest`), señuelos de tiempo constante, bloqueo 5/15 min y escalonado 3/5/10 para el PIN, sobre libsodium, QR con SHA-256 + HMAC de 128 bits. OWASP: A02/A04 cumplen; A07 parcial (sin 2FA hasta 2.1, sin rehash); A05 (`APP_DEBUG=true` en `.env.example`); A09 (sin asiento de autenticación en `audit_log`). Mejoras propuestas, por valor: rehash en login, auditar login/fallo/bloqueo, `APP_DEBUG=false` de serie, 2FA (2.1), cambio de contraseña y revocación de tokens por API.

**Zona horaria (28-08-2026):** el empleado de pruebas está en Hotel Atlántico (`Atlantic/Canary`) y el quiosco `quiosco-pruebas` en Hotel Marina (`Europe/Madrid`); el usuario confirmó que **no es un fallo y no se toca**.

**Decisión del usuario mantenida contra un hallazgo de la revisión:** el portal ya no rotula la zona en la tabla del registro. El día del cambio de hora (24-10-2026 en `EdgeCaseSeeder`, 23:00→07:00 = 9 h) se lee sin explicación, y el panel sí rotula «Entrada (CET)». Si molesta, la propuesta del revisor es devolver solo la abreviatura en las cabeceras.

**Pendiente (hallazgo de revisión, fuera de alcance):** al dar de baja a un empleado (`OffboardEmployeeHandler`) **no se revoca su credencial** — sigue `delivered` y ningún oyente de `EmployeeOffboarded` la revoca, aunque el catálogo de motivos tenga `offboarding`. Decidir si la baja revoca automáticamente (asiento propio en `audit_log`) o si RRHH lo hace desde el tablero (la ficha de una persona no activa ya remite allí).

**Pendiente menor:** `HELD_GAP_MS`/`PIN_VERIFY_TIMEOUT_MS`/visor en tablet real; prueba de integración de que `unaccent` está instalada; E2E del panel (`frontend-admin/tests/e2e` no existe); E2E del portal con la jornada del cambio de hora; en vertical (800×1280) el visor queda a 440 px — revisar `55vmin` si se quiere mayor; comando `product:doctor` (Fase 5) debería comprobar `qr_size_mm` contra lo imprimible.

**Siguiente acción:** abrir PR de `feat/quiosco-tarjeta-portal-panel-v1.2` a `main`, `git push origin v1.1.0`, y etiquetar `v1.2.0` tras el merge.


## Sesión de UI/UX y buscadores (28-08-2026)

**Rama `feat/ui-ux-panel-portal-quiosco`, 8 commits convencionales sobre `main` (`fd46569`), sin push.** Ejecutado en tres oleadas de agentes en paralelo con ficheros disjuntos, revisión de `revisor-codigo` en medio (0 bloqueantes, 9 importantes — todos corregidos salvo dos que chocaban con decisiones explícitas del usuario, ver abajo) y verificación conjunta final en verde: `web-kit` 137 · panel 171 · portal 69 · quiosco 300 unitarias + 33 E2E (axe limpio, bundle crítico 100,7/250 KiB) · backend `make quality` y `make test` **1635 pruebas / 8729 aserciones** · `qa:traceability --check` y `docs:consistency --check` en verde.

**Hecho:**

1. **Agente nuevo `ui-ux`** (`.claude/agents/ui-ux.md`, registrado en doc 03): sistema visual, contraste medido, disposición y coherencia entre SPA.
2. **Sistema visual compartido** en `packages/web-kit` (`theme.css` + `base.css` + `fonts.css`, `contrast.ts`, `themePairs.ts`): paleta cálida del fabricante (kodigolab.es: terracota `#d66c3a`/`#b8542a`, crema `#fff7ed`, texto `#3a2e28`, Poppins/Inter **autoalojadas** con `@fontsource`, radio 14 px). Tokens `--kq-*` expuestos con `@theme inline` para que **la tarea 5.8 (marca blanca) los sobreescriba en tiempo de ejecución** sin recompilar. `theme.spec.ts` mide WCAG AA de cada pareja declarada (blanco sobre `#d66c3a` falla, 3,46:1 → los botones sólidos usan `primary-strong #b8542a`, 4,84:1). Guía en `docs/06-guia-visual.md`, enlazada desde `CLAUDE.md`. Aplicado a las tres SPA: cero `slate-*`/hex sueltos. Los cinco colores de confirmación del quiosco no son marca y no se tocan.
3. **Quiosco**: enlace PIN bajo el subtítulo de instrucciones (texto «Ficha con tu código y PIN», estilo secundario) y repetido como única vía en `camera-failure`; aviso art. 13 a la mitad de alto sin recortar contenido; precaché de fuentes limitado a latin y verificado por `check-bundle-budget.mjs`. **Cambio de comportamiento en `scanPipeline.ts`**: tarjeta sostenida delante de la cámara se detecta por flanco (`HELD_GAP_MS = 2000`, cadencia de ZXing 800 ms) — antes reiniciaba la confirmación cada 2,5 s y la pantalla de instrucciones no volvía; `LOCAL_REPEAT_WINDOW_MS` sigue en 2500.
4. **Panel**: `PaginationBar` reutilizable (en `frontend-admin/src/shared/ui`, no en web-kit: el portal no pagina, ADR-036); plantilla con cuadro nombre/código (`q`, debounce 300 ms, `maxlength` 100), selects de estado/centro/departamento con valores reales, estado del PIN filtrado en cliente con aviso, filtros en la query string y página acotada al total; credenciales con filtrado y paginación **en cliente** (el contrato no pagina a propósito), «pendiente de tarjeta» como opción del select de estado (`pending=true` al servidor).
5. **Backend**: `pending` de `GET /credentials/status` acepta `true/false` (era el 422 «must be true or false»); **`GET /employees?q=`** nuevo en contrato y código, insensible a mayúsculas **y acentos** (extensión `unaccent`, migración `2026_08_28_100000`, reversible, verificada en ambos sentidos); comodines escapados; auditoría guarda `search: bool`, nunca el término (regla dura 21).
6. **Portal**: contenedor `lg:w-[88%] 2xl:max-w-[1600px]`; jornadas en una columna (orden cronológico).

**No hecho, a propósito:**

- **Enviar por correo el PDF de la tarjeta al generarlo — descartado por el usuario (28-08-2026): no se ejecuta ese cambio.** Contradecía la regla dura 11 / ADR-014 (el plan lo repite en 2.12 y 5.6: «no hay envío por correo… no manda credenciales por correo») y roza la 12. Además «al usuario» es ambiguo: ¿al empleado (no tiene correo obligatorio, y la credencial dejaría de ser solo física) o al usuario de gestión que imprime (tiene correo, pero la tarjeta firmada quedaría en un buzón)? Sigue vigente el ADR-014 sin matices.
- Dos hallazgos de `revisor-codigo` chocan con lo pedido y se dejaron como pidió el usuario: el enlace PIN dentro del bloque de instrucciones **no se ve mientras hay una confirmación en pantalla** (3,5–5 s); y el texto sin el calificador «¿Sin tarjeta?» puede hacer que el PIN se use por comodidad (cada fichaje por PIN se marca para revisión, RF-AT-11). Se mitigó con estilo secundario. Si el uso del PIN sube, revisar.

**Pendiente menor:** integración que afirme que `unaccent` está instalada (hoy si se revierte la migración fallan 10 feature con un error SQL confuso); casos `ß`/ligaduras; E2E del panel (`frontend-admin/tests/e2e` no existe aún); el filtro de estado del PIN de plantilla es de la página visible — llevarlo al contrato si se usa; `HELD_GAP_MS` conviene medirlo en tablet real; el enlace «Volver a la plantilla» de la ficha no conserva los filtros (el botón atrás sí).

**Siguiente acción:** abrir PR de `feat/ui-ux-panel-portal-quiosco` a `main`.


## Sesión de auditoría y corrección (27-08-2026, posterior al cierre)

**Auditoría independiente de la Fase 1 ejecutada y publicada** (informe artifact: <https://claude.ai/code/artifact/271dbcba-2a1c-4d7c-a264-e89c09deac96>). Todas las puertas re-ejecutadas en vivo y en verde con los números declarados (1585→1592 pruebas, cobertura 91,54 %/85,1 %, MSI 84,04 %, E2E 32, audits limpios). Veredicto: implementación sólida; dos afirmaciones del cierre no se sostenían — la CI de GitHub **no podía estar en verde** desde mitad de la fase (job ③ sin PostgreSQL para las suites Feature/Contract, timeout 8 min < mutación ~25 min) y **p95 <150 ms no tenía ninguna evidencia** (k6 inexistente).

**Siete correcciones aplicadas y commiteadas en la rama `fix/auditoria-fase-1`** (6 commits convencionales, sin push todavía):

1. `security(export)` — CsvDialect neutraliza inyección de fórmulas CSV (OWASP A03; 7 pruebas byte a byte nuevas).
2. `refactor(attendance)` — RN-07 vuelve a una sola fuente (`ClockingPolicies`); el handler tenía copia viva propia.
3. `fix(ci)` — job ③ levanta el postgres del producto (initdb crea los 3 roles + `fichaje_test`) + redis, timeout 45; `deps-audit-js` audita el workspace raíz (antes se saltaba admin sin avisar); `lint-frontend` instala con `npm ci` de raíz; lockfiles anidados de kiosk/portal retirados. Verificado con actionlint + simulación local; **falta el push y mirar Actions de verdad** (gh sin auth en esta máquina).
4. `test(quality)` — gate de duración de la suite unitaria (`UNIT_SUITE_MAX_SECONDS`, 4 s contenedor; medida real 2,6-2,7 s) y los docs dejan de prometer "<2 s" sin herramienta (8 menciones alineadas en plan/doc02/CLAUDE.md). Verificado rompiéndolo a propósito.
5. `test(perf)` — herramienta k6 completa (aprovisionamiento server-side con firma real, N orígenes en paralelo porque el producto limita a ~10 fichajes/s por IP, agregado de muestras crudas). **Medido en esta máquina: ~450 ms/fichaje sin carga, saturación a ~21-25 aceptados/s → RNF-P-02/06 NO evaluable en Docker Desktop/Windows; pendiente pasada en hardware Linux de referencia** (herramienta lista: `load-tests/k6/run.sh`).
6. `feat(web-kit)` — panel y portal capturan por fin errores (`clientErrors`, mismo contrato saneado del quiosco, buffer para el transporte de la 5.12; 9 pruebas). Sentry evaluado y descartado como servicio (ADR-016/020); conceptos adoptados.

**Cambios de entorno de desarrollo (solo `.env`, no versionados)**: `QR_SIGNING_KEY_CURRENT` generada (antes vacía: el `/scan` real devolvía 503 en dev) y `KIOSK_VLAN_CIDR=172.28.0.0/16` (la red "app" de Compose, para que el tráfico local caiga en la zona VLAN). Contenedores `app` y `nginx` recreados.

**Hallazgo de diseño anotado, sin tocar**: el borde limita el scan a 600 r/m por IP también en la VLAN, y el propio comentario del template dice que "todos los quioscos de un hotel salen por la misma IP" — con una sola IP el techo del hotel son ~10 fichajes/s + burst 50, por debajo de los 50/s de RNF-P-06. Revisar si RNF-P-06 asume varias IPs de quiosco o si la zona VLAN debe llevar otra clave (p. ej. por dispositivo).

**Prueba manual en local habilitada por primera vez** (commit `fix(dev)` en la misma rama): las SPA se arrancan desde el host (`npm run dev`: kiosk 5173, admin 5174, portal 5175) con proxy `/api` al Nginx del entorno — verificado en vivo con login de gestión, login de portal y padrón de quiosco con token real. Guía completa para usuario no técnico: <https://claude.ai/code/artifact/f1a7cba7-0f21-4e55-9853-c503ec8181ec>. Causa raíz anotada como deuda: **los contenedores `node-*` están estructuralmente rotos desde ADR-036** (montan solo la carpeta de cada SPA; dependencias y `web-kit` viven en la raíz del workspace) — arreglarlos exige rediseñar su montaje/entrypoint. Al `.env` de dev se añadió también `IDENTITY_PIN_SEALING_SECRET_KEY` (antes ausente: el quiosco ocultaba la vía PIN) y existe el dispositivo `quiosco-pruebas`. Segundo hueco del mismo `.env` local, encontrado al imprimir una tarjeta desde el panel (27-08-2026): faltaba el bloque `LARAVEL_PDF_*` que `.env.example` sí trae — sin `LARAVEL_PDF_NO_SANDBOX=true`, Chromium no puede crear su sandbox en el contenedor (`Failed to move to new namespace … Operation not permitted`) y `POST /credentials/{uuid}/print` devuelve 500 (la credencial queda intacta: `printed_at`/`key_id` no se tocan). Añadido al `.env`; verificado con un PDF de humo dentro del contenedor. El `.env` local acumula desfase con `.env.example` (unas 30 variables sin valor explícito, todas con default en `config/`): si vuelve a fallar algo "inesperado", comparar los dos antes de buscar en el código. **Bug real del quiosco encontrado y corregido en la misma prueba manual** (sin commit aún): `ScanView.vue` y `PinView.vue` creaban `createApiClient()` **sin pasar `deviceToken`**, así que la SPA jamás enviaba `Authorization` aunque la llave estuviera en `localStorage` — padrón, latido y fichajes acababan en 401 y el quiosco mostraba «Sin conexión · N pendientes» de forma permanente (el 401 marca `reachability=false`, por diseño). La verificación previa del padrón «con token real» se había hecho con `curl`, no desde la SPA, y ninguna prueba cubría el cableado. Corregido con `deviceToken: readDeviceToken` en ambas vistas; `type-check`/`lint`/296 unitarias en verde; verificado en vivo (heartbeat y roster 200, `scan/batch` 207 con el fichaje que llevaba 7 min en cola). **Deuda para `qa-testing`**: una prueba de `ScanView`/`PinView` que afirme que el cliente sale con la cabecera `Authorization` cuando hay llave (la ausencia de esa prueba es lo que dejó pasar el bug).

**Versión 1.1.0 preparada y rama empujada (27-08-2026)**: `VERSION` 1.0.0 → 1.1.0 (hay un `feat`, salto *minor*), `CHANGELOG.md` cerrado con `changelog.sh generate --release 1.1.0` y `check 1.1.0` en verde, `fix/auditoria-fase-1` en `origin` hasta `chore(release): version 1.1.0`. **La PR a `main` no se pudo abrir desde esta máquina** (`gh` sin sesión y sin `GH_TOKEN`): abrirla desde la web con el cuerpo preparado, y **etiquetar `v1.1.0` sobre `main` tras el merge** (`git tag -a v1.1.0 -m 1.1.0 && git push origin v1.1.0`; la CI valida el CHANGELOG al etiquetar). Si el merge es *squash*, el CHANGELOG generado en `main` a partir de entonces solo verá el commit del squash: preferir *merge commit* o *rebase* para no perder los mensajes convencionales.

**Pendiente inmediato de esta sesión**: (1) abrir la PR y verificar Actions en verde; (2) pasada k6 en hardware de referencia; (3) los pendientes menores del informe que no se corrigieron (guarda de producción de `APP_DEBUG`, `config/sanctum.php`/`permission.php` publicados, cobertura de funciones de admin al 70,0 % justo, Rector 115 ficheros).

## Estado actual

Ejecutando la **Fase 1 (MVP de fichaje)** del plan de implementación, por instrucción explícita del usuario: *"Ejecuta la fase 1 para tener el mínimo viable del proyecto. No crees nada fuera de esta fase 1."* Alcance: las 18 tareas de `plan implementacion/03-fase-1-mvp-fichaje.md`, en oleadas de agentes en paralelo según sus precondiciones.

## Objetivo actual

**Fase 1 cerrada.** Las 18 tareas y los cuatro pasos del cierre de fase (`plan implementacion/03-fase-1-mvp-fichaje.md`, sección "Cierre de fase", doc 03 §6.6) están completos:

1. `seguridad-cumplimiento` — revisión contra STRIDE, RGPD y art. 34.9 ET, informe por severidad ✅
2. `revisor-codigo` — duplicación, complejidad innecesaria, incumplimientos de la Definición de Terminado ✅
3. `qa-testing` — cobertura, MSI, y que cada requisito del Anexo A de la fase tenga prueba (`qa:traceability --check`) ✅
4. `devops-observabilidad` — instrumentación y runbook de cada alerta nueva ✅

Instrucción del usuario: continuar hasta cerrar la fase y **no más**. Cumplida — **no se ha empezado la Fase 2**. **Decidido el 29-08-2026** (ver la sesión «cierre de la Fase 1 con reservas» al principio): la fase queda cerrada con tres reservas de hardware/estimación y la Fase 2 arranca tras dos tareas previas (toolchain del frontend, Playwright del panel).

**Cerrado además, a petición del usuario, un hueco real de la tarea 1.7**: `GET /api/v1/health` y `GET /api/v1/ready` estaban completos en el contrato desde la Fase 0 pero nunca se habían implementado (solo comentarios que daban por hecho que existían). Implementados por `backend-laravel`: `/health` no toca ninguna dependencia (BD/Redis/disco) y expone la versión SemVer desplegada, resuelta de `APP_VERSION`/`IMAGE_TAG` o, si no son SemVer válido (p. ej. `latest` en desarrollo), del fichero `VERSION` nuevo en la raíz del repositorio; `/ready` sí comprueba PostgreSQL y Redis, con `503 problem+json` genérico sin detallar qué componente falló. Verificado en vivo: `curl https://localhost/api/v1/health` → `{"status":"ok","version":"1.0.0"}`. `make test` **1585 pruebas / 8530 aserciones** en verde.

**Versionado con SemVer, según lo ya diseñado en doc 02 §10.5, puesto en marcha por primera vez en esta sesión.** Todo el trabajo pendiente del cierre de fase (297 ficheros) se ha commiteado en un único commit (`acc659a`, tipo `feat`, con `Co-Authored-By`) sobre el commit previo `23c7a1c` ("Fase 1 Sin optimizar", hecho fuera de esta sesión — ver aviso más abajo). `make changelog generate --release 1.0.0 --write` regeneró `CHANGELOG.md` (commit `ed5aa7a`) y se etiquetó **`v1.0.0`** (tag anotado, local, **todavía sin `git push`** — pendiente de que el usuario confirme el push).

**Aviso importante sobre el CHANGELOG generado**: la mayoría de los commits del histórico (incluidos `23c7a1c` y el propio `acc659a` de este cierre) no siguen el formato de Conventional Commits, así que `infra/scripts/changelog.sh` los descarta con aviso por `stderr` en vez de inventarles contenido — es el comportamiento diseñado, no un fallo. La entrada de `1.0.0` en `CHANGELOG.md` es por tanto más corta de lo que el trabajo real de esta fase merece (una sola línea "cierre de la Fase 1..." representa 18 tareas + 4 pasos de cierre). **A partir de ahora, si se quiere un CHANGELOG fiel, cada commit debe llevar formato convencional** (`feat:`, `fix:`, `security:`, etc.) — no se puede corregir esto a mano sin romper la garantía del fichero de que "se genera, no se edita".

Umbrales que deben quedar en verde (§9.2, RNF-M-01): cobertura dominio ≥90 % / backend ≥75 % / frontend ≥70 %; MSI ≥80 % sobre `Modules/*/Domain`; suite unitaria <2s; `vue-tsc`/PHPStan 9 con 0 errores; Deptrac/Pest Arch 0 violaciones; `axe` 0 violaciones críticas/graves; p95 fichaje <150ms (RNF-P-02); bundle crítico del quiosco ≤250KB gzip.

Lo que ningún agente puede cerrar (hardware o recalibración manual, quedan como pendiente documentado, no bloquean el resto): prueba de resistencia 12h en tablet real, tarjeta física impresa y escaneada, recalibración de estimación de horas (R16).

**Paso 1 (seguridad-cumplimiento) completado.** Veredicto: el núcleo criptográfico/probatorio está bien construido, sin objeciones de fondo (HMAC en tiempo constante, cadena de hash con separador de registro, tres roles de BD, minimización del padrón, rechazos idénticos). Lo que bloquea no es el diseño, es que algunas garantías no están verificadas o aplicadas del todo. Cuatro hallazgos bloqueantes:

- **[ALTO-1] `config/quality.php` seguía con `current_phase=0`** — la puerta de trazabilidad no vigilaba la Fase 1 en absoluto. **Corregido por mí a `1`** y re-ejecutado `qa:traceability --check`: confirma exactamente 5 requisitos sin ninguna prueba en todo el árbol (los otros 7 que salían en el chequeo del contenedor sí tienen etiqueta Playwright real, solo que ese contenedor no monta los frontends): **RF-ID-08, RNF-D-04, RNF-P-03, RQ-10** pendientes de qa-testing/devops-observabilidad, y **RL-09 ya corregido por mí** (era solo añadir `@RL-09` a una prueba E2E existente, `frontend-kiosk/tests/e2e/scan.spec.ts:192`).
- **[ALTO-2] `PORTAL_INTERNAL_ONLY=true` es un control fantasma**: existe en `.env.example` y se promete al cliente en `docs/05`, pero no lo lee ni la aplicación ni Nginx — no hay vhost de portal todavía. Pendiente de `devops-observabilidad` (vhost + CIDR) y de corregir la documentación mientras tanto para que no afirme un control inexistente.
- **[ALTO-3] `GET /credentials/status` no audita la divulgación** de la plantilla completa (nombre, código, centro, departamento) — incoherente con `/kiosk/roster`, que divulga menos y sí audita. Requiere decisión de diseño (candado de `audit_log` es un `pg_advisory_xact_lock` global, hay que pesar la contención). **Delegado a `arquitecto-dominio`, lanzado en segundo plano.**
- **[ALTO-4] Seis endpoints sin prueba de autorización negativa** (credenciales, PIN, y **el módulo Kiosk entero sin ninguna prueba propia** — `/kiosk/roster` y `/kiosk/heartbeat` sin ni un test). Pendiente de `qa-testing` (paso 3).

Hallazgos medios/bajos, todos anotados y repartidos: desfase de reloj sin cota que active revisión (delegado a `arquitecto-dominio`, junto con ALTO-3), RS-02 con solo 2 de sus 3 ejes de límite de tasa (mismo agente), exportación legal sin purga en disco (para devops-observabilidad), contrato de `/auth/logout` no declara `employeeToken` pese a aceptarlo ya en código (hallazgo positivo: el logout del portal **ya funciona**, solo falta que el frontend lo llame y el contrato lo declare — no bloqueante, no hace falta `/me/logout` nuevo), hash señuelo del PIN sin prueba que lo ate al hasher configurado, redacción de RL-12 sobre el cifrado del padrón que promete más de lo que da.

**Confirmado explícitamente por la revisión (sin acción)**: la clave `IDENTITY_PIN_SEALING_SECRET_KEY` falla cerrado correctamente si no se genera (el quiosco oculta el PIN, no es una avería) — solo falta añadirla a la tabla de secretos de `docs/cliente/instalacion.md`, no bloqueante.

**Lanzados en paralelo, en segundo plano**: paso 2 del cierre (`revisor-codigo`, duplicación/complejidad/Definición de Terminado) y `arquitecto-dominio` (las tres decisiones de diseño de arriba: auditoría de `/credentials/status`, desfase de reloj, tercer eje de RS-02).

**Paso 2 (revisor-codigo) completado.** El backend se coordinó bien entre las nueve oleadas (verificado explícitamente: `PinAttempts`, `HashedEmployeePinVerifier`, `ReadEmployeeWorkDays` son reutilización real, no copias). El problema está en el frontend, con un patrón de fondo: *"cuando dos oleadas necesitaron lo mismo, el backend extrajo un puerto y el frontend copió un fichero"* — porque el backend tiene la frontera hexagonal del §1.6 obligando a decidir dónde vive cada cosa, y entre las tres SPA no hay ninguna frontera declarada.

**1 hallazgo bloqueante**: `frontend-portal` (1.11) copió ~1450 líneas de `frontend-admin` (1.16) — cliente HTTP, formateo de fecha, cinco componentes de UI, y **el cálculo de totales de jornada** (`workdayTotals.ts`, cuerpo idéntico carácter por carácter). Ya divergió: el portal no lee `performed_at_local` como el panel y reconvierte en el navegador, con riesgo real de discrepancia en cambio de hora.

**Decisión del usuario**: paquete compartido (`npm workspaces`, `@kronoqr/web-kit`) en vez de aceptar la duplicación con ADR. **Documentada en ADR-036** (`docs/adr/ADR-036-las-spa-comparten-un-paquete-de-calculo-y-presentacion.md`). Lanzado `frontend-panel` para crear el paquete y migrar `frontend-admin`; cuando termine, lanzar `frontend-portal-empleado` para migrar `frontend-portal` (fija de paso el bug de `performed_at_local`).

**6 hallazgos importantes más, todos ya en marcha en paralelo (no dependen de la decisión anterior)**:
- Backend (`backend-laravel`, lanzado): dos escritores de CSV con fin de línea distinto (CRLF vs LF, contradiciendo su propio comentario) — unificar en un VO de `Shared`; `Row` (manejo seguro de columnas nulas de PDO) vive en `Reporting` y `Compliance` sigue haciendo casts a mano sobre datos con consecuencia legal — mover a `Shared`; siete copias del andamiaje de telemetría con dos semánticas distintas de `trace_id` — consolidar en `Shared/Infrastructure/Telemetry`; validación de rango de fechas escrita tres veces en `Reporting/Http/Request/` — extraer a un trait.
- Frontend-kiosk (`frontend-quiosco`, lanzado): `dispatch()` de fichaje duplicado entre las tuberías de QR y PIN, ya divergente (la rama de rechazo muestra una hora distinta según la vía) — extraer función común; `pinSealingKnown` no distingue "aún no se sabe" de "esta instalación no ofrece PIN", puede dejar a alguien hasta 30 min ante un teclado inactivo tras una recarga.

7 hallazgos menores anotados por la revisión, no bloqueantes, no repetidos aquí (ver el resultado completo del agente si hace falta el detalle): restos de código muerto en el portal, costuras que invitan a copiar (`pipeline` no opcional en `useScanSession`, opciones de `getOfflineQueueController` ignoradas silenciosamente en llamadas posteriores), criterio de formato de hora distinto entre quiosco y panel/portal (`h23` vs `short`), catálogo de motivos de corrección traducido cuatro veces y ya divergente en un texto, `Europe/Madrid` como valor por defecto literal en el alta de centro (ADR-017), y el hallazgo de que el logout del portal en realidad **ya funciona** técnicamente (`POST /auth/logout` acepta el token del portal aunque el contrato no lo declare) — solo falta que el frontend lo llame.

**`frontend-quiosco` cerrado y verificado.** `settleFrom()` extraído a `features/scan/application/`, usado por las dos tuberías; decidido y confirmado que `rejected` usa el instante del intento (la única opción posible además de la correcta: el resultado de rechazo no trae ningún instante del servidor). `pinSealingKnown` sustituido por `rosterSettled()`, que se resuelve en un `finally` de `refresh()` sea cual sea el resultado, distinguiendo de verdad "aún no se sabe" de "esta instalación no ofrece PIN".

**Verificado dos veces**: por el agente y de forma independiente por mí — con una corrección mía por el camino: mi propia edición anterior (añadir la etiqueta `@RL-09` a `scan.spec.ts`) dejaba una línea sin formatear, `npm run lint` lo detectó, corregido con `prettier --write`. Tras eso: `type-check`, `lint`, **296 pruebas unitarias** y `build` (bundle crítico 100,5/250 KiB) en verde, más **30 pruebas E2E de Playwright** (incluye `pin.spec.ts`, `scan.spec.ts`, `offline.spec.ts`) confirmadas por el propio agente.

**`frontend-panel` (paquete compartido) cerrado y verificado.** Creado `packages/web-kit` (`@kronoqr/web-kit`, npm workspaces, sin build propio — se consume como TypeScript fuente vía `exports`), con `http.ts`, `datetime.ts` (la que lee `*_local` sin reconvertir), `announcer.ts`, `downloadDocument.ts`, `dateRange.ts`, `workdayTotals.ts` y los cinco componentes de UI genéricos, cada uno con su suite. `frontend-admin` migrado para consumirlo, sus copias locales borradas.

**Verificado dos veces**: por el agente y de forma independiente por mí, con una corrección mía por el camino — al paquete nuevo le faltaba `.prettierignore` (fallaba `lint` contra su propio directorio de cobertura generado), añadido copiando el patrón de `frontend-admin`. Tras eso: `frontend-admin` con `type-check`/`lint`/**141 pruebas**/`build` en verde (bundle JS práticamente sin cambio, 300,93→301,13 KB gzip, confirma que el paquete es *tree-shakeable*); `packages/web-kit` con `type-check`/`lint`/**39 pruebas** (cobertura 96,4 %) en verde de forma independiente.

**Lanzado `frontend-portal-empleado`** para migrar `frontend-portal` al mismo paquete, incluida la corrección del bug de `performed_at_local` que motivó todo esto. En segundo plano.

**`arquitecto-dominio` (decisiones de dominio) cerrado.** Las tres resueltas, con criterio propio y no solo aplicando la propuesta de la revisión al pie de la letra:

- **[ALTO-3] Auditoría de lecturas en volumen — regla general fijada (ADR-037)**: se audita cuando los datos son de terceros, salen del proceso hacia una persona/dispositivo, y es un conjunto de personas o el registro horario de una. `GET /credentials/status` y `GET /employees` (índice) ahora auditan; `/kiosk/roster` y `/employees/{uuid}/workdays` seguían bien; `/me/*` confirmado sin asiento (cierra la duda abierta desde 1.11); la ficha individual tampoco (la subsume el asiento del índice). **Un solo candado de auditoría, no uno por dataset** — partirlo bifurcaría la cadena de hash de ADR-010; si algún día amenaza el p95 de fichaje, la palanca es agrupar por ventana, no partir el candado.
- **[MEDIO-1] Desfase de reloj — aprobado y movido a `Domain/`**: nuevo VO `ClockSkew` + `ReviewPolicy` en `Attendance/Domain/`, con el umbral resuelto por `OperationalSettings` (regla dura 14). Hallazgo del propio agente: no era solo "dejar el dato listo para la 3.5" — **RN-15 (requisito de esta fase) tenía una segunda frase sin implementar** ("si supera el umbral, requiere validación del responsable"). No se acota `occurred_at` (RF-AT-10 lo prohíbe explícitamente).
- **[MEDIO-2] RS-02 — enmendado el requisito, no implementado el tercer eje (ADR-038)**: un límite por credencial no frena la enumeración (quien enumera nunca repite credencial), duplicaría peor lo que RF-AT-06 ya resuelve, y sería la única vía por la que el producto podría dejar a una persona concreta sin fichar (rompe la regla dura 19). RS-02 reescrito en doc 01 §8 y `requisitos.yaml`; los dos ejes que sí existen ganan prueba propia sobre `/scan` (antes se apoyaban en pruebas de otros endpoints).

**Incidente encontrado y corregido por mí al recibir el resultado**: el agente creó su ADR de RS-02 como `ADR-036`, chocando con el `ADR-036` que yo ya había creado (paquete compartido de frontend) — dos ficheros distintos con el mismo número. Corregido: renombrado el de RS-02 a **ADR-038**, y arreglada cada referencia cruzada (`docs/01` §8, `docs/02` §4 —tabla de ADR, le faltaban además las filas de los tres ADR nuevos 036/037/038, añadidas—, `docs/requisitos.yaml`, `KioskRateLimit.php`, `RegisterScanTest.php`). Verificado con `make docs-consistency` (sin divergencias tras la corrección) y reconfirmado `make quality`/`make test` (**1368 pruebas / 8146 aserciones**, sin el fallo intermitente de autoload que reportó el agente en una de sus cuatro pasadas).

**`backend-laravel` (consolidación de duplicación) cerrado.** Los cuatro puntos resueltos: `CsvDialect` compartido en `Shared/Infrastructure/Export/` (decisión notable: `CsvLegalExportWriter` dejó de usar OpenSpout porque esa librería no permite configurar el fin de línea — ahora escribe directo con `fopen()`+`CsvDialect`, con una prueba nueva que compara bytes reales entre los dos escritores); `Row` movido a `Shared/Infrastructure/Persistence/` y adoptado por `Compliance` (ya no hay casts `(int)` a pelo sobre el total legal); `SpanScope` compartido para las siete clases de telemetría, con `PortalAccessTelemetry` corregido de verdad (usaba `Span::getCurrent()` y el `trace_id` podía no coincidir con el del log; ahora usa `startActive()`); trait `ValidatesWorkDateRange` en `Reporting/Http/Request/` (tres ficheros de ~120 líneas bajan a 57-90).

Decisión de fronteras documentada: `deptrac.yaml` gana una capa nueva y estrecha `SharedExport`, concedida solo a `ReportingHttp` y `ComplianceInfrastructure`, para no abrir todo `SharedInfrastructure` (que incluye adaptadores como `CachePinAttempts`) a capas HTTP.

**Verificado dos veces**: por el agente y de forma independiente por mí (contra Docker real): `make quality` verde, `make test` **1368 pruebas / 8146 aserciones** en verde (confirmado también tras mi corrección de la colisión de ADR), `make docs-consistency` sin divergencias.

**Deuda anotada por el propio agente, para `qa-testing`**: prueba unitaria propia de `CsvDialect` (entrecomillado RFC 4180) y de `Row` (tipos que devuelve PDO), de `SpanScope` (traceId con span inerte, fuga de scope), feature que confirme que el `trace_id` del log de portal coincide con el del span sin `traceparent`, y prueba de validación de rango sobre las tres peticiones a la vez con la misma tabla de casos.

**`frontend-portal-empleado` cerrado — ADR-036 completado del todo.** Migradas las ~1450 líneas duplicadas a `@kronoqr/web-kit` y corregido el bug de `performed_at_local` en `CorrectionHistory.vue`. El agente verificó el arreglo revirtiéndolo temporalmente para confirmar que la prueba nueva (corrección firmada en el cambio de hora de octubre 2026) fallaba exactamente como se esperaba, y volvía a pasar con el fix — no se limitó a comprobar que la prueba pasaba, comprobó que fallaba sin el fix.

**Verificado dos veces**: por el agente y de forma independiente por mí — `frontend-portal` con `type-check`/`lint`/**69 pruebas** (cobertura 97,7 %)/`build` en verde; `packages/web-kit` reverificado en verde (39 pruebas) como segundo consumidor, sin necesitar ningún cambio.

**Las tres SPA ya no duplican cálculo ni presentación (ADR-036 cerrado).** Los 6 hallazgos importantes de `revisor-codigo` están todos cerrados.

**Paso 3 (`qa-testing`) lanzado**, en segundo plano, con la lista consolidada: ALTO-4 de seguridad (autorización negativa de 6 endpoints + módulo Kiosk sin pruebas propias, prioridad más alta), los 12 requisitos sin prueba de `qa:traceability --check` (investigar de verdad si es desajuste de ruta o limitación del contenedor de desarrollo, no asumir), `make mutate` real, y la deuda de pruebas unitarias de `CsvDialect`/`Row`/`SpanScope`/`ValidatesWorkDateRange`/trace_id del portal anotada por `backend-laravel`.

**Paso 3 (qa-testing) cerrado. De 12 requisitos sin prueba a 1 (`RF-ID-08`, que no le corresponde a este paso).**

Trabajo por bloque:
- **ALTO-4 (autorización negativa)**: matriz de gestión ampliada con los 6 endpoints que faltaban; **módulo Kiosk completo desde cero** (`tests/Feature/Kiosk/`, 21 casos) — al construirlo, el control positivo (no la prueba negativa) **destapó un 500 real en `POST /credentials/{uuid}/print`**: la imagen Docker de desarrollo llevaba 8 días sin reconstruirse desde que el Dockerfile empezó a instalar node/chromium para Browsershot, así que imprimir una tarjeta fallaba silenciosamente porque nadie tenía una prueba de feature sobre ese endpoint, solo comprobación de que la ruta existe. **Corregido por mí**: `docker compose build app` + reinicio del contenedor, confirmado `node`/`chromium` presentes y `make test` (1538 pruebas) reconfirmado en verde contra la imagen nueva. La prueba en sí usa un doble (`FakeCardRenderer`) para no depender de que haya navegador en cualquier máquina, pero el hallazgo era real y afectaba al entorno de desarrollo de verdad, no solo a un test.
- **Los 12 de trazabilidad**: comprobado empíricamente (no supuesto) que 8 eran un problema real de resolución de rutas — `qa:traceability` usaba `base_path('..')`, que vale `/var/www` en el contenedor pero el repo está montado en `/var/www/repo`, así que Playwright nunca encontraba nada (0 de 0) aunque las etiquetas sí existían. **Corregido**: las rutas ahora se resuelven desde `$repoPath`, verificado con el mismo resultado en contenedor y CI. `RNF-D-04` y `RQ-10` cubiertos con pruebas nuevas reales (detector de migraciones bloqueantes, `make mutate` ejecutado). Queda solo **`RF-ID-08`**, correctamente no resuelto aquí — es del paso 4.
- **`make mutate` real ejecutado dos veces** (antes/después, 21,8 min cada una): **MSI 84,04 %** idéntico en ambas (no se tocó dominio), umbral 80 % superado.
- Deuda de `backend-laravel` (CsvDialect, Row, SpanScope, ValidatesWorkDateRange, trace_id del portal) y el señuelo del PIN: todo cubierto, cada prueba nueva verificada rompiendo la implementación a propósito antes de darla por buena.

**Dos hallazgos reales adicionales, anotados y no bloqueantes**:
- **Dos migraciones incumplen RNF-D-04** (`2026_08_20_100100_mint_credential_secret_at_print_time.php`, `2026_08_20_100200_add_pin_provisioning_to_employees_table.php`): añaden `CHECK`/`FOREIGN KEY` sobre tablas con datos sin `NOT VALID` (bloqueo `ACCESS EXCLUSIVE`). No tocadas — reescribir una migración que puede haberse ejecutado ya es una decisión de despliegue, no de esta sesión. Quedan como deuda explícita en un trinquete de la prueba nueva (`knownMigrationDebt()`), que solo puede vaciarse, nunca crecer.
- `RNF-O-01` no existe en `docs/requisitos.yaml` pese a que doc 02 §8.1 especifica un requisito de observabilidad — hueco del catálogo, no de esta fase.

**Verificado dos veces**: por el agente y de forma independiente por mí (incluida la reconstrucción de la imagen Docker) — `make quality` verde, `make test` **1538 pruebas / 8411 aserciones**, `qa:traceability --check` con solo `RF-ID-08` pendiente, `make mutate` MSI 84,04 %, `make docs-consistency` sin divergencias.

**Paso 4 (`devops-observabilidad`) completado. Cierre de la Fase 1 verificado en verde del todo.**

- **RF-ID-08 resuelto en el borde, no en la aplicación.** `PORTAL_INTERNAL_CIDR` (nueva variable de instalación, mismo patrón que `KIOSK_VLAN_CIDR`/`METRICS_ALLOW_CIDR`) sustituye al `PORTAL_INTERNAL_ONLY` fantasma, que se retiró. Un `geo` nuevo (`$kronoqr_portal_allowed`) en `infra/docker/nginx/templates/kronoqr.conf.template` gatea `location ^~ /api/v1/me/` con un `403` en el borde antes de PHP-FPM — el mismo patrón que `/metrics`. El equivalente de desarrollo (`infra/docker/nginx/dev-extra/frontends.conf`, proxy a Vite del portal) lleva el mismo candado. **Verificado en vivo, no solo por prueba estática**: con `PORTAL_INTERNAL_CIDR` puesto a una red que no incluye al host, `/api/v1/me/login` y `/portal/` devuelven `403`; con el valor correcto (`172.28.0.0/16` en dev, red "app" de Compose), pasan el filtro con normalidad (`400`/`502` según disponibilidad del backend, nunca `403`); `/api/v1/auth/*` no se ve afectado. `nginx -t` en verde. Prueba estática nueva en `QualityGatesTest.php` (`->group('RF-ID-08')`) que ancla la configuración. **No implementado y fuera de alcance de infra**: la parte de RF-ID-08 que dice que exponer el portal a internet "activa requisitos adicionales de contraseña" — es lógica de aplicación (Identity), no de borde HTTP; queda anotada en «Pendiente». La entrega de `frontend-portal/dist/` como estático de producción sigue diferida a la tarea 5.4 (decisión ya tomada antes de esta sesión, no tocada): cuando se implemente, esa location tiene que reusar `$kronoqr_portal_allowed`, quedó dicho en un comentario de la plantilla.
- **`composer audit` ejecutado contra el contenedor real**: `No security vulnerability advisories found.` Sin hallazgos que reportar.
- **Purga programada de exportaciones legales huérfanas (MEDIO-3)**: nuevo comando `compliance:purge-legal-export-temp` (`backend/app/Modules/Compliance/Infrastructure/Console/`), programado **cada hora** en `routes/console.php`, con ventana `COMPLIANCE_LEGAL_EXPORT_TEMP_RETENTION_HOURS` (6 h por defecto, `backend/config/compliance.php`). Actúa **solo** sobre `storage/framework/legal-exports/` (el temporal de la descarga HTTP que `deleteFileAfterSend()` no llega a borrar si el cliente aborta a medias); **nunca** toca `storage/app/legal-exports/`, la copia deliberada de `compliance:legal-export` para Inspección. El runbook `docs/runbooks/requerimiento-inspeccion.md` gana una sección nueva (§7, "Después de entregar: custodia y borrado del fichero") con el procedimiento manual para esa segunda ruta, que sigue sin cron. 4 pruebas nuevas (`PurgeOrphanedLegalExportTempFilesCommandTest`), incluida una que confirma que la copia de consola nunca se toca aunque tenga 30 días de antigüedad.
- **Fila que faltaba en la tabla de secretos** (`docs/cliente/instalacion.md`): añadida `IDENTITY_PIN_SEALING_SECRET_KEY`, con el comando de generación que ya vivía en `.env.example` y la advertencia de que vacía es un caso legítimo (instalación sin fichaje por PIN).
- Documentación de apoyo actualizada para no dejar el nombre de variable viejo suelto: `docs/02-stack-tecnologico-y-plan-implementacion.md` (Anexo B), `plan implementacion/01-herramientas-y-entorno.md`, `plan implementacion/03-fase-1-mvp-fichaje.md`, `plan implementacion/05-fase-5-productizacion.md` (dos menciones). `docs/adr/` no se tocó — no hacía falta ninguna decisión de arquitectura nueva.

**Verificado**: `make quality` verde (Pint 726 ficheros, PHPStan 9 sin errores, Deptrac 0 violaciones, ShellCheck/shfmt 0 hallazgos, lint de OpenAPI 0 problemas); `make test` **1543 pruebas / 8427 aserciones** en verde (1538 + 5 nuevas: 1 de `QualityGatesTest` + 4 de la purga); `php artisan qa:traceability --check` → **`Trazabilidad: todos los requisitos de las fases ejecutadas tienen prueba.`**, ya sin `RF-ID-08` pendiente; `make docs-consistency` sin divergencias; `composer audit` limpio.

**LA FASE 1 QUEDA CERRADA.** Las 18 tareas están completas, los cuatro pasos del cierre de fase están hechos, y las tres puertas de calidad (`make quality`, `make test`, `qa:traceability --check`) están en verde sin ningún requisito pendiente. Lo que queda son los pendientes explícitos de abajo (ninguno bloquea el cierre): dos decisiones de diseño delegadas y no cerradas (auditoría de lecturas RS-05, revocación real de sesión de portal), los requisitos adicionales de contraseña de RF-ID-08 al exponer a internet, y lo no automatizable (hardware real).

## Completado

- **1.1** Dominio Attendance (agregados, VOs, eventos, puertos)
- **1.2** Pruebas de dominio + cobertura/mutación (MSI ≥ 80 %)
- **1.3** Esquema y migraciones (constraints declarativas, roles de BD)
- **1.6** Módulos Identity (auth de gestión) + Workforce (empleado/centro/departamento)
- **1.14** `audit_log` particionado y encadenado por hash (ADR-027, ADR-033)
- **1.18** Copias cifradas y verificadas + simulacro de restauración
- **1.5** Credenciales QR HMAC + tokens de dispositivo de quiosco (módulo Identity)
- **1.4** RegisterScan (caso de uso central: anti-rebote RF-AT-06, idempotencia, cambio de tramo, auditoría del fichaje)
- **1.7** Endpoints `/scan`, `/scan/batch`, `/kiosk/roster`, `/kiosk/heartbeat` + rate limiting por zona
- **1.10** Impresión de tarjetas PDF, lote, entrega y panel de estado (backend + panel `frontend-admin`; ADR-034)
- **1.13** Provisión, entrega y restablecimiento del PIN de 6 dígitos (backend + panel `frontend-admin`)
- **Frontend de 1.6** (login de gestión sin 2FA, listado/alta/ficha/baja de empleado) y pantallas de `frontend-admin` de **1.10** (panel de credenciales) y **1.13** (PIN) — construidas por `frontend-panel` en esta sesión, ver «Último punto trabajado»
- **1.8** PWA del quiosco completa: escaneo continuo ZXing, wake lock, feedback visual/sonoro, confirmación, i18n, accesibilidad AA, aviso de privacidad, Playwright configurado desde cero con cámara simulada (QR limpio y degradado) — construida por `frontend-quiosco` en esta sesión
- **1.15** Correcciones trazadas completa (dominio por `arquitecto-dominio` + infraestructura por `backend-laravel`, relevo en esta sesión): `POST /shift-entries`, `PATCH /shift-entries/{uuid}`, `POST /shift-entries/{uuid}/void`, migración `shift_corrections`, `audit_log`, `manual_corrections_total`, ADR-035
- **1.9** Cola offline Dexie en el quiosco (encolado antes de confirmar, padrón cifrado, backoff, `POST /scan/batch` en lotes de 50)
- **1.17** Exportación legal para Inspección de Trabajo (`GET /reports/legal-export`, CSV con BOM, comando de consola, runbook, pantalla en `frontend-admin`)
- **1.16** Panel: detalle de jornada por empleado (`frontend-admin/src/features/workdays/`, RF-PA-03)
- **1.12** PIN de respaldo de 6 dígitos en el quiosco, con bloqueo escalonado por intentos (3/5/10) y cifrado libsodium en la cola offline
- **1.11** Portal del empleado (`frontend-portal/`): acceso con código y PIN, mi registro, mi exportación en CSV

## Pendiente

**La Fase 1 está cerrada.** Lo que queda es deuda explícita, ninguna bloqueante, para decidir al empezar la Fase 2 o antes si alguien lo pide:

- Si la *lectura* de `GET /credentials/status`, `GET /employees/{uuid}/workdays` y `/me/*` queda registrada en `audit_log` (RS-05) — criterio inconsistente entre tareas, hace falta que alguien (`seguridad-cumplimiento`) fije la regla general
- **RF-ID-08, la mitad que queda.** La restricción de red (`PORTAL_INTERNAL_CIDR`, por defecto restringido) está implementada y verificada en el borde HTTP. Lo que sigue sin existir es la parte de aplicación: "exponer el portal a internet activa requisitos adicionales de contraseña". Es un cambio en Identity (política de contraseña/PIN condicionada a la exposición de red), no de infraestructura — pendiente de asignar.
- La entrega de `frontend-admin/dist/`, `frontend-kiosk/dist/` y `frontend-portal/dist/` como estáticos de producción sigue diferida a la tarea 5.4 (decisión previa a esta sesión, no tocada). Cuando se implemente esa location de Nginx para el portal, tiene que reusar el `geo` `$kronoqr_portal_allowed` que ya existe en la plantilla — quedó anotado en el propio fichero.
- Si se añade `POST /api/v1/me/logout` para revocar de verdad la sesión del portal en dispositivo compartido (hoy el botón "Salir" solo limpia `sessionStorage`, el token sigue vivo hasta su caducidad de 2h)
- Generar y guardar `IDENTITY_PIN_SEALING_SECRET_KEY` (par de claves del cifrado del PIN offline, 1.12) con el resto de secretos de despliegue antes de producción — la fila de custodia ya está en `docs/cliente/instalacion.md`
- No automatizable, necesita hardware real: prueba de resistencia de 12h, tarjeta física impresa y escaneada con éxito en quiosco real
- Recalibración de la estimación de horas (R16)
- Deuda de pruebas menor acumulada por varias tareas (E2E de exportación legal, E2E de portal con Playwright si se decide añadir esa infraestructura, `make mutate` sobre varios VO nuevos, pruebas de concurrencia puntuales) — no bloqueante, ver el detalle en cada bloque de esta wave más abajo
- Deuda de pruebas de la 1.15 anotada para `qa-testing` (no bloqueante): ver «Último punto trabajado»
- **Entorno de desarrollo local** (no de esta sesión, no bloqueante): `node-kiosk`, `node-admin` y `node-portal` están en bucle de reinicio en este contenedor (`sh: vite: not found` — `node_modules` sin instalar en el volumen). No afecta a Nginx ni al backend; solo impide levantar los tres frontends en modo Vite en esta máquina hasta que alguien reinstale dependencias del volumen `node`.

## Archivos modificados

Alto volumen (backend/app/Modules/{Attendance,Identity,Workforce,Compliance}, migraciones, infra/, docs/). Ver `git status` / `git log` para el detalle exacto — no se repite aquí para no duplicar lo que git ya sabe. Cambios de esta sesión fuera de código de tarea: `docs/02-stack-tecnologico-y-plan-implementacion.md` (fila de ADR-033 en §4), `docs/adr/ADR-033-tres-roles-de-base-de-datos-no-dos.md` (nuevo), `CLAUDE.md` (sección "Trabajo entre sesiones"), `HANDOFF.md` (nuevo).

## Problemas conocidos

- **Límite de sesión transitorio en agentes en segundo plano**: interrupción recurrente, no relacionada con el trabajo en sí. Se resuelve reanudando el mismo agente por `SendMessage` indicando dónde se cortó — nunca relanzar un agente duplicado para la misma tarea.
- **Resuelto:** `CommittedDatabase` ya no hace `migrate:fresh` ni resetea `RefreshDatabaseState::$migrated` — vacía tablas con `DELETE`+reintento por el rol de migración y preserva los catálogos de producto. 12 pasadas completas de `make test` en verde.
- **Resuelta:** la pregunta de diseño sobre impresión diferida de credenciales — ver ADR-034 en «Decisiones tomadas». Ya no bloquea nada; solo falta que la tarea 1.10 la respete.
- **Resuelto (corregido en esta sesión):** `CredentialSeeder` no rellenaba `credentials.uuid` (NOT NULL desde la migración de 1.5), lo que rompía `migrate:fresh --seed` con `SQLSTATE[23502]`. Añadido `uuid` generado por fila. Verificado con `migrate:fresh --seed` completo de principio a fin (Pint y PHPStan también limpios sobre el fichero).
- **Deuda anotada por 1.4, no bloqueante, para tareas futuras**: falta E2E Playwright del ciclo completo con offline→sync (1.8/1.9), mutación sobre `DebouncePolicy` (`make mutate`), prueba de carga k6 (RNF-P-06), prueba de reconciliación de `daily_totals` (2.7), y una prueba que ejecute los seeders (habría cazado el defecto de `CredentialSeeder` antes).
- **Decisiones de 1.4 para que las revise `arquitecto-dominio` cuando toque** (no bloquean, quedan anotadas): puertos nuevos `ScanLog`/`ScanMetrics` en `Attendance/Application/Port/` (mismo patrón que `WorkDayRepository`); `ScanResult`/`ScanIntent` viven en `Port/`, no en `Domain/`; el docblock de `WorkDayRepository::save()` (fichero de 1.1) dice que la proyección la hace `save()` pero en realidad la hace un listener — desactualizado; `EmployeeClockedIn`/`EmployeeClockedOut` no llevan `deviceId`/`scanId`; `RN-07` (`MINIMUM_COMPUTABLE_MINUTES`) es una constante en el handler en vez de venir de `installation_settings`.

## Decisiones tomadas

- **ADR-033**: tres roles de base de datos, no dos (`fichaje_migrator` / `fichaje_app` / `fichaje_maintenance`), porque PostgreSQL 16+ no permite quitarle `SUPERUSER` al rol de arranque del clúster y `fichaje_app` lo era. No revierte ADR-027, lo corrige en su literal de conteo.
- **Orden de oleadas**: 1.14 y 1.6 antes que 1.5/1.4, siguiendo las precondiciones específicas de cada tarea (no el diagrama simplificado de fin de documento, que las mostraba como independientes).
- **Separación de responsabilidades 1.4/1.5 en paralelo**: 1.4 usó un doble de prueba para `CredentialResolver` y no tocó `Identity/Infrastructure`, dejando el adaptador real (`HmacSignatureVerifier`) exclusivamente a 1.5.
- **Revisión seguridad-cumplimiento de 1.5: sin bloqueantes.** 8 hallazgos medio/bajo, todos no bloqueantes y repartidos a tareas futuras (1.7: límite de tasa por actor sobre `/credentials`, formato `problem+json` en 429; 1.10: motivo de revocación estructurado, trigger de inmutabilidad en `credentials`, atribución de actor en `credentials:issue`; guardas de producción para `APP_DEBUG`/suelo de rechazo). Detalle completo en el resultado del agente, no repetido aquí.
- **Tres correcciones aplicadas directamente** (recomendadas por esa revisión como baratas y no bloqueantes, pero mejor cerradas ya): `EloquentCredentialRepository::save()` ya no reescribe todas las columnas, solo `revoked_at`/`revoked_reason`; `IdentityServiceProvider` falla cerrado (`return false`) ante un tokenable de Sanctum desconocido en vez de aceptarlo; `#[SensitiveParameter]` en los cuatro puntos que manejan el payload QR en claro (`HmacSignatureVerifier::resolve()`, `QrPayload::of()/parse()/tryParse()`). **Reconciliadas con ADR-034 y verificadas: 150/150 pruebas de Identity y PHPStan 9 limpio tras el rediseño de `arquitecto-dominio`.**
- **ADR-034 (`arquitecto-dominio`): el token de la credencial nace al imprimir, no al emitir.** Resuelve el bloqueo de 1.10: `credentials.secret_hash`/`key_id` son irreversibles, así que no pueden sobrevivir desde la emisión hasta una impresión diferida — tienen que generarse en el momento de imprimir. Emitir (`POST /credentials`) pasa a ser un acto administrativo sin secreto (`uuid`, `employee_id`, `issued_at`); imprimir (tarea 1.10) acuña `key_id`+`secret_hash`+`printed_at` juntos y de forma irrepetible (sin `--force`); entregar sigue igual. Tarjeta perdida: revocar→reemitir→imprimir, tres actos auditados. El esquema `QrPayload` desaparece del contrato OpenAPI (ninguna respuesta HTTP vuelve a llevar el QR en claro). Reescribió `Credential.php`, `IssueCredential`, `IssuedCredential`, exenciones nuevas (`CredentialAlreadyPrinted`, `CredentialNotPrintedYet`, `CredentialAlreadyDelivered`), migración `2026_08_20_100100_mint_credential_secret_at_print_time.php`, y dejó un bloque "Lee esto antes de empezar" en la tarea 1.10 del plan con el detalle de implementación (orden de la transacción de impresión, invariantes con CHECK a ambos lados, riesgo residual si se pierde la respuesta HTTP tras imprimir). Deja abierta a propósito la tensión con la rotación de clave con solape (tarea 2.12, fuera de esta fase) — anotada en las consecuencias de la ADR.

## Último punto trabajado

Tareas 1.5 y 1.4 cerradas del todo. 1.4 verificada en verde por su agente: 965 pruebas, `make quality` limpio, concurrencia real demostrada (10 peticiones paralelas al mismo `scan_id`, 30 fichajes simultáneos), `daily_totals` cuadrando con `shift_entries` (0 divergencias sobre 44.169 filas). Encontró y corrigió dos condiciones de carrera reales del propio código de 1.4 (conexión heredada por los hijos del fork; anomalía de lectura bajo READ COMMITTED en el anti-rebote). Encontró un defecto en la entrega de 1.5 (`CredentialSeeder` sin `uuid`) que **ya he corregido y verificado yo directamente** con un `migrate:fresh --seed` completo.

Wave 6 (1.7, 1.10, 1.13) verificada y cerrada en la sesion siguiente. Los tres agentes ya habian dejado el codigo escrito (endpoints de scan/kiosk, impresion de credenciales, PIN) pero esta seccion de HANDOFF decia "lanzada y en curso" cuando ya estaba en el arbol de trabajo sin verificar. Verificacion completa:

- `make quality` tenia fallos reales: 1 de Pint (un `@param` superfluo), 16 de PHPStan (tres imports `use` redundantes en docblocks `@see` que ademas violaban Deptrac al cruzar modulo/capa -- corregidos citando la clase en prosa en vez de con `use`; varios `is_string()` siempre-verdadero sobre argumentos de consola que Larastan ya tipa por la firma del `$signature`; once casts `(string)` sobre `mixed` en `PinIssuanceEndpointsTest.php`, corregidos con el patron `is_string($x) ? $x : ''` que ya usaba el resto del fichero) y 3 de Deptrac (los mismos imports de docblock). Todo corregido; `make quality` en verde.
- `make test` tenia 2 fallos reales:
  - `OpenApiContractTest`: la lista de rutas esperadas no incluia los endpoints nuevos de la Wave 6. Actualizada.
  - Bug de fondo en la idempotencia de `/scan` y `/scan/batch` (regla dura 8): `RegisterScanHandler::replay()` recalculaba `worked_minutes` desde el estado ACTUAL de `shift_entries` en vez de devolver el que se calculo la primera vez. Un reenvio despues de que el mismo dia tuviera mas fichajes devolvia un numero distinto al original -- viola RF-AT-07 literalmente. Solo lo destapo el test de idempotencia de lote de la 1.7 (clock_in + clock_out + reenvio de ambos); las pruebas de idempotencia de la 1.4 solo reenviaban un unico escaneo y nunca lo ejercitaron. Corregido con una columna nueva `scan_events.worked_minutes` (migracion `2026_08_19_101100_create_scan_events_table.php`, todavia sin commitear, asi que se edito in situ en vez de con expand/contract): se fija en el momento del fichaje y se lee tal cual en el reenvio, tanto para aceptados como para el anti-rebote. CHECK nuevo `scan_events_chk_worked_minutes` (nulo solo en rechazo real). Toco `ScanRecord`, `RecordedScan`, `EloquentScanLog`, `ScanEvent`, y los fixtures de `ScanLogTest.php`/`DatabaseConstraintsTest.php`/`EdgeCaseSeeder.php`/`VolumeSeeder.php` que insertaban filas sin la columna nueva. Eliminado `accumulatedMinutes()`, que quedo sin uso.
- 1009 pruebas, `make quality` y `make test` en verde tras las correcciones.
- Pendiente de la propia Wave 6: ninguno de los tres agentes toco `frontend-admin/` -- no hay pantalla de panel para PIN ni para impresion/estado de credenciales. Falta lanzar `frontend-panel`.

Incidente de esta sesion, ya resuelto: al revisar `docs/problemas/posibles_problemas.md` se ejecuto por error `rm -f` sobre el fichero (no trackeado en git). Reconstruido palabra por palabra del contenido leido momentos antes en la misma conversacion; verificado identico al original. Sin perdida real, pero queda anotado.

Sobre `docs/problemas/posibles_problemas.md`: su contenido es una instantanea vieja (de antes de que 1.2/1.3/1.5 se dieran por completadas) y ya no describe el proyecto actual -- cita ficheros que nunca existieron con esos nombres (`RegisterScanAction.php`, `GetEmployeeByPinAction.php`; los reales son `RegisterScanHandler.php` y los handlers de `Workforce/Application/UseCase/*Pin*`). No se ha tocado el fichero mas alla de restaurarlo: decidir si se borra o se marca obsoleto queda para quien lo pidio.

No hay ningún agente en segundo plano corriendo en este momento.

## Siguiente acción

**Hallazgo adicional al preparar esta accion**: `frontend-admin/` es un esqueleto (router, shell, i18n, cliente API generado) pero cada carpeta de `src/features/` solo tiene un README.md placeholder. No solo faltan las pantallas de 1.10 y 1.13: **tampoco existe la de 1.6** (login de gestion, listado/ficha de empleado), que 1.13 necesita como base (el PIN se muestra en la ficha). Consultado con el usuario: eligio construir las tres en una sola pasada.

**Agente `frontend-panel` completado y fusionado en esta sesion.** Trabajo en worktree aislado (arrancado desde el commit `ed5e515`, sin las ~500 modificaciones sin commitear de esta fase); al terminar copie manualmente `frontend-admin/src/`, `frontend-admin/tests/` y `frontend-admin/env.d.ts` al arbol principal (sin tocar `docs/api/openapi.yaml`: el agente lo habia copiado byte a byte desde el checkout compartido solo para poder regenerar `schema.d.ts` en su worktree aislado, no es un cambio suyo). Worktree y rama `worktree-agent-a44ea72bcdcc2114e` ya eliminados tras el merge.

Construido: autenticacion de gestion (login/logout/me, Pinia, guarda de rutas por ambito de token — sin 2FA, es de la 2.1), listado/alta/ficha/baja de empleado, panel de credenciales con los cinco estados derivados + impresion individual/lote + registro de entrega confirmado + emision/revocacion (anadidas por el agente para que el panel tenga salida en todas las filas), y el dialogo de PIN de un solo vistazo + restablecimiento con desbloqueo inmediato.

**Verificado tras el merge, en el arbol principal (no solo en el worktree):** `npm run type-check`, `npm run lint`, `npm run test:unit` (108 pruebas, cobertura 90,95/81,71/71,25/90,95 % statements/branches/functions/lines, umbral del doc 02 §9.2 es 70 %) y `npm run build` — los cuatro en verde.

**Decisiones del agente que quedan anotadas, no bloqueantes:**

- Token de sesion en `sessionStorage` (no `localStorage`, no persistido en Pinia): la sesion de gestion es corta y el equipo del quiosco/recepcion es compartido. El PIN no toca ninguno de los dos.
- Rutas en ingles (`/login`, `/employees`, `/employees/:uuid`, `/credentials`, `/forbidden`).
- Motivo obligatorio (catalogo + libre) en baja y en revocacion, aunque el contrato lo declara opcional — decision defendible (RRHH necesita saber meses despues por que alguien no pudo fichar) pero revisable si se quiere un motivo canonico en la auditoria en vez de texto libre traducido.
- Sin virtualizacion de tabla (el listado pagina en servidor); hara falta cuando llegue la rejilla de jornadas con meses de historico.
- `LoginRequest` del contrato es correo + contrasena, no codigo de empleado — el agente siguio el contrato real en vez de mi brief, correctamente.

**Dos cosas pendientes de otra tarea/agente, anotadas por el propio `frontend-panel`:**

1. **Sin confirmar con `backend-laravel`**: si la *lectura* de `GET /credentials/status` (que expone nombre, codigo, centro y departamento de toda la plantilla) queda registrada en `audit_log`, o solo las escrituras. Revisar antes del cierre de fase.
2. `GET /credentials/status` no devuelve la zona horaria del centro (solo `site_id`/`site_name`); el panel cruza con `GET /sites` para poder pintar `printed_at`/`delivered_at` en hora local. Funciona pero son dos llamadas para un dato que la fila podria traer — candidato a ajuste de contrato si molesta.

**Pendiente real de Wave 6, mas alla de lo anterior: ninguno.** Falta el criterio de terminado no automatizable de la tarea 1.10 (paso 11 del plan): imprimir una tarjeta de verdad y escanearla en el quiosco — requiere tablet e impresora fisicas, es para quien tenga el hardware a mano, no para un agente.

Wave 7 leida y lanzada en esta sesion (dos agentes en paralelo, background, cada uno en worktree aislado):

- **1.8** (`frontend-quiosco`): PWA del quiosco -- escaneo continuo con ZXing, wake lock, feedback visual/sonoro, pantalla de confirmacion, i18n, accesibilidad AA, aviso de privacidad, presupuesto de rendimiento, y dejar Playwright configurado de verdad (`npm run test:e2e` hoy apunta a un placeholder `scripts/e2e-not-ready.mjs`). Brief explicito de que la prueba de resistencia de 12 h en tablet real NO es su responsabilidad (necesita hardware).
- **1.15** (`arquitecto-dominio`, primera mitad del relevo `arquitecto-dominio` -> `backend-laravel`): diseno del objeto de valor `CorrectionReason` (con `OTROS` exigiendo >= 20 caracteres de forma inconstruible), invariantes RN-13/RN-06/RN-05, puertos y firma de `CorrectShiftHandler`, evento `ShiftCorrected`, pruebas unitarias de dominio. Explicitamente NO toca migraciones, HTTP, policies ni `openapi.yaml` -- eso es la segunda mitad, para `backend-laravel`, que hay que lanzar cuando este termine.

Ambos trabajan en worktrees aislados arrancados desde el ultimo commit (`ed5e515`), sin las modificaciones sin commitear de esta fase -- igual que el agente de la Wave 6, habra que fusionar manualmente sus cambios al arbol principal al terminar (copiar ficheros nuevos/modificados, sin arrastrar su copia de `docs/api/openapi.yaml` si la tienen, que sera la vieja del commit base) y reverificar alli, no solo en el worktree.

**1.8 cerrada del todo en esta sesion.** El primer intento se corto por el limite de sesion transitorio ya conocido (no relacionado con el trabajo); se reanudo por `SendMessage` desde el punto exacto donde se corto ("Now the degraded-QR spec and package/tsconfig wiring") y termino en verde. Fusionado al arbol principal (solo `frontend-kiosk/`, sin tocar `docs/`) y **reverificado alli, no solo en el worktree**: `type-check`, `lint`, 161 pruebas unitarias, `build` (60,6 KiB JS critico / 3,2 KiB CSS gzip, presupuesto 250/40 KiB), 20 pruebas E2E con Playwright y camara simulada (QR limpio + degradado + accesibilidad con `axe-core`, 0 violaciones), y la cabecera `Permissions-Policy: camera=(self)` contra el Nginx real. Instalado `npm install` en el arbol principal (el agente anadio `@playwright/test` y `@axe-core/playwright`).

Decision de alcance que quedo bien resuelta por el propio agente: preparo el canal de errores del cliente (`errorReporter`, saneado anti-PII) pero **no lo conecta al `heartbeat`** porque `KioskHeartbeatRequest` no tiene campo para ello en el contrato (`additionalProperties: false`) -- exactamente lo que pedia el encargo ("preparar, no necesariamente consumir"). Consumirlo de verdad es la tarea 5.12 (Fase 5), no bloquea nada de esta fase.

**Pendiente real de 1.8, no automatizable:** la prueba de resistencia de 12 h en tablet real (memoria, temperatura, bateria, recuperacion del wake lock) -- necesita hardware, queda para quien lo tenga.

**1.15 cerrada del todo en esta sesion.** `backend-laravel` se corto tambien por el limite de sesion transitorio nada mas empezar -- tan pronto que **nunca llego a crear su worktree aislado**: al reanudarlo por `SendMessage` continuo directamente sobre el arbol compartido. Verificado dos veces, no solo por el informe del agente: `make quality` y `make test` (**1097 pruebas, 5007 aserciones**) ejecutados independientemente por mi despues de que el agente dijera que habia terminado, ambos en verde. `MSI` del dominio 84,80 % (umbral 80 %). `qa:traceability --check` en verde.

Cuatro decisiones que el agente resolvio por su cuenta, ya revisadas:

1. Puerto nuevo `ShiftEntryHistory` para distinguir 404 de 409 en `PATCH`/`void` (la firma de `WorkDayRepository::findWorkDayOfShiftEntry()` que fijo `arquitecto-dominio` no lo permitia sin este puerto extra). Razonable, revisable si `arquitecto-dominio` prefiere otro sitio.
2. El mapeo de excepciones de carrera (`ShiftAlreadyOpen`, `OverlappingShiftEntry`, `ClockOutBeforeClockIn`) a 409/422 esta acotado por ruta (`attendance.shift-entries.*`), para no filtrar la causa de un rechazo de `/scan` (regla dura 17, RS-03).
3. **Corrigio un bug preexistente de `EmployeeSeeder`** (escribia `pin_hash` sin `pin_issued_at`, violando una restriccion de la tarea 1.13) que tenia rota `make migrate` en la base de datos de **desarrollo** desde esa tarea. La de pruebas no se vio afectada. **Ya reconstruida** (`make migrate-fresh && make seed`, confirmado por el usuario antes de ejecutar por ser una accion destructiva): 58.885 tramos, 3 correcciones sembradas con sus versiones anteriores, 0 divergencias de proyeccion.
4. Regenero `frontend-kiosk/src/shared/api/schema.d.ts` pese a que mi encargo de la 1.8 decia que no tocara `frontend-kiosk/` -- correcto: la CI regenera los tres clientes y falla si alguno diverge del contrato, y es un fichero generado. Verificado tras el hecho: `frontend-kiosk` y `frontend-admin` siguen con `type-check` limpio.

**Deuda de pruebas anotada por el propio `backend-laravel` para `qa-testing`, no bloqueante:** E2E del recorrido «corregir un tramo y ver bajar el total» (no puede escribirse hasta que el panel de la 1.16 exista), prueba de concurrencia real sobre `PATCH` (dos gestores corrigiendo el mismo tramo a la vez, `CorrectShiftHandler` no reintenta a proposito), prueba property-based sobre `CorrectionReason` con texto multibyte, y una correccion que cruce el cambio de hora de octubre (`EdgeCaseSeeder` ya tiene los datos, falta la prueba).

**Wave 7 (1.8 + 1.15) cerrada del todo.**

**Wave 8 leida y lanzada en esta sesion (tres agentes en paralelo, background, cada uno en worktree aislado):**

- **1.9** (`frontend-quiosco`): cola offline Dexie -- encolar antes de confirmar, padron cacheado cifrado, backoff exponencial, `POST /scan/batch` en lotes de 50 ordenados por `occurred_at`, borrado solo tras confirmacion del servidor, indicador de pendientes. Construye sobre `ScanSubmissionPort`/`RosterLookupPort` que la 1.8 ya dejo preparados en `directSubmission.ts` para esto exacto.
- **1.17** (`backend-laravel` + skill `/informe-nuevo`): exportacion legal para Inspeccion -- `GET /api/v1/reports/legal-export`, CSV con streaming y BOM, correcciones de la 1.15 incluidas con autor/motivo, comando `compliance:legal-export`, runbook, mas la descarga desde `frontend-admin/src/features/reports/`.
- **Hueco descubierto en el plan, resuelto antes de lanzar 1.16**: el paso 1-2 de la tarea 1.16 da por hecho que `GET /api/v1/employees/{uuid}/workdays` "ya existe desde la 1.11" -- pero 1.11 (portal del empleado) todavia no se ha ejecutado (Wave 9, despues de esta) y `backend/app/Modules/Reporting/` esta completamente vacio (solo `.gitkeep`). Como `frontend-panel` no toca backend, lance primero un `backend-laravel` **solo para ese endpoint de lectura** (consulta + autorizacion `manager+|self`, distinto de `GET /me/workdays` que si es de la 1.11). **1.16 (`frontend-panel`) se lanza cuando este termine, no antes** -- construye la pantalla directamente sobre la forma de respuesta que este agente deje documentada.

Los tres tocan `docs/api/openapi.yaml`/`routes/api_v1.php` a la vez (los dos `backend-laravel`) -- mismo patron que la Wave 6, avisados explicitamente, fusion manual al terminar cada uno.

Al recibir cada resultado: fusionar al arbol principal (si no crearon worktree por corte de sesion temprano, como paso con la 1.15, ya estara en el arbol compartido) y reverificar alli con `make quality && make test` / `npm run type-check && lint && test:unit && build` + `make e2e` segun toque, no fiarse solo del informe del agente. En cuanto el endpoint de `workdays` cierre, lanzar `frontend-panel` para 1.16 con su forma de respuesta exacta.

Cuando las tres tareas de la Wave 8 (1.9, 1.16, 1.17) cierren del todo: leer el detalle completo de la Wave 9 (1.11 portal del empleado, 1.12 PIN de respaldo en el quiosco) y lanzarla.

**Sesión nueva (kronoqr-e3): triaje real del estado de la Wave 8 antes de seguir.** `ListAgents` no mostraba ningún agente en segundo plano (sesión distinta a la que los lanzó, no reanudables por `SendMessage`). Inspeccionados los tres worktrees directamente (diff contra `main` y contra `ed5e515`) en vez de fiarme del HANDOFF anterior, que los daba por "lanzados" sin decir en qué quedaron:

- `agent-a72d88d4e68f7d23e` (1.9, cola offline Dexie): por inspección parecía **completa** (dominio/aplicación/infraestructura, cifrado del padrón, sync runner, 4 tests unitarios + 1 E2E) pero sin verificar en verde ni fusionar.
- `agent-a55f152c60aa716f2` (endpoint `GET /employees/{uuid}/workdays`, prerrequisito de 1.16): **cortado a media tarea** — dominio/aplicación/infraestructura de `Reporting` y el contrato OpenAPI hechos, pero sin controlador HTTP, sin ruta, sin policy, sin pruebas.
- `agent-a84de444df3fea535` (1.17, exportación legal): **cortado a media tarea** — dominio/aplicación/infraestructura de `Compliance` (incluida la clase del comando de consola) y parte del contrato OpenAPI hechos, pero sin controlador HTTP, sin ruta, sin registrar el comando, sin runbook, sin pantalla en `frontend-admin/src/features/reports/`, sin pruebas.

Relanzados los tres, cada uno como agente nuevo pero **apuntado al worktree existente** (instruido explícitamente a no repetir lo ya hecho, solo completar y verificar), en paralelo y en segundo plano.

**1.9 cerrada del todo en esta sesión.** El agente reverificó lo que ya estaba (era casi todo correcto) y encontró y corrigió un bug real: `ScanQueue.confirm()` se tragaba un `store.remove()` fallido, `drain()` volvía a reclamar las mismas filas y las reenviaba sin límite — bucle HTTP no acotado si IndexedDB fallaba o iba llena en una tablet real. Corregido: `confirm()` devuelve si el borrado se comprometió de verdad; si no, reprograma con backoff en vez de reenviar inmediatamente; añadido `MAX_BATCHES_PER_DRAIN` como cinturón de seguridad. También corrigió 4 condiciones de carrera en E2E (aserciones que asumían que la petición al servidor ya había pasado cuando en realidad 1.9 confirma localmente *antes* de la red, por diseño) con `expect.poll`. Fusionado manualmente al árbol principal (`frontend-kiosk/src`, `tests`, `package.json`/`package-lock.json`; borrado `features/scan/application/directSubmission.ts`, superseded por la cola real) y **reverificado allí, no solo en el worktree**: `type-check`, `lint`, 231 pruebas unitarias (90/82/87/90 % st/br/fn/lines), `build` (98,5 KiB JS crítico / 3,2 KiB CSS gzip, dentro de presupuesto 250/40 KiB) y 25 pruebas E2E con Playwright, todo en verde. Instalado `npm install` en el árbol principal (el agente añadió `fake-indexeddb` como dependencia de test). Worktree y rama `worktree-agent-a72d88d4e68f7d23e` eliminados tras el merge.

Deuda no bloqueante anotada por el propio agente: prueba de resistencia de 12 h en tablet real (memoria, batería, IndexedDB bajo presión de almacenamiento — justo la vía del bug que corrigió), transiciones offline↔online reales (el E2E las simula abortando rutas y disparando el evento `online`), y verificación de la cabecera `Permissions-Policy: camera=(self)` contra el Nginx real (ya cubierta en la 1.8, no repetida aquí). `qa:traceability --check` no se pudo ejecutar en el worktree (entorno sin `backend/vendor` ni PHP 8.4) — pendiente de confirmar cuando se ejecute `make test` completo del backend.

**Endpoint `GET /employees/{uuid}/workdays` (prerrequisito de 1.16) cerrado del todo en esta sesión.** El agente completó la capa HTTP que faltaba (`Controller`, `Policy` `manager+` = `{admin, rrhh}`, `FormRequest`, `Resource` que hace la única conversión a hora local, telemetría sin PII) y de paso corrigió un bug real que dejó el agente anterior: un heredoc en `DatabaseWorkDayJournalReader.php` interpolando `{$this::CURRENT_ONLY}` — un heredoc no interpola constantes de clase, así que el endpoint devolvía `500` siempre. Fusionado al árbol principal (todo el módulo `Reporting`, `bootstrap/app.php`, `routes/api_v1.php`, `docs/api/openapi.yaml`, `tests/Contract/OpenApiContractTest.php`, `tests/Feature/Reporting/`, `tests/Unit/Reporting/` — diffs verificados como puramente aditivos antes de copiar, cero contenido del árbol principal perdido) y **reverificado allí, no solo en el worktree**, contra el entorno Docker real: `make quality` (Pint + PHPStan 9 + Deptrac + contrato OpenAPI) en verde, `make test` **1131 pruebas / 5153 aserciones** en verde, `qa:traceability --check` en verde. Worktree y rama `worktree-agent-a55f152c60aa716f2` eliminados tras el merge.

Decisiones del agente ya revisadas: `self` (el propio empleado viendo su jornada) **no se implementa aquí a propósito** — es `GET /me/workdays` con `self:read`, tarea 1.11, y un token de portal no alcanza esta ruta porque le falta `attendance:read`; `auditor` recibe `403` aunque su token lleve `attendance:read` (el Anexo B no lo pone en `manager+`, lo suyo es 1.17); ámbito `attendance:read` y ruta deliberadamente fuera del grupo `employees:*` aunque comparta prefijo de URL (leer horas no es leer la ficha). `docs/trazabilidad-pruebas.md`: el agente descartó por error una regeneración propia con `git checkout --` y lo restauró desde una copia idéntica del árbol principal — sin pérdida, confirmado por el propio agente con `git diff --stat`.

**Deuda anotada por el agente, para `qa-testing` y para la propia 1.16 (no bloqueante):** E2E del recorrido «detalle de jornada con una corrección de la 1.15, resumen de→a» (no puede escribirse hasta que exista la vista `frontend-admin/src/features/workdays/`, que es la 1.16 propiamente dicha); Vitest del componente de suma de tramos + accesibilidad AA; integración con volumen (un mes cargado, confirmar que las cuatro consultas planas del adaptador no son N+1); decidir si el aviso `reporting.daily_totals_divergence` debe incrementar una métrica (hoy es solo un `warning` sin prueba); `make mutate` no se ejecutó sobre los VO nuevos de `Reporting` (no debería bajar el MSI del dominio, pero no está confirmado).

**1.16 lanzada en esta sesión** (`frontend-panel`), en segundo plano, **directamente en el árbol principal** (no worktree aislado — ya no hace falta, el prerrequisito que la bloqueaba está mergeado y en verde). Consume `GET /employees/{uuid}/workdays` (`EmployeeWorkDaysResource`, ruta `reporting.employees.workdays`), reutiliza el shell/router/auth/i18n ya construidos del panel.

Pendiente: los resultados de 1.16 (detalle de jornada) y de 1.17 (exportación legal), ambos aún en segundo plano. Al recibir cada uno: fusionar al árbol principal si hiciera falta (1.16 ya está en el árbol principal, solo hay que reverificar; 1.17 sigue en su worktree — diffs aditivos a verificar antes de copiar) y reverificar allí con `make quality && make test` / `npm run type-check && lint && test:unit && build` + `make e2e` según toque, no fiarse solo del informe del agente.

**Lección repetida**: no dar una wave por "cerrada" ni por "lanzada y en curso" en HANDOFF sin haber comprobado el estado real de cada worktree/agente al empezar la sesión siguiente.

**Sesión nueva (kronoqr-e9): triaje de la Wave 8 al empezar, otra vez por instrumento, no por confianza en el HANDOFF anterior.** `ListAgents` no mostraba ningún agente en segundo plano (sesión nueva). Comprobado el árbol principal y el único worktree que quedaba (`.claude/worktrees/agent-a84de444df3fea535`, rama `worktree-agent-a84de444df3fea535`) con `find`/`diff` explícitos, no de memoria:

- **1.16 (panel: detalle de jornada) — la entrada anterior de este HANDOFF decía "lanzada en esta sesión, directamente en el árbol principal", pero en el árbol principal `frontend-admin/src/features/workdays/` solo contiene el `README.md` placeholder.** No hay vista, no hay ruta en el router, no hay nada que fusionar: el agente de la sesión anterior no llegó a escribir ningún fichero antes de que la sesión terminara. Se trata como no empezada.
- **1.17 (exportación legal) — completa en el worktree, sin fusionar al árbol principal.** Todas las capas de `Compliance` (dominio/aplicación/infraestructura/HTTP con controlador+policy+request), la ruta `GET /reports/legal-export`, cuatro ficheros de test, la vista `frontend-admin/src/features/reports/LegalExportView.vue` + su ruta en el router. Sin fusionar todavía: el árbol principal solo tiene el `README.md` placeholder de `reports/` y el `Compliance` de antes de 1.17 (solo audit log de 1.14). **Ninguno de los dos árboles se puede copiar entero al otro**: el `openapi.yaml` del worktree parte de antes de que se mergeara el endpoint de `workdays` (prerrequisito de 1.16) y no lo tiene, mientras que el del árbol principal no tiene la sección `Compliance`/`legal-export` que añadió 1.17 — hace falta empalmar, no sobrescribir. Tampoco se encontró runbook para la exportación legal en el worktree pese a que la tarea lo pedía.

Relanzados: `backend-laravel` para completar la fusión de 1.17 (empalme cuidadoso de `openapi.yaml`, `routes/api_v1.php` y `ComplianceServiceProvider.php`, runbook que falta, verificación con `make quality && make test` y `qa:traceability --check` en el árbol principal, limpieza del worktree al terminar) y `frontend-panel` para construir 1.16 desde cero (no hay nada que retomar), ambos en paralelo y en segundo plano.

**1.17 cerrada del todo en esta sesión.** El agente fusionó todas las capas al árbol principal (empalmando `openapi.yaml` —tag `Compliance` + path `/api/v1/reports/legal-export`, sin tocar el `Reporting`/`workdays` ya existente—, `ComplianceServiceProvider.php`, `routes/api_v1.php`, `bootstrap/app.php`, y regenerando el `schema.d.ts` de los tres frontends). Encontró y corrigió tres defectos reales al fusionar, no solo al construir:

1. `backend/tests/Architecture/AggregateBoundaryTest.php` tenía un guardia mal escrito (`str_contains($source, 'ShiftEntry')` seguía dando positivo con `ExportedShiftEntry`) y su propio comentario explicativo contenía las formas literales que el test existe para detectar, en un fichero que se explora a sí mismo. Corregido con límite de palabra en la regex.
2. `backend/storage/framework/legal-exports/` no tenía `.gitignore`: cada `make test` dejaba CSV con registro horario nominal sin ignorar por git. Añadido el `.gitignore` y borrado el residuo.
3. El runbook (`docs/runbooks/requerimiento-inspeccion.md`, sí existía pese a que el brief decía que faltaba) citaba una ruta de navegación del panel que no existe («Informes → …»); la sección real es **Inspección**, en `/admin/reports/legal-export`. Corregido.

**Verificado en el árbol principal contra Docker real, no solo por el informe del agente**: `make quality` verde (Pint 656 ficheros, PHPStan 9 sin errores, Deptrac 0 violaciones, contrato OpenAPI 0 problemas), `make test` **1185 pruebas / 7461 aserciones, 0 fallos** (48 pruebas / 2293 aserciones solo de RF-IN-05), `qa:traceability --check` verde (782 pruebas etiquetadas), `make docs-consistency` sin divergencias, `frontend-admin` con `type-check`/`lint`/`test:unit` (117 pruebas, 91,6 % líneas)/`build` en verde, `frontend-kiosk` y `frontend-portal` con `type-check`/`lint` en verde tras regenerar tipos. Worktree y rama `worktree-agent-a84de444df3fea535` eliminados (quedó una carpeta vacía bloqueada por Windows durante la fusión, sin rastro en git; se puede borrar a mano con `rmdir`).

**Deuda anotada por el propio agente, no bloqueante:** falta el nivel E2E del §9.5 («auditor entra → genera → descarga el CSV» con Playwright); prueba de integración con volumen de un año contra el techo de «menos de 1 hora» que promete el runbook (hoy solo hay volumen moderado); `make mutate` no se ha ejecutado sobre los VO nuevos de `Compliance/Domain/ValueObject`. Limitación de alcance consciente y documentada en el runbook: la exportación no filtra por centro/departamento, solo plantilla completa o una persona — cualquier alcance nuevo necesita poder describirse en `audit_log` y en la cabecera del fichero; si hace falta, es una decisión de producto, no técnica.

**1.16 cerrada del todo en esta sesión, pero con un incidente real que hay que tener en cuenta para futuras oleadas paralelas.**

**Incidente**: 1.16 y 1.17 corrían en paralelo. El agente de 1.16 (`frontend-panel`), sin que se le indicara worktree alguno (el encargo decía explícitamente "trabaja en el árbol principal, trátalo como si empezaras de cero"), terminó trabajando dentro de `.claude/worktrees/agent-a84de444df3fea535` — el mismo directorio que el encargo de 1.17 estaba usando como *fuente de lectura* y que borró al terminar su fusión (paso final que sí le pedí explícitamente). Al borrarlo, se llevó por delante el trabajo sin commitear de 1.16. El propio agente de 1.16 detectó la pérdida, no se rindió, y **rehizo todo directamente en el árbol principal desde cero**, reverificando allí. Sin pérdida real, pero **lección para próximas oleadas paralelas: si un encargo debe borrar un worktree al terminar, verificar antes de lanzar que ningún otro agente en paralelo pueda estar usando esa misma ruta**, y preferir no reutilizar nombres de worktree de sesiones anteriores como fuente de lectura mientras haya otros agentes activos.

**Qué construyó** (RF-PA-03, detalle de jornada): `frontend-admin/src/features/workdays/` completo — listado de jornadas con total, tabla de tramos (turno nocturno como un solo tramo marcado "día siguiente", fichaje encolado con su retraso explicado, descuadre de total señalado en vez de ocultado), histórico de correcciones con "antes → después" (reutilizando y extendiendo `ChangePreview.vue` con `fromLabel`/`toLabel`, ya que "valor actual/nuevo" sería falso en un histórico), ruta `/employees/:uuid/workdays` y enlace condicionado por ámbito desde la ficha de empleado. **No implementa el diálogo de corrección (RF-PA-04)** — explícitamente fuera del encargo, ámbito de token distinto (`attendance:correct` vs `attendance:read`).

**Verificado dos veces** (por el agente y de forma independiente por mí después): `npm run type-check`, `lint`, `test:unit` (**180 pruebas, 0 fallos**, cobertura 92,9/85,26/76,04/92,9 % st/br/fn/lines) y `build`, los cuatro en verde en el árbol principal.

**Pendiente de confirmar con `backend-laravel`** (mismo tipo de duda ya abierta para `GET /credentials/status`): la pantalla avisa al usuario de que la lectura de jornadas queda en `audit_log` (RS-05) — falta comprobar que existe de verdad una prueba de feature de ese asiento con su alcance antes de dar por bueno ese texto.

**Deuda anotada por el propio agente, no bloqueante**: falta el E2E con `@axe-core/playwright` que pide el plan — `frontend-admin` no tiene esa dependencia ni suite Playwright (sí la tiene `frontend-kiosk`); lo escrito es una comprobación estructural de accesibilidad en Vitest, no un análisis axe real. Añadirlo es una decisión de infraestructura del repositorio, no de esta tarea.

**Wave 8 (1.9, 1.16, 1.17) cerrada del todo.**

**Wave 9 iniciada.** Leído el detalle completo de 1.11 y 1.12 en el plan (líneas 1413-1731). Hallazgo relevante antes de lanzar: 1.11 y 1.12 comparten el mismo mecanismo de bloqueo por intentos de PIN (`Shared/Application/Port/PinAttempts.php` + `CachePinAttempts.php`, ya creado por la 1.13 pero con un solo escalón y sin distinguir origen kiosco/portal). Para no repetir el incidente de agentes en paralelo tocando el mismo componente compartido (visto en la Wave 8 con el worktree), se secuencia en vez de paralelizar del todo:

1. `backend-laravel` construye primero el backend de **1.12** (`POST /api/v1/scan/pin`), y de paso extiende `PinAttempts`/`CachePinAttempts` a los tres escalones (3→5min, 5→15min, 10→60min, reset a 24h) **con `origin` en la clave** desde ya, aunque hoy solo lo use el quiosco — para que 1.11 lo reutilice sin tocarlo de nuevo. Lanzado en segundo plano.
2. Cuando termine: lanzar en paralelo `frontend-quiosco` (pantalla del teclado PIN, 1.12) y `backend-laravel` para el backend de **1.11** (`/me/login`, `/me/workdays`, `/me/export`, reutilizando el mismo `PinAttempts` con `origin=portal`).
3. Cuando el backend de 1.11 termine: lanzar `frontend-portal-empleado` (las tres pantallas del portal — acceso, mi registro, mi exportación; **no es una PWA**, sin service worker, ADR-015).

**El agente de backend de 1.12 se detuvo en modo plan** (heredado de un modo plan de la sesión orquestadora que seguía técnicamente activo pese a no mostrar recordatorio en turnos intermedios — confirmado al recibir después el aviso explícito "Exited Plan Mode"). No escribió código; encontró cuatro problemas reales y pidió decisión antes de seguir:

1. **Contradicción real entre dos reglas**: el plan pide PIN "encolable offline" (regla dura 19) y a la vez "cero PIN en claro" en el dispositivo — ambas juntas exigen decidir cómo. **Decisión del usuario**: cifrado asimétrico del PIN en la cola offline (el quiosco cifra con la clave pública del servidor antes de encolar; solo el servidor descifra con la privada al sincronizar; confirmación provisional local, verificación e incidencia real en servidor — mismo patrón que un padrón cacheado que no reconoce una tarjeta). Comunicada al agente.
2. Duplicidad de variables de configuración del PIN (`IDENTITY_PIN_*` en uso desde 1.13 con 5/900s, vs `.env.example`/Anexo B doc 02 con `PIN_*` 3/300s + escalones, sin usar por nadie) — **aprobado** conservar `IDENTITY_PIN_*`, bajar a 3/300 y añadir los escalones ahí, limpiar el bloque huérfano y corregir el Anexo B.
3. El PIN solo no identifica a nadie (`RosterMember` no lleva código) — **aprobado** exigir `employee_code` + PIN, igual que el login del portal.
4. `scan_events.result` no tiene un valor específico para "PIN incorrecto" — **aprobado** reutilizar `rejected_unknown` (regla dura 17), sin tocar `Domain/`.

Reanudado por `SendMessage` (no relanzado de cero) con las cuatro decisiones.

**Backend de 1.12 cerrado del todo en esta sesión.** Construido sobre el diseño acordado: `RegisterScanHandler::handle()` ahora acepta una `CredentialResolution` ya resuelta, y el camino del PIN (`RegisterPinScanHandler`/`RegisterPinScanCommand`) la construye por su cuenta y reutiliza el mismo método para todo lo demás (transacción, agregado, anti-rebote, proyección, auditoría, idempotencia) — sin segundo camino donde repetir el bug de `worked_minutes` de la 1.7.

**Cifrado del PIN offline, implementado como se decidió**: sobre cerrado de libsodium (`crypto_box_seal`, X25519). El quiosco cifrará con `pin_sealing_public_key` (nuevo campo en `KioskRoster`, `null` si la instalación no ofrece PIN) y solo el servidor descifra con `IDENTITY_PIN_SEALING_SECRET_KEY` (par nuevo, específico para esto — **hay que generarlo y guardarlo con el resto de secretos de despliegue antes de ir a producción**, comando dado en el informe del agente). Un sobre que no abre **no cuenta como intento fallido** (si contara, cualquiera bloquearía el PIN de otro enviando basura con su código).

**Cambio de comportamiento de configuración que hay que anunciar**: el primer escalón del bloqueo pasa de 5 fallos/15 min (valor de facto que traía 1.13) a 3 fallos/5 min (el que pedía el Anexo B) — si algún entorno de prueba se había acostumbrado al valor viejo, ahora bloqueará antes.

**Verificado dos veces, no solo por el informe del agente** (yo mismo, de forma independiente, contra Docker real): `make quality` verde, `make test` **1274 pruebas / 7731 aserciones, 0 fallos** (antes de esta tarea: 1185/7461), `qa:traceability --check` verde (849 pruebas etiquetadas). `ResetEmployeePin` (1.13) confirmado en verde tras el cambio al puerto compartido `PinAttempts` (su `clear()` no cambió de firma).

**Un fichero de `frontend-kiosk` sí se tocó** (`tests/unit/rosterCache.spec.ts`, 3 líneas: `pin_sealing_public_key: null` en un fixture, consecuencia mecánica de hacer el campo requerido-pero-nulable en el contrato) — nada de `src/features/`, decisión razonable, no revertida.

**Deuda anotada por el propio agente, no bloqueante**: falta el E2E de Playwright «fichar por PIN en el quiosco, con y sin red» (no se puede escribir hasta que exista `frontend-kiosk/src/features/pin/`, la siguiente mitad de esta misma tarea); `make mutate` no ejecutado sobre `PinLockoutPolicy` ni los VO nuevos de `Shared/Domain`.

**Lanzados en paralelo, en segundo plano**: `frontend-quiosco` (pantalla de teclado PIN con cifrado libsodium antes de encolar/enviar, cierra 1.12) y `backend-laravel` para el backend de **1.11** (`/me/login`, `/me/workdays` reutilizando la consulta de `Reporting` con ámbito `self:read`, `/me/export` en CSV, reutilizando `PinAttempts` con `PinOrigin::Portal`).

**Backend de 1.11 cerrado y verificado por mí de forma independiente**: `make quality` verde, `make test` **1343 pruebas / 8042 aserciones, 0 fallos** (antes: 1274/7731), `qa:traceability --check` verde (896 pruebas etiquetadas). Reutilizó `PinAttempts`/`CachePinAttempts` de 1.12 sin tocarlos, tal como se pidió. De paso corrigió dos bugs reales de infraestructura de pruebas compartida: `tests/Support/Http/Api.php` no reiniciaba el guard de autenticación entre aserciones de una misma prueba (`AuthManager` es singleton, `RequestGuard` memoriza el usuario — cualquier prueba de "cuenta desactivada/revocada" pasaba en verde con el código roto); y `PinVerificationTest` era flaky preexistente por usar el reloj real con bcrypt de coste 12 (corregido con `FixedClock`).

**Dos decisiones que quedan abiertas, para revisar antes del cierre de fase:**

1. **`/me/*` no escribe en `audit_log`.** El agente argumenta que RS-05 habla de acceso a datos de **terceros**, y aquí el empleado accede a los suyos propios — razonamiento defendible, pero es la misma pregunta que ya estaba abierta desde 1.16/1.10 para `GET /credentials/status` y `GET /employees/{uuid}/workdays` (ahí sí hay terceros de por medio). Falta una revisión conjunta, probablemente de `seguridad-cumplimiento`, que fije el criterio general de qué lecturas se auditan y cuáles no, en vez de que cada tarea lo decida por su cuenta.
2. **RF-ID-08 (portal restringido a red interna por defecto) no está implementado por nadie todavía.** `PORTAL_INTERNAL_ONLY=true` existe en `.env.example` pero no lo lee ni la aplicación ni Nginx — no puede implementarse del todo hasta que exista un vhost propio para `frontend-portal/`, que es justo lo que falta construir. Pendiente para `devops-observabilidad` cuando el frontend exista, antes de cerrar la fase.

**Deuda anotada por el propio agente para `qa-testing`, no bloqueante**: E2E «entrar con código y PIN → ver registro → descargar» (no escribible hasta que exista `frontend-portal/`); concurrencia de dos sesiones simultáneas del mismo empleado; `make mutate` sobre `PortalSession`; prueba de tiempo constante a nivel HTTP de `/me/login`. Ruido ajeno detectado, no corregido: `frontend-kiosk` falla `lint` por artefactos de Playwright en `test-results/` sin excluir en el ignore de ESLint (sí está en `.gitignore`).

**Siguiente**: lanzado `frontend-portal-empleado` para la interfaz de 1.11 (tres pantallas: acceso, mi registro, mi exportación — no es una PWA, ADR-015). `frontend-quiosco` (1.12) sigue en segundo plano.

**1.12 cerrada del todo en esta sesión.** La pantalla de PIN reutiliza literalmente la cola offline de la 1.9 (extendida a unión discriminada `qr`/`pin` para que ambas vías se drenen respetando el orden cronológico real por `occurred_at`, sin variante de lote para PIN porque el backend no la ofrece) y todo el feedback visual/sonoro ya construido en 1.8. Sellado del PIN con libsodium en el cliente antes de encolar o enviar, tal como se decidió. El botón "¿Sin tarjeta?" solo aparece si `pin_sealing_public_key` no es `null` en el padrón (ADR-017: nada específico de un cliente, aquí ni siquiera visible si la instalación no activó el PIN).

Encontró y corrigió una condición de carrera real al construir el E2E: `PinView` podía expulsar a alguien a la pantalla de tarjeta en un arranque en frío/recarga directa en `/pin`, porque `pin_sealing_public_key` empieza en `null` hasta que el padrón carga — añadido un estado `pinSealingKnown` que distingue "aún no se sabe" de "no existe".

**Verificado dos veces**: por el agente y de forma independiente por mí después, en el árbol principal: `type-check`, `lint`, **287 pruebas unitarias** (cobertura 91,9/83,45/88,43/91,9 % st/br/fn/lines) y `build` — **bundle crítico 100,5 KiB / 250 KiB (40,2 %)**, dentro de presupuesto pese a añadir `libsodium-wrappers` (va en el chunk `PinView` cargado por `import()` dinámico, no en el crítico). El PIN en claro se verificó ausente en tres capas independientes (pipeline serializado, cola de Dexie reabierta con un handler nuevo, e IndexedDB leída desde fuera de la app en el E2E).

**Pendiente real de 1.12, no bloqueante**: prueba en tablet real (teclado nativo para el código de empleado, tiempo de sellado WASM en frío la primera vez del día, precarga real del service worker); el bloqueo por intentos (3/5/10) es enteramente del backend, ya verificado en 1.12-backend, el quiosco solo ve el mismo rechazo genérico.

**1.11 cerrada del todo en esta sesión.** Tres pantallas (acceso, mi registro, mi exportación), sin ninguna más. Mensaje único y genérico ante cualquier rechazo de login (nunca cuenta atrás del bloqueo del PIN, que el backend oculta a propósito); sí muestra `Retry-After` en el 429 por IP, que es un mecanismo distinto y el contrato sí lo revela. `GET /me/workdays` y `GET /me/export` no aceptan ningún identificador de empleado ni en ruta ni en query — no hay nada que una URL manipulada pueda cambiar (verificado en tres capas). Confirmado sin rastro de PWA (`grep` sin resultados) y sin librería de queries para mantener el bundle pequeño.

**Verificado dos veces**: por el agente y de forma independiente por mí después: `type-check`, `lint`, **99 pruebas unitarias** (cobertura 97,55/91,52/98,66/97,55 % st/br/fn/lines) y `build` (185 kB JS / 66,6 kB gzip, 13,8 kB CSS / 3,7 kB gzip) — los cuatro en verde. Sin infraestructura Playwright en el proyecto para SPAs de gestión (tampoco la tiene `frontend-admin`); el recorrido completo queda cubierto por pruebas de componente con `@vue/test-utils` en vez de E2E real — decisión consistente con el resto del repositorio, no inventada por el agente.

**Hallazgo real, sin resolver, para el cierre de fase**: el contrato no tiene ningún endpoint de cierre de sesión para el portal (`POST /api/v1/auth/logout` es solo de `managementToken`). El botón "Salir" borra la sesión de `sessionStorage` de ese dispositivo, pero el token del servidor sigue vivo hasta su caducidad natural (máx. 2h) — en un ordenador compartido del centro es una laguna real, aunque acotada (sin el token en `sessionStorage`, nadie puede reutilizarlo desde la interfaz). Pendiente decidir si se añade `POST /api/v1/me/logout` (`employeeToken: [self:read]`) antes de cerrar la fase.

**Wave 9 cerrada del todo** (1.11 + 1.12, backend y frontend, los cuatro verificados de forma independiente).

Leccion para la proxima sesion: verificar `make quality && make test` antes de marcar una wave como cerrada en HANDOFF.md, no solo confirmar que los agentes devolvieron un informe. El informe de un agente describe lo que intento, no necesariamente lo que quedo en verde. Y cuando un agente se corte por el limite de sesion transitorio, reanudarlo por `SendMessage` citando su ultimo resultado parcial -- nunca relanzar un agente nuevo para la misma tarea (patron ya documentado en «Problemas conocidos», confirmado de nuevo en esta sesion con dos agentes distintos).

**1.17 (exportación legal) fusionada al árbol principal y reverificada allí.** El worktree `agent-a84de444df3fea535` y su rama ya están borrados. Empalme, no sobrescritura, en los cuatro ficheros compartidos: `docs/api/openapi.yaml` (añadido el tag `Compliance` y el path `/api/v1/reports/legal-export` completo — el path es autocontenido, no necesitó ningún schema nuevo de componentes; conservado íntegro el `Reporting`/`workdays` que el worktree no tenía, y ajustada la descripción del tag `Reporting` para que no siga reclamando «las exportaciones»), `backend/routes/api_v1.php`, `backend/bootstrap/app.php` (handler de `InvalidLegalExportRequest` → 422, conservados los de `Reporting`), `ComplianceServiceProvider.php` y `tests/Contract/OpenApiContractTest.php` (lista de rutas + los cuatro contract tests nuevos, conservados los tres de `workdays`). Regenerados los `schema.d.ts` de los **tres** frontends.

Correcciones aplicadas sobre lo que entregó el agente, no cosmética:

1. **`tests/Architecture/AggregateBoundaryTest.php` estaba roto por su propio parche.** El agente añadió un `str_contains($source, 'ShiftEntry')` para que `CsvLegalExportWriter->close()` dejara de contar como llamada al método interno del tramo, pero `ExportedShiftEntry` contiene esa subcadena, así que el falso positivo seguía; y el comentario que añadió escribía `->close(` y `->open(` literales en un fichero que **se explora a sí mismo**, lo que lo convertía en su propio infractor. Sustituido por un límite de palabra (`(?<![A-Za-z0-9_])ShiftEntry(?![A-Za-z0-9_])`) y reescrito el comentario sin las formas literales.
2. **El runbook sí existía** (`docs/runbooks/requerimiento-inspeccion.md`) — la entrada anterior de este HANDOFF decía que no. Verificado contra el código (firma de `compliance:legal-export`, prefijos `TRAMO`/`CORRECCION`, `legal_exports_total`, `legal_export.generated`) y corregida solo la ruta de navegación del panel, que citaba un menú «Informes → …» que no existe: la sección se llama **Inspección** y cuelga de `/admin/reports/legal-export`.
3. `frontend-admin/tests/unit/i18n.spec.ts` fallaba `prettier --check` en el árbol principal (defecto preexistente, no de 1.17). Aplicado el formato correcto.
4. **`backend/storage/framework/legal-exports/` no tenía `.gitignore`.** El controlador genera ahí un temporal por petición y lo borra con `deleteFileAfterSend()`, pero la suite de pruebas no envía la respuesta, así que cada `make test` dejaba CSV con el registro horario nominal en un directorio **no ignorado**. Añadido el `.gitignore` con la convención de Laravel (`*` / `!.gitignore`) y borrada la residuo.

**Verificado en el árbol principal contra el entorno Docker real, no en el worktree:** `make quality` en verde (Pint 656 ficheros, PHPStan 9 sin errores, Deptrac 0 violaciones / 0 uncovered / 3685 permitidas, contrato OpenAPI 0 problemas; Rector sigue informativo con 103 ficheros, deuda preexistente de todo el repositorio). `make test`: **1185 pruebas / 7461 aserciones**, 0 fallos. Los tests de RF-IN-05 en concreto: 48 pruebas / 2293 aserciones. `qa:traceability --check` en verde (782 pruebas etiquetadas, todas las fases ejecutadas con prueba) y `docs/trazabilidad-pruebas.md` regenerado. `make docs-consistency` sin divergencias. `frontend-admin`: `type-check`, `lint`, `test:unit` (117 pruebas, 91,6 % líneas) y `build` en verde; `frontend-kiosk` y `frontend-portal`: `type-check` y `lint` en verde tras regenerar sus tipos.

**Deuda anotada de 1.17, para `qa-testing`:** no hay E2E Playwright del recorrido «entrar como auditor → generar → descargar el CSV» (§9.5 lo exige para un recorrido de usuario); no hay prueba de integración con volumen real de un año contra el techo de «menos de 1 hora» del runbook; `make mutate` no se ha ejecutado sobre los VO nuevos de `Compliance/Domain/ValueObject`. **Limitación funcional consciente, ya escrita en el runbook §5:** la exportación no filtra por centro ni por departamento — solo plantilla completa o una persona.

**Residuo de limpieza, trivial:** `.claude/worktrees/agent-a84de444df3fea535/` quedó como directorio **vacío** en disco (git ya no lo conoce y la rama está borrada); Windows lo tenía bloqueado por el propio proceso que hacía la fusión. Un `rmdir` desde fuera de esa sesión lo termina.

**Ruido de entorno, no del código:** los contenedores `node-admin`/`node-kiosk`/`node-portal` están en bucle de reinicio por un `MODULE_NOT_FOUND` de la binding nativa de rollup (`node_modules` instalados desde Windows, montados en Linux). Preexistente y sin efecto sobre nada de lo verificado aquí, que se ejecutó en el host o en el contenedor `app`.

## Notas importantes

- Instrucción del usuario, vigente para toda la fase: no crear nada fuera del alcance de la Fase 1.
- Cada tarea usa el agente que indica la tabla de `plan implementacion/03-fase-1-mvp-fichaje.md`; no improvisar otro.
- Patrón de reanudación de agentes: mensaje por `SendMessage` citando el punto exacto donde se cortó (del campo `<result>` de la notificación de fallo), pidiendo terminar pruebas + verificación final, nunca repetir trabajo ya hecho.
